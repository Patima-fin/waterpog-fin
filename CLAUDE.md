# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is
**Water POG Financial Console** — a Thai-language single-page financial dashboard (weekly cashflow, debt register, bank diary, IV/receivables, P&L, budget, etc.) for a construction/water company. UI text is Thai.

## ★ Current backend = Supabase (LIVE) — read before the Architecture section
The data backend **migrated from Google Sheets → Supabase (PostgreSQL + Realtime + Auth)** and is **live in production** as of 2026-06-16/17. `config.js` has `BACKEND:'supabase'` + `USE_SUPABASE_AUTH:true` + RLS on (see the "Supabase Phase 1/4/5" + auditLog entries near the end of this file). Practical implications:
- **`app/data_supabase.js` owns `window.WTPData`** (upsert/delete per row, realtime push, `authSignIn`, `writeTable`, `forceDeleteRows`). **`app/data_sync.js` (Google Sheets sync) early-returns and is dormant**; **`apps_script/Code.gs` / `Code.standalone.gs` are no longer deployed/used** (kept as historical fallback). Don't edit those for live behavior.
- **Every entity reads/writes through Supabase** — the 23 CRUD entities + `pnlBase`/`budgetHo` (Phase 5) + `auditLog` + `cashflowPresent` (`SHEET_TABLES`/`SHEET_TABLE_SET` in `data_supabase.js`). Schema = one JSONB row per entity (`{id, data, updated_at}`), camelCase table names **must be quoted**; PostgREST caps `select` at 1000 rows so `selectAll()` paginates with `.range()` + `.order('id')`.
- Login is **Supabase Auth** (`username@AUTH_EMAIL_DOMAIN`, role/pages from `app_metadata`); manage users on the `#users` page with a runtime service-role key. The Sheets-era `USERS`/Apps-Script descriptions in this file's *changelog* are historical.
- The much-discussed Sheets data-loss guards (grace windows, anti-clobber/anti-wedge, replaceAll base-reconcile) were a **Sheets-era concern**; on Supabase only 3 light guards remain (`_hasValidSession`, activity-gate, light mass-delete). The deep guard history below is context, not current plumbing.

## No build step — in-browser Babel (read this first)
There is **no bundler, no package.json, no npm, no tests, no lint**. `index.html` loads React 18 + `@babel/standalone` from CDN, then loads every source file as `<script type="text/babel" src="app/xxx.jsx?v=DATE">`. Babel transpiles JSX **in the browser** at page load.

Consequences:
- Every top-level `function`/`const` in a `.jsx` is a **global** shared across all files — there are **no imports/exports**. (`fmtNum`, `Modal`, `WTPData`, `WTPOverride`, `categorizePayable`, etc. are all global.)
- **Adding a new `.jsx` → add a `<script type="text/babel">` tag in `index.html`.**
- **Editing any `.jsx`/`.js`/`.css` → bump its `?v=` query in `index.html`** (e.g. `?v=20260609a` → `...b`). This is the cache-bust; without it, returning browsers serve the cached old file even after deploy. **Every edit pairs with a `?v=` bump.**

## Run / preview locally
Must be served over **HTTP** (not `file://`) — it fetches ~25 separate `.jsx`.
- `python -m http.server 8000` from repo root, open `http://localhost:8000` (entry = **`index.html`**). No Node on this machine.
- `.claude/launch.json` has an `http-server` config (port 8000) for preview tooling.
- The app has a **login gate**: a valid `wtp-session` in `localStorage` is required; data loads from **Supabase** (cached in `localStorage` key `wtp-fin-data-v8`, except `SKIP_CACHE_KEYS` entities which are fetched fresh each load). With RLS live, the anon key can't read data without a real login — to preview, render a component into an overlay with mock data + injected session (see the `preview-login-gate-verify` memory).

## Deploy = `git push` to `master` → GitHub Pages
Live at **https://patima-fin.github.io/waterpog-fin/** — GitHub Pages serves the **`master`** branch. **Changes are only visible after pushing to `master`** (there is no local→live path). The `Netlify` steps in `SETUP.md` and `netlify.toml` are **stale** — hosting is GitHub Pages. HTML entry is **`index.html`** (the older `Financial Dashboard.html` is not the live entry).

**Push checklist (every push):** bump `?v=` of each edited file in `index.html` → **update this `CLAUDE.md`** (per repo rule below) → commit → `git push origin master`.

## Architecture
- **`app/app.jsx`** — root component + **hash router** (`location.hash` → route key → page component), `data`/`setData` state, login gate (`currentUser` from `wtp-session`), sidebar/topbar. Default route is **`home`** (was `daily`).
- **`app/data.js`** — `window.WTPData` base: `load()` (reads `localStorage` cache `wtp-fin-data-v8`, else seed), `save()`, `subscribe()`, `newId()` (deterministic-ish unique id — see 2026-06-17 fix), `markInvoicesPaidFromReceipts`, `SKIP_CACHE_KEYS`. Holds seed/mock data. On Supabase, `data_supabase.js` wraps/overrides the network methods. `app/config.js` defines `window.WTP_CONFIG` (`BACKEND`, `SUPABASE_URL`/`SUPABASE_ANON_KEY`, `USE_SUPABASE_AUTH`, `AUTH_EMAIL_DOMAIN`, `BRAND_CODE`, `SESSION_TTL_MS`, `IDLE_LOGOUT_MS`, `FORCE_LOGOUT_BEFORE`, `PRESENCE_HEARTBEAT_MS`, `AUTO_PUSH_*`). **(The old `APPS_SCRIPT_URL`/`AUTO_REFRESH_MS`/`USERS` keys were removed 2026-06-25.)**
- **`app/data_sync.js`** — Google Sheets sync layer: polling (hot/cold), grace windows, anti-clobber / anti-wedge guards, row-level writes. Hardened repeatedly against data-loss (see `git log` `fix(sync)`). **★ มี backend ทางเลือก = Supabase: ตั้ง `WTP_CONFIG.BACKEND:'supabase'` แล้ว `data_sync.js` จะ early-return (ปิดตัว) ปล่อยให้ [`app/data_supabase.js`](app/data_supabase.js) เป็นเจ้าของ `WTPData` แทน — ดูหัวข้อ "2026-06-16 — Supabase backend (Phase 1)" ท้ายไฟล์.**
- **`apps_script/Code.gs`** — the backend, pasted into Google Apps Script bound to the Sheet (`Code.standalone.gs` = standalone variant, `PnL.additions.gs` = extra). Server-side guards reject emptying/halving any table.
- **`app/components.jsx`** — shared UI globals: `Modal`, `Icon`, `Badge`, `KpiTile`, `EditableNumber`, formatters `fmtNum`/`fmtMoney`/`fmtDate`.
- **`app/page_*.jsx`** — one file per page (`page_home`, `page_cashflow`, `page_cashflow_present`, `page_bank_diary`, `page_bank_recon`, `page_payment_recon`, `page_debt`, `page_debt_ledger`, `page_daily_balance`, `page_invoices`, `page_warroom_p1/p2`, `page_pnl`, `page_budget`, `page_projects`, `page_receipts`, `page_checks`, `page_users`, `page_audit_log`, `page_sts_*`, `page_interest_calc`, `page_data_extras`, `page_investor`). Plus `app/charts.jsx`, root `tweaks-panel.jsx`.
- **Investor Dashboard** (route `#investor`) — **`app/page_investor.jsx`**, a self-contained premium investor-presentation page (vanilla `React.createElement`, no JSX-children helpers; all identifiers prefixed `INV*`/`inv*`). **(2026-06-15) Rebuilt to mirror the KTB investor pitch deck "Project Turtle — Company Overview" (18 pages).** Now **10 sub-sections** via a sub-nav `sec` index: 1 Investment Highlights · 2 Company Profile · 3 Products & Technology · 4 Market Opportunity · 5 Financial Performance · 6 Project Economics · 7 Customers & Projects · 8 Team & Shareholding · 9 Sustainability (ESG) · 10 Investor Room. **TH/EN** toggle (`T` dict, persisted `wtp-inv-lang`), **Light/Dark** theme (`PAL` palettes, persisted `wtp-inv-theme`), **Present mode** (fixed full-screen), **Export** = `window.print()`. **Two data sources:** (a) **live** KPIs derived from `PCU.deriveProjects(data...)` (`invMetrics`: contractTotal/received/backlog, byRegion/byType/byFy/byProv/byStatus, `PCU.cashflowByMonth`) — used in Highlights/Customers; (b) **STATIC deck content** held in module constants (manual update each new fiscal year): `INV_HIGHLIGHTS` (6), `INV_MIX` (product-type %), `INV_FIN`/`INV_FIN_YEARS` (P&L 2023A–2025A, THB mn — Revenue 958.3, 70.3% YoY, EBITDA 88.5, etc.), `INV_DRINK` (POG Drink momentum), `INV_MARKET` (81,701 villages / 90% untapped), `INV_SHARE`/`INV_SHARE_GRP` (**confidential shareholding** — founders 61.1% / ops 30.7% / PE 8.2%, behind login + a "ข้อมูลลับเฉพาะ" badge), `INV_FOUNDERS` (3 bios), `INV_ESG` (3 pillars + indices). `INV_PRODUCTS` = the 14 real product models+prices. New UI atoms: `InvSeg` (100% segmented bar), `InvTable` (P&L/shareholder matrix), `gridR` (responsive 1-col under 760px). `InvThaiMap` (§7 "by region") **now delegates to the real 77-province `window.ThaiMap`** (vendored MIT GeoJSON — see 2026-06-23 entry), with the old stylized bubble map kept as fallback. Investor Room embeds PDFs/video via **runtime upload** (`URL.createObjectURL` → iframe/`<video>`) + a YouTube/Drive URL field (persisted `inv.videoUrl` via `WTPOverride.setRaw`) — large source files are NOT committed to the repo. Added to `isPresentation` in `Topbar`. **To update financials/shareholding: edit the `INV_*` constants at the top of `page_investor.jsx`** (they're not in the synced data).
  - **Section 6 "Project Economics" = an interactive per-project cash-flow timeline** (`InvEconomics`): a **product selector** (default `PL`/POG L) + editable **contract value** + a horizontal timeline of the project cash cycle. **(2026-06-15, build `20260615l`) Reskinned to the "Cash Flow Dashboard" Claude Design handoff** ("ออกแบบให้สวยล้ำ ... นำเสนอผู้บริหาร/นักลงทุน" → premium white-minimal investor look). The handoff's static mockup numbers were reproduced 1:1 by the existing engine's PL defaults — so the compute engine + present mode were **kept** and only the look changed. **(same day) Manual entry was then REMOVED per user request ("ตัวเลขเอาตามแบบ ไม่กรอกมือ") — all amounts are now READ-ONLY from each product's standard cost data: `cfg = INV_CF_DEF(code, prod.price)` (plain const, recomputed on product change; no React state / `setField` / `persist`). Dropped: the per-stage `InvNumIn` inputs, the contract input (now a read-only `฿` display), the Reset button, and the `WTPOverride('inv.cfp.<code>')` persistence. `InvNumIn` is now an orphaned module-level helper. Switching product is the only interaction (besides full-screen present).** **LG was re-added per user request** (2 steps: `ออก LG ค้ำประกัน` `out` `lgPct` at "7 วันก่อนลงนาม", and `ได้รับ LG คืน` `in` `lgPct` `ret:true` at "+2 ปี") → `invCfGroups(cfg)` now returns **11 day-groups** (was 9): allocation → +30d goods lot 1 → **issue LG** → **signing day** (milestone + commission 6% + install advance) → ~2mo goods lot 2 → after-PnP install lot 1 → **deliver M1 / claim 40%** (+ final goods lot) → +30d receive 40% → **deliver M2** (+ final install) → +30d receive 60% → **+2yr LG returned**. **Timeline = one column per day-group** (`column(big)` over `gs`, the per-group derived array): timing pill → **node circle with a ↓/↑/◆ icon sitting on a red→gold→green gradient axis** → a **text-forward card** (`buildEventCard`; **2026-06-15: the full card frame was removed per user feedback "ดูเบียด" — now frameless text with a box ONLY around the amount block** via `boxCol` = valley→gold / flip→green / else flow-color): icon-badge + phase tag (ระยะลงทุน/ระยะเก็บเงิน) + `#n`, TH title + EN subline + milestone extra, a tinted amount block (a **read-only big ฿ headline** = the group's flow; events show "เริ่มต้น") + a **% bar** (flow/contract), then **คงเหลือสะสม + a depth bar** (|bal|/peak). **2 phase bars** span above the columns (`ระยะลงทุน · Investment` over groups 0..valleyIdx, `ระยะเก็บเงิน · Collection` over the rest). Valley/flip groups get a floating **badge** ("จุดต่ำสุด" gold / "พลิกเป็นบวก" green). `valleyIdx` = min-cumulative group, `flipIdx` = first group whose running balance turns positive. Milestones (`mark`) carry no cash. **KPI row** (`kpiRow`, custom — not `InvKpi`): a **hero gold shimmer card** ("▼ จุดต่ำสุด — ต้องสำรองเงินทุน", peak ฿ + progress bar + % of contract + "จ่ายก่อนรับงวดแรก") · gross-profit · contract (in/out) · net-at-close. (`@keyframes invShimmer` injected via an inline `<style>` in the return.) Computes **"paid before first receipt" (`beforeRecv1`)**, **peak funding need (`peak`)**, **gross margin (`margin`)**, **net at close (`netAtClose`)**. Amounts come straight from `INV_CF_DEF(code, price)` — **read-only, no persistence** (the `WTPOverride('inv.cfp.*')` save path was removed with manual entry). `el = R.createElement` alias at module top. **Real per-product cost data is embedded from `ข้อมูลราคาต้นทุน.xlsx` (1 Jan 2569) as `INV_COST_GOODS`/`INV_COST_INST`; `INV_CF_DEF(code, price)` seeds from them (falls back to % estimate for products absent from the file). COSTS only — the selling/contract value (`รับ` side) = the `INV_PRODUCTS` price (read-only).** Cash-flow cfg fields: `g1/g2/g3` (goods), `i1/i2/i3` (installation), `commPct/lgPct/m1Pct/m2Pct`, `contract`. **Below the timeline: a custom step table (`stepTable()`, replaced the old `InvTable`/`stepRows`)** — ขั้นตอน (numbered chip colored by flow) · รับ · จ่าย · สุทธิ · **คงเหลือสะสม (centered red/green balance bar deepening into the valley)** per day-group, valley/flip rows tinted + left-bordered, plus a **gold grand-total row** + a footer summary. (Typography enlarged build `20260615k` — values 15px / chips 28px — so it reads in proportion at full width.) **PL defaults ⇒ peak ฿4.1M (76.3% before first receipt), gross profit ฿1.2M (23.1%), totals ฿5,670,000 รับ / ฿4,423,450 จ่าย / net +฿1,246,550** — matches the deck exactly. `renderTimeline(big)` + `column(big)` scale up for a dedicated full-screen present mode (`tlFull` state, fixed overlay z-1200, Esc to close, includes product selector + KPIs + enlarged timeline) — separate from the page-level Present mode.
- **`app/page_home.jsx`** — **Home Page = ศูนย์แจ้งเตือนการเงิน (Financial Alert Center)** (route `#home`, default landing page; from a Claude Design handoff). Read-only, **purely derived from live entity tables** (no `setData`). CSS prefixed `hp-*`. Layout: header → 2-col grid (alert center + "วันนี้" panel) → 3 bottom cards (โครงการ / เงินสดคาดการณ์รายสัปดาห์ / บัญชีธนาคาร).
  - **`hpBuildAlerts(data, asOf, weekProj)`** — derives actionable alerts from invoices / payables / bankAccounts / forecastEntries / projects / debtLedger, tagged by **category** (`bank` / `receivable` / `payable` / `liquidity` / `project`, shown as filter chips with counts) and **severity** (`urgent` ด่วน / `watch` เฝ้าระวัง / `track` ติดตาม → red/amber/blue tints, sorted urgent-first). **Reference date = real today** (`new Date()`), NOT `data.meta.asOf` (stale in prod). Noisy groups (overdue payables, issue/aged IVs) are **capped (top-N by amount) + aggregated** into a "…อีก N ราย · รวม ฿X" row via `emit()` — essential because real data has 400+ payables / 700+ invoices (raw = 70+ alerts → ~16 after capping).
  - **CRITICAL — do NOT use `data.pipeline` / `data.cashFlow` / `data.daily`**: those seed summary objects are **all-zero in production** (not maintained by sync). Everything is recomputed from live entities.
  - **Forecast cash timing = `PAYMENT_DATE`** (fallback `DATE`); `DATE` is the booking date. `EXPENSE_TYPE==='LOAN'` marks financing (excluded from operating cash-in/out, surfaces as its own liquidity alert). Only non-`ACTUAL/BOOKED/CANCELED` (i.e. `PLANNED`) rows count as forecast.
  - **Weekly cash projection** (`weekProj`): start = Σ current bank balance (latest `cashflowSnapshots` per `Bank_AC`, else `BALANCE`), then running balance over 5 weeks adding Σ forecast net per week (by PAYMENT_DATE). `minClosing < 0` → urgent liquidity alert.
  - **Projects card**: contract = Σ `มูลค่าสัญญาที่เซ็น`; received = Σ `receipts.netReceived` (the `Receive`/`TOTAL` project columns are **null** in synced data); AR = contract − received.
  - Defensive field accessors handle seed ↔ synced-UPPER shapes (`hpBankName/AcNo/Balance`, payables `cust_name`/`netpayment`/`due2`, no `status` field → treat all as outstanding, overdue if `due < today`).
  - **Bank logos** (`HpBankLogo` + `HP_BANKS` map): local app-icon PNGs in the repo folder **`LOGO BANK/`** (`ไทยพาณิช.png` SCB / `กสิกร.png` KBANK / `กรุงไทย.png` KTB / `กรุงเทพ.png` BBL / `เกียรตินาคิน.png` KKP) — full-color icons with brand bg baked in, shown filling the rounded tile (`object-fit:cover`). `hpBankBrand(name)` matches BANK_NAME → `{file,color}`; the `src` is `encodeURI('LOGO BANK/' + file)` (space + Thai filenames). Unknown banks (or a missing file) fall back to brand-colored initials. Account no. inline after the short name (`SCB · 4863`). **The `LOGO BANK/` folder must be committed/pushed** for GitHub Pages to serve the logos. (An earlier version pulled logos from the omise/banks-logo CDN — replaced by these user-supplied files.)
- **Project Control Dashboard** (route `#projects`) — **`app/page_project_control.jsx`** (UI) + **`app/pc_engine.jsx`** (engine), replacing the old `ProjectsPage` (still in `page_projects.jsx`, unused but kept as fallback). From a Claude Design handoff, re-themed to bright `--brand-*` blue. Excel-grade data grid (sort/multi-sort, per-column filter dropdown with **เลือกทั้งหมด** + indeterminate, **drag-to-reorder column headers** via `moveCol`, freeze/pin, resize, show-hide, conditional formatting, density, pagination) + 5 status KPI cards (each shows **count + ฿ value** of that group) + 8-tile KPI grid + 15-stage status funnel + **click-drillable** monthly cashflow + LG monitoring + right slide-out drawer (overview/installments/Finance Master tabs). **Debt Monitoring section was removed** (ไม่ได้ใช้); `PcDebtSection`/`debtByCreditor` remain defined but unused. All identifiers prefixed `PCU`/`PCGrid`/`Pc*`/`pc*` to avoid global-scope collisions. **Derives a dashboard row shape from `data.projects`** (`PCU.deriveProjects`) — status/sub-status engine, installments (unlimited งวด via `% งวด N`/`Summary Payment N`/`มูลค่า งวด N`/delivery+accept dates). **`buildInstallments` reads delivery/acceptance dates BEFORE the break/skip check** — a งวด is created when it has *any* of: % / มูลค่า / Summary Payment / delivery date / accept date (date-only n=1 with no financial col ⇒ amount = full contract, pct = 100). **`delivered`** = `isDelivered` OR has delivery date; **`paid`** = `Summary Payment N > 0` (engineer's convention) OR delivery + Payment-Status text. Forecast (`acceptDate||deliveryDate + creditTerm`, default 30), Outstanding AR = contract − received. **`deriveProjects` ends with `dedupeProjectRows`** (safety net): collapses synthetic/empty-`Contract No.` rows that duplicate a real-coded row of the same name+fy (and dedups identical real codes, keeping the most-filled `_raw`) — fixes the "same project twice / code=null pair" that leaks in from the synced sheet; **distinct real contract codes with the same site name are KEPT** (re-tendered in a different FY = genuinely different contracts, not a dup).
  - **TWO project data stores (important):** the page reads `baseProjects = localProjects (wtp-proj-control-v2, 120 cols, per-browser localStorage) ?? data.projects (synced)`. **As of 2026-06-12 the synced `ENTITY_HEADERS.projects` in `apps_script/Code.gs` + `Code.standalone.gs` was expanded 35→~120 cols** so the full งวด/ส่งมอบ/ตรวจรับ/Summary/% data syncs to the whole team (previously only the 35-col subset synced, so on any device WITHOUT the local snapshot งวดงาน showed empty + duplicates appeared — POG progress still worked because POG cols were in the 35). **⚠️ Code.gs must be manually re-pasted into Google Apps Script + redeployed, then re-upload the Excel once** to populate the new sheet columns. Upload-merge ([pc_engine] `parseProjectControl` Phase 3) **rebuilds each project row entirely from the latest file** (only `id` preserved) → re-uploading with งวด 2 added reflects the latest; blank-in-file ⇒ field cleared. Finance Master (`pcfin.*`) is separate and never overwritten.
  - **Forecast is split into 2 งวด** (`PCU.splitForecast`): row gets `fc1Amount/fc1Date` + `fc2Amount/fc2Date` (+ `forecastReceive` total, `forecastDate` earliest). **Single-installment projects → งวด 1 = 0, งวด 2 = 100%**; multi-งวด → งวด 1 = first installment, งวด 2 = งวด 2+ folded. `row.forecastLines = [{no,amount,date}]` drives `PCU.cashflowByMonth` (buckets **per งวด-line**, not per project, since งวด 1/2 land in different months) — each month carries `lines[]` for the **cashflow drill-down modal** (click a bar). `PCU.forecastYears` also reads `forecastLines`.
  - **Advanced filter** (`PcAdvancedFilter` modal + `pcApplyAdvanced`, fields from `pcAdvFields()`): multi-condition AND/OR over assignee/status/sub-status/fy/region/province/type/LG bank/amounts/progress + per-งวด booleans (`d1/a1/p1`, `d2/a2/p2` = delivered/accepted/paid งวด 1·2, exposed on the row). **These flags follow the `splitForecast` convention**: single-installment projects map their sole งวด to **งวด 2** (`d2/a2/p2`), leaving `d1/a1/p1`=false — so the filter "ยังไม่รับเงินงวด 1" no longer falsely matches single-งวด projects that were already paid (`p2` carries the truth). Applied inside `topRows` so KPI/funnel/cashflow/grid all reflect it. Has a one-click preset for the canonical "assignee LIT · ส่งงวด 1 · ได้ใบตรวจรับ · ยังไม่รับเงิน" query.
  - **Investor-grade reports** — `PcExportModal` (in `PcGridToolbar`) is a **3-step modal** (เนื้อหา × รูปแบบไฟล์ × **เลือกคอลัมน์**), replacing the old dropdown. **เนื้อหา**: สรุป / รายละเอียด / **สรุป+รายละเอียด** (`content` ∈ `'summary'|'detail'|'both'`). **รูปแบบไฟล์**: **PDF** (`PCU.openReport`) / **Excel .xlsx** (`PCU.exportXLSX`) / **CSV** (`PCU.exportCSV`). All use the current grid-filtered+sorted rows + `scopeLabel`.
    - **Column picker** (step 3, shown when content≠summary or format=csv): user picks which columns to export **from ALL engineer columns (the raw 120, not just what's shown on the grid)** + computed columns, **grouped by the finance team's 5 categories**. `PCU.buildExportColumns(rows)` returns col defs `{key,label,type,group,get}` — group `'คำนวณ'` (14 derived) + raw cols grouped by `pcColCat(key)` (one of `PC_CAT_ORDER` = พื้นฐาน/เสาเข็ม/ความคืบหน้า/ส่งมอบ-รับเงิน/อื่นๆ). `pcColType(key)` tags money/date/pct/text → formatting. `PC_EXPORT_DEFAULT` = analytics + every spec col with `exp>0`. Picker has เลือกทั้งหมด/ค่าเริ่มต้น/ล้าง + search + per-group select-all. Selected defs → `cols` arg to all three exporters; value via `col.get(row)`, formatted by `pcCellText` / `pcCellXl`.
  - **`PC_COL_SPEC` is the single source of truth for columns** (`app/pc_engine.jsx`, from the finance team's `โครงสร้างไฟล์.xlsx`): each `{key, cat, web, exp}` — `cat` = one of the 5 categories, `web`/`exp` ∈ {0,1,2} (1 = must show, 2 = show if available, 0 = hidden). Drives **(a)** export picker grouping/defaults, **(b)** grid columns, **(c)** advanced filter fields. `PC_ENG_COL_ORDER`/`PC_SPEC_BY_KEY`/`pcColCat` derive from it; exported on `PCU`.
    - **Grid** (`PCGrid.makeColumns`) = the existing derived columns **+ a raw column per `PC_COL_SPEC` entry** (skipping keys that have a derived equivalent via `DERIVED_FOR_RAW`: Contract No./พื้นที่/Type/Start/Finish/Region/Province/มูลค่าสัญญาVAT). Raw col: `id='raw:<key>'`, `kind` = pcColType, grid `type` num/date/text, `value` returns numeric (money/pct) / ISO (date) / string so sort+filter work; `cat` set for the column menu. `PcCellRender` default case formats by `col.kind` (฿ / % / fmtDate). `DEFAULT_VISIBLE` = core identity+analytics + every `web>0` column (mapped to derived where one exists) → ~32 visible of ~130. **Column show/hide menu is grouped by category + search.** Grid state key bumped `wtp-pc-gridstate-v2`→**`v3`** (new default columns).
    - **Advanced filter** (`pcAdvFields`) appends `pcAdvRawFields()` — every `web>0||exp>0` raw column as a filter field (money/pct→num, date→**date** with ตั้งแต่/ก่อน/ตรงวันที่ ops + `<input type=date>`, else enum). `pcApplyAdvanced` gained a `date` branch.
  - **All dates display as `DD/MM/YYYY` CE** (`PCU.fmtDate` short). `PCU.isoOf` robustly parses the engineer file's **mixed** date formats → ISO: `Date` object, `22/May/26`, ISO, and `n/n/n` with 2- or 4-digit year. D/M vs M/D disambiguation: a part >12 is the day; if ambiguous, **4-digit year ⇒ D/M/Y** (Thai-typed text), **2-digit year ⇒ M/D/Y** (Excel US auto-format, e.g. `8/30/24`→30/08/2024, `2/11/26`→11/02/2026). `parseProjectControl` normalises **all** `pcColType==='date'` columns via `isoOf` on import (`DATE_COLS` derived from `PC_COL_SPEC`), and grid raw date columns return ISO from `value` so sort/filter/display are all consistent.
    - `PCU.openReport(kind, rows, scopeLabel, cols)` kind `'summary'|'detail'|'both'` — styled HTML window (brand-blue, **Water POG logo** via `new URL('waterpog_Logo-02.png', location.href)`, print-color-exact); `'both'` concatenates summary + `page-break-before` + detail. `buildDetailBody()` renders the `cols` table (money right/฿, pct·date center, ค้างรับ amber). Summary = KPI tiles + pipeline + monthly cashflow per year + LG.
    - `PCU.exportXLSX(kind, rows, scopeLabel, cols)` — xlsx-js-style styled workbook: `'summary'`→sheet **สรุป**, `'detail'`→sheet **รายละเอียด** (built from `cols`: autofilter + freeze panes, `#,##0` money, red ค้างรับ), `'both'`→both. PC-prefixed helpers `pcXlSet/pcXlRow/pcXlFmt` + `PCXL` style map.
  - **Progress % source = physical construction (POG), NOT installments.** `PCU.pogProgress(p)` = `% (POG+STANK)` + `% (POG DRINK)` (the two Excel columns are mutually exclusive — only one is ever filled per project, so summing = whichever is set). `deriveProgress` uses this as **primary** (after cancelled/awaiting→0); falls back to installment-weighted (ส่งมอบ=75%·ตรวจรับ=90%·รับเงิน=100%) only when **no** POG value, then received/contract, then 20. `row.progressDetail = {source:'pog'|'installment'|'received', pog, pogStank, pogDrink, total, delivered, accepted, paid}`; drawer Overview note explains the source (e.g. "งานหอถัง+ระบบ (POG+STANK) = 100%"). Fixed projects (e.g. ENC165) that had only POG + dates but no งวด columns showing a stuck 20%.
  - **Upload Excel** (`PCU.parseProjectControl`) reuses the Migration merge logic (Contract No → name+FY → synthetic `XL-`/`WS-`, preserves ids, keeps projects absent from the file) → saves a **local snapshot** `wtp-proj-control-v2` (full 120 cols, survives sync) used as the project base, and `setData` pushes to the Sheet. It also returns a **`diff`** (`{added, cancelled, missing, signed}` — newly-cancelled, new contract no., real codes absent from the new file, and WS-→real-code by name) shown in the **`PcUploadDiff` modal** after upload. **Finance Master** (LG / debt / assignee / creditTerm / remark — fields engineers don't own) is stored **separately** so Excel upload never overwrites it: one JSON-string override per project, key `pcfin.<ContractNo>` in `data.manualOverrides` (team-shared via sync) + localStorage `wtp-pc-finance-v1` cache. Uses **`WTPOverride.setRaw(key, value)`** — a new variant of `set` that does NOT coerce to Number (so string/JSON values sync). Grid state persists in `wtp-pc-gridstate-v2` (bumped from v1 when default columns changed: Start/Finish moved near front, fc1/fc2 งวด columns added).
- **`staging/`** — one-off `.tsv` import data + PowerShell build scripts (not part of the running app).

## Data model & overrides
- Entity tables: `bankAccounts`, `payables` (AP), `pvVouchers` (PV), `forecastEntries`, `invoices` (IV), `receipts`, `checks`, `debtMaster`, `debtLedger`, `bankEntries`, `bankTransfers`, `cashflowSnapshots`, `projects`, `users`.
- **`WTPOverride`** — a synced key→value KV store (`data.manualOverrides`) for UI tweaks that must NOT mutate backend entity tables: cashflow edits (`cf.*`), per-PV category (`cf.pvCat.<PL_PV_No>`), IV plan-lock (`*.ivPlan.*`), Project Control Finance Master (`pcfin.<ContractNo>` = JSON string). API: `resolve(key, fallback)`, `set` (coerces to Number), **`setRaw`** (keeps string/JSON — used by `pcfin.*`), `setMany`, `clear`, `has`, `_load()` (returns merged cloud+local map). (`cf.paidAp.*` manual-paid flags are **deprecated** — removed from cashflow when AP planning moved to Bank Diary.)
- **Roles**: `viewer`/`owner` = read-only (`_wtpRoleIsReadOnly()` true, `WTPAuth.can('canEdit')` false); `manager`/`staff` can edit. Gate all edit UI behind `canEdit`.

## Conventions & gotchas
- **Cache-bust `?v=`** on every edit (see above) — easiest thing to forget, breaks deploys silently.
- Sync is **last-write-wins per entity** across users; many guards stop accidental mass-delete/clobber. Never push a stale-low or emptied table. Clearing a field over sync requires it in `CLEARABLE_FIELDS` / `mergeRowKeepSheetForEmpty` (else it bounces back).
- Some date fields are **DD/MM/YYYY (Thai)** — normalize to ISO before `new Date()` or months flip.
- Cashflow categories (หมวด 1-4): `categorizePayable(ap)` / `categorizeForecastEntry(fe)`, plus vendor→category map `cfVendorCat` (localStorage `wtp-cf-vendor-cat`, e.g. ลีซ อิท → 3) and per-PV override `cf.pvCat.<PL_PV_No>`.
- **Cashflow = Forecast vs Actual, ไม่ยุ่งกับ AP (since 2026-06-11).** Outflow **Forecast** (`forecastByWeekCat`) = **`forecastEntries` only** (manual, ตั้งจากหน้า Forecast: non-LOAN, outflow `AMOUNT<0`, STATUS≠CANCELED) — AP-by-due-date is **no longer** pulled into cashflow (fixes the AP+forecast double-count). Also the cashflow `forecastEntries` var is **filtered to drop `EXPENSE_TYPE='AP'`** — those are forecast rows Bank Diary's `BDApPanel` writes when you plan an AP payment; they'd otherwise double-count against the manual lump estimates. **Actual** (`pvActualByWeekCat`) = `pvVouchers` by `Pmt_Date` (category via `resolvePvCategory`, NOT default-cat-1) + `forecastEntries` STATUS∈{ACTUAL,BOOKED}. The old flex-pool ("จ่ายตามสภาพคล่อง") + manual-paid (`cf.paidAp`) machinery was **removed** from cashflow; AP planning (ครบดิว/เลือกจ่าย/เลื่อนจ่าย) lives in **Bank Diary** (`BDApPanel`). KPI / Plan-table / weekly-tracking all derive from the **same** `forecastByWeekCat` grid so top ↔ bottom numbers match. Both Forecast and Actual weekly cells are click-drillable (`openForecastDrill` / `openActualDrill`). **Weekly tracking card (section 02) per-week progress bar:** the `%` label **rides the bar's leading edge** (`left: min(100,pct)%`, white-inside when fill ≥40% else dark-outside-right) and caps at the right end when ≥100%; each card's footer shows a small soft pill **`▲ เกินแผน ฿X`** (red, actual>plan) / **`▼ เหลือ ฿X`** (green, actual<plan) tucked under "Total Paid :" — `diff = planTotal − actualTotal`.
- Cashflow IV forecast excludes already-received IVs (`ivIsPaid`); a day-1 "plan lock" freezes the monthly baseline so PLAN doesn't shrink as IVs get paid.
- **AP paid via PV ⇒ drop its forecast (Bank Diary only now).** An AP whose number matches a `pvVouchers.AP_No` is "paid". **Bank Diary** uses a global `paidApSet` (built from `pvVouchers.AP_No`) applied in **both** the account card (`bdBuildAccountView`) **and** the forecast panel (`BDForecastPanel`) — filters out any forecast (ประมาณการ) whose `REF_DOC ∈ paidApSet` so the real PV row represents the outflow (else the plan lingers / double-counts vs the already-reduced BALANCE). Both spots must stay in sync when touching forecast logic. (Cashflow no longer pulls AP, so its `buildPaidVchnoSet`/`isApPaid` were removed.)
- **AP amount = net (`netpayment`).** Bank Diary `bdNormAP` and the AP panel show `netpayment` (ยอดสุทธิ), falling back `Amount → net_new → Balance_Amount1` — matches Cashflow's `Number(ap.netpayment || ap.Amount)`. Don't show `Balance_Amount1` (ยอดคงเหลือ) as the AP amount.
- **Sortable+filterable tables**: reuse the shared `FilterableColHeader` + `ColFilterDropdown` (in `components.jsx`) with parent-owned `colFilters`/`openCol` state and a `getValue(row, colKey)` (see [[colfilter-getvalue-convention]] — must be `(row, key)`). Used by `page_invoices` and the Bank Diary AP panel. For date/number columns also pass the optional **`getSortValue(row, colKey)`** so the filter dropdown lists values in real order (return ISO `YYYY-MM-DD` for dates, the raw `number` for amounts) instead of string-sorting the formatted display (DD/MM/YYYY / `1,000` sort wrong). The AP panel uses `tableLayout:fixed` + fixed col widths (vendor flexes) with `…` ellipsis on long text cells. Bank Diary account-card footer shows **net ending balance** (`base + visIn − visOut` over the shown period), not lowest-balance; overdue-checks modal rows expand to a detail panel; ReconcilePanel is column-sortable.
- **Bank Diary card = period-scoped; everything respects the selected period (`periodEnd`), never a fixed 7-day window.** Shortage alert `isShort = shortInPeriod` (red border/badge/"ต้องเติมเงิน" strip + the `shortAccounts` KPI) — NOT `shortNear`; badge reads "ไม่พอในช่วง `periodLabel`". The "📆 ภายใน 7 วัน" chip only shows when `next7 <= periodEnd` (hidden for sub-7-day views like "สัปดาห์นี้"). When changing alerts, keep them period-based or they "leak" next-week items into a this-week view.
- **Save card as PNG** (`BankAccountCard` 📷 button): `html2canvas-pro` (already loaded in `index.html`, global `window.html2canvas`) on `cardRef`; `ignoreElements` skips `[data-no-capture="1"]` (the button itself); output is **cropped to the `[data-capture-end="1"]` footer bottom** + a small pad and accent finish line (red if `netEnding<0`, green if ≥0) so it doesn't look torn. Same html2canvas pattern as `page_daily`/`page_invoices`/`page_pnl`.
- **Transfer-item label is direction-aware**, computed from the leg's own direction — NOT `bankEntry.description` (the stored note can be the wrong-side "โอนเงินไป …" on both legs). `bdBuildAccountView` takes a `transferInfoByRef` (transferRef → {fromNo/fromBank, toNo/toBank}, built from `transferPairs` + `accounts`); inflow leg → "รับโอนจาก «from»", outflow → "โอนเงินไป «to»". A genuine user note (description not starting with "โอนเงินไป"/"รับโอนจาก") is kept as the item `remark`.
- **Bank Reconciliation page** (`app/page_bank_recon.jsx`, route `#bank_recon`, hidden from `owner`/`viewer` like other bank screens) — กระทบ PV ในระบบ กับ statement จริง ราย **บัญชี** ราย **เดือน**. Imports **CSV + Excel** (KTB) via the already-loaded global `XLSX`; a **column-mapping modal** (`BRMappingModal`) maps date / amount-or-(debit+credit) / desc / ref / balance per **bank brand** (auto-detect `brBrandKey`, saved per brand in `localStorage wtp-bankrecon-map-v1`) → works for any bank, no hardcoded parser. Statement lines + reconcile state are **localStorage v1** (`wtp-bankrecon-stmt-v1` / `-state-v1`, via `BankReconStore`), **not** synced — re-importable working data; the data-loss-prone Sheets sync stays untouched. `brReconcile` matches statement outflows ↔ PV by amount (±0.01) + date (±3d) + cheque/ref, with a subset-sum pass for 1-line-=-many-PV; **3 buckets**: matched / **missing (ขาดบันทึก)** / unmatched-PV. **Feedback loop:** "บันทึกจ่ายจริง" on a missing line writes a `forecastEntries` row tagged **`EXPENSE_TYPE='BANK_RECON'`** + `STATUS='ACTUAL'` (+ `ACTUAL_AMOUNT`/`ACTUAL_DATE`/`CATEGORY`/`Bank_AC`) → appears as **Actual on Cashflow** (and "จ่ายจริงแล้ว" in Bank Diary) with zero new plumbing. **⚠️ These rows are ACTUAL-only — they were never a plan, so cashflow's ประมาณการ (forecast) computations MUST exclude them or the forecast double-counts** (a recon-recorded payment would show in both Forecast and Actual). `page_cashflow.jsx` skips `EXPENSE_TYPE==='BANK_RECON'` in **both** `forecastByWeekCat` and the next-month rollover forecast loop (the `AMOUNT` field is kept negative so the sign stays right in Bank Diary's `bdNormForecast`); `pvActualByWeekCat` is left untouched so they still count as Actual. Fix is at the compute layer, so it retroactively de-double-counts the 15+ recon rows already in the sheet — no re-recording needed. **The drill-downs must mirror this:** `openForecastDrill` ALSO skips `BANK_RECON` (else the click-through item list exceeds the corrected cell total), and `openActualDrill` labels recon rows **`source:'STM'`** (badge reads "STM" + note "กระทบยอด (STM)") instead of "Forecast" — they're real payments, not a plan. The `PlanVsActualCard` lock badge (`🔒 ล็อกแผน …` + ล็อกใหม่ button) was moved to the **card footer** (below the progress bar, with a `borderTop` divider) instead of right under the header. (2026-06-14) Reuses `bd*` globals from `page_bank_diary.jsx` (`bdAcct`, `bdNormPV`, `bdAcctMatchesCheck`, `bdLast4`, `bdBrand`). `brMonthlyView` sorts by date + **orientation-aware** file index (handles newest-first files like BBL) so running-balance/opening/closing stay correct. **Real-file specifics (presets in `BR_PRESETS`, from actual บัญชี exports):** KBANK/BBL CSVs are **TIS-620/CP874** (not UTF-8) → `brDecodeText` (UTF-8 strict, fallback `windows-874`); Thai-month dates ("01-มิ.ย.-2569") via `BR_THAI_MONTH`; header row auto-found by `brGuessHeaderRow` (row with most header-like cells — `blankrows:false` drops blank rows so a fixed index shifts); **SCB** file holds *multiple accounts* → `acctCol` filters to the selected account, and its **Note column = the PV number** → `brReconcile` **pass 0** matches by PV-ref (strongest signal, handles 1-PV-many-legs). **KTB** exports an **encrypted .xls** (มีรหัสผ่าน) — `brParseFile(file, password)` ส่ง `password` ให้ `XLSX.read` (SheetJS 0.18.5 ถอด RC4/Standard ได้). เมื่อ `XLSX.read` โยน error `password/encrypt/protect` → เด้ง **`BRPasswordModal`** ให้ใส่รหัส → `parseAndPreview(file, pwd)` อีกครั้ง (รหัสผิด = วนถามใหม่ · ถอดไม่ได้/รูปแบบไม่รองรับ = โชว์คำแนะนำ Save As `.xlsx/.csv` ไม่ใส่รหัสเป็น fallback). รหัสใช้ในเครื่องเท่านั้น ไม่เก็บ/ไม่ขึ้น cloud.

## 2026-06-12 batch (17 fixes)
- **Global-scope name collisions are real — `grep "function <name>"` before adding/editing a shared component.** Found `StatusPill` defined in BOTH `components.jsx` (the real one: a `<select className="badge ...">` taking `value`/`options`/`onChange`, used by `page_invoices` status column) AND `page_projects.jsx` (a `<span>` dot taking `{status}`). Since `page_projects.jsx` loads **after** `components.jsx`, its version silently clobbered the global → the IV-outstanding status cell rendered a **text-less gray dot**. Fixed by renaming the page_projects one → `PjStatusPill`. The components `StatusPill` also got `display:inline-block` (the `.badge` `display:inline-flex` hides a `<select>`'s selected text in Chrome). See [[global-scope-name-collision]].
- **Dates are CE (ค.ศ.) everywhere.** `fmtDate` = `DD/MM/YYYY` CE (already). Display labels that added `+543` (Buddhist year) were converted to CE across cashflow header, `page_receipts` month groups, `page_interest_calc` `monthLabel`/`quarterLabel`, `brFmtMonth` (recon), `pc_engine.fmtDate` (now `DD/MM/YYYY` CE, was `DD/MM/YY` BE) + its report year labels, and Project Control year dropdown/drill. **Do NOT touch `+543`/`-543` that are input *parsing* (พ.ศ.→ค.ศ.) or the FY identifier (`pc_engine.deriveFy` `+543-2500`) — those are not date displays.** Thai month *names* are kept (e.g. "มิถุนายน 2026").
- **Never auto-delete rows from a synced entity table on load** — it trips the data_sync row-level mass-delete guard → resync loop → renderer hangs (hit it removing "ตัด PV แล้ว" forecasts, 148→39). Use a **display filter** instead: `DataCrudPage` now supports `config.hideRow(row, data)` (hide-only, no mutation). `ForecastEntriesPage` hides EXPENSE_TYPE='AP' rows whose `REF_DOC ∈ pvVouchers.AP_No` AND `∉ payables.vchno`. See [[no-autodelete-synced-rows]].
- **Present Mode** now also covers `pnl` + `projects` (added to `isPresentation` in `Topbar`; both page roots get `present-page`). War Room pages (`warroom1/2`) get class `wr-page` + a present-only responsive `zoom` (1.08→1.32 by width) so their many inline-`px` font sizes scale up like Weekly Forecast.
- **P&L** `ต้นทุนงานก่อสร้าง` = COGS + Cost of service + **Commission** (`PL_compute`: `constructCost` now includes commission, `totalCost = constructCost`; the row list moved Commission above the subtotal and dropped the duplicate total row).
- **Bank Daily card** skips PV rows with amount 0 (deposit write-offs). **Bank Reconciliation** pass-0 (explicit-PV-ref match) now uses a **system-wide PV pool** (`opts.refPool` = all `pvVouchers` with a `PL_PV_No`, any account/month) so a statement line citing a PV number isn't flagged "ขาดบันทึก" just because the PV booked in a different month/account.
- **Bank logos** (`HpBankLogo`, global from `page_home.jsx`) added to Bank Daily card, Bank Recon header, `page_daily_balance` (both tables), and DATA BANK grid.
- **`page_checks`**: row edit/delete buttons removed; delete moved into the view popup. **`page_audit_log`**: clickable column sort + entity tag on `update` rows + full (wrapping) summary. **The action filter tabs + KPIs are now derived from the *actual* `action` vocabulary the backend writes — NOT the seed assumption of add/update/delete (those values don't exist in the sheet).** Real values: `applyDiff` (CRUD row-writes → label "แก้ไขข้อมูล"), `replaceAll` (full sync → "Sync"), `budgetImportMonth`/`plImportMonth` ("นำเข้า Budget"/"นำเข้า P&L"). Tabs are built dynamically from `actionOptions` (distinct actions in data, sorted by count, each with a count badge) so every tab matches real rows; `AL_ACTION_META`/`AL_ACTION_LABEL` map keys→Thai label+color (with add/update/delete kept only as legacy fallbacks). KPI tiles = บันทึกทั้งหมด / แก้ไขข้อมูล(`applyDiff`) / นำเข้า(`budget+pl`) / Sync(`replaceAll`). (The old hardcoded เพิ่ม/แก้ไข/ลบ tabs matched nothing → looked broken.) **`page_users`**: column sort + new separate **`department`** (หน่วยงาน) field/column (synced like `notifyDailyBalance`). **DATA เจ้าหนี้คงค้าง** footer: `colSpan` 6→7 so the total sits under Net Payment and the stripe spans the full row.
- **Debt Ledger** (`#debt_ledger`): maturity-alert banner for Active contracts within 60 days of `maturityDate` (click → open contract).
- **STS Workflow** (`page_sts_workflow`): A4 PDF report (`buildStsReportHTML`/`openStsReport`) — print one (drawer button) or many (per-row checkboxes + toolbar "ปริ้น PDF (n)"); plus a **"ยกเลิกการตรวจ"** button (deletes the project's `stsCalcResult` rows → back to รอ review).
- **Login → always `#home`** (`handleLogin` forces hash+route). **Splash loading bar** (`index.html`) uses `linear` timing (was `ease-in-out`) so it runs continuously without a stutter.

## 2026-06-13 batch (round 2 — 11 fixes)
- **Present Mode spotlight now also covers hero/KPI blocks that are plain inline-styled `<div>`s** (not `.card`/`.kpi`): the hover-lift + brand-ring rule gained `.hero-pill` (War Room รายปี), `.pnl-hero` + `.pnl-kpi-card` (P&L — classes added in `page_pnl.jsx`). Present-mode table spotlight was broadened from `.tbl tbody tr` → **any `table` in `.present-page`** (so `.pnl-report`/`.pnl-budget`/`.pnl-det-tbl` highlight too) and a **cell-level** `td:hover` highlight was added (เห็นชัดว่าชี้เซลล์ไหนในแถว). **Budget Control Center** added to present mode (`budget` in `isPresentation`, `present-page` on `.bcc-page`).
- **War Room present-mode font sizing** — its text is all inline-`px`, so it's scaled with `zoom` on `.wr-page` (present only). Calibrated to **1.3→1.6** (was 1.08→1.32) because Weekly Forecast cells measure ~16px vs War Room ~12px (×1.3). Don't measure `zoom` via `getComputedStyle().zoom` (Chrome returns "1"); measure `getBoundingClientRect()` ratio instead.
- **Home “เงินสดคาดการณ์รายสัปดาห์”** (`hpWeekRange`, `weekProj.weeks[]`): week labels are now **real date ranges** (`DD/MM–DD/MM` from today, not static W1/W2); each bar is a **button → drill `Modal`** showing ยอดยกมา + the contributing `forecastEntries` (PLANNED, by PAYMENT_DATE) + net + closing — clarifies the data source (cash0 = Σ bank balances).
- **Splash loading bar** → Material-style **two-bar indeterminate** (`indet1`/`indet2` in `index.html`) — overlapping bars mean one is always moving, no wrap-gap stutter (the single sliding pill still hitched at the loop point).
- **ประมาณการรายจ่าย: real delete, not hide.** Removed the `hideRow` filter; the paid-via-PV + gone-from-payables AP rows now show with a red banner + **“ลบถาวร (N)”** button (`purgePaidAp`). Deletes in **batches ≤25% of the table per click** to stay under the data_sync mass-delete guard (massDel `> max(8, 50% base)` / wedge `< 70% base`) — one click in prod (few rows), repeat-until-clear in seed (over-matches). `DataCrudPage` gained a `config.banner` slot + unique row keys (`id_${index}`) so a duplicate seed id can't drop a row.
- **Bank Reconciliation account picker** = **bank buttons** (logo + brand + last4, one click) instead of a `<select>` (`brBrandChip` now unused).
- **ภาระหนี้ทั้งหมด** (`page_debt`): one **combined NON-BANK card** (`DebtGroupCard` got `subGroups`+`nested` props) — expand it to reveal 2 nested sub-groups “สินเชื่อโอนสิทธิ์” (`isAssignmentDebtCat`: LIT/ลีซอิท, STS, WCI, *Project*, FS; Non-WCI excluded) + “นักลงทุน” (the rest), each itself expandable to its category mini-cards. (Not 3 separate top-level cards — keep it tidy: one place, expand what you want.)
- **Debt Ledger popup** (`page_debt_ledger` drawer): split into **2 tabs** (`drawerTab` state, default `'interest'`) — 💵 คืนเงินกู้ (เงินต้น) = action bar + รายการรับ/คืนเงินกู้ table · 📈 ดอกเบี้ย = filter tabs + schedule + auto-compare. Status/lineage blocks stay above the tabs.

## 2026-06-13 batch (round 3 — 7 fixes)
- **`YmdPicker` (global, `components.jsx`)** — cascade ปี→เดือน→วัน (3 `<select>`, ค.ศ.) แทน `<input type="date">` ในตัว **กรอง** วันที่. `value`=ISO|'' · `onChange(iso)` (ปี+เดือนพอ → emit, วัน default 1; ไม่มีปี/เดือน → ''). ใช้ที่: Bank Diary AP "กรองครบกำหนด" (from/to), Project Control advanced-filter + date-range, Daily Balance วันที่บันทึก. **ฟอร์มกรอกข้อมูล (invoice/maturity ฯลฯ) ยังเป็น `<input type=date>` ตามเดิม** (นี่คือ "filter" เท่านั้น).
- **`ErrorBoundary` (global, `components.jsx`) wraps the page** in `app.jsx` (`<ErrorBoundary key={route}>{page}</ErrorBoundary>`) — render error ของหน้าไหนโชว์ fallback (⚠️ + ลองอีกครั้ง/รีโหลด) แทน "จอขาวหายทั้งหน้า". `key={route}` → reset เมื่อเปลี่ยนหน้า.
- **Bank Reconciliation: สำรอง/กู้คืน** (💾 สำรอง / ↩️ กู้คืน บน header) — export/import JSON ของ `BankReconStore` (lines+state+mapping) กันข้อมูล localStorage หาย/ย้ายเครื่อง. (แนวทางถาวรยังเป็น Supabase ตาม [[data-backend-migration]].)
- **Weekly Forecast `PlanVsActualCard`** ได้ `overflow:hidden` → แถบ `.kpi-accent` ซ้ายโค้งตามมุมการ์ดในโหมดปกติ (เหมือนโหมดนำเสนอ).
- **ภาระหนี้: กล่อง BANK/NON-BANK กางแล้ว** เปลี่ยนจากการ์ดรายหมวดเรียงเต็ม (ละลานตา) → **ลิสต์ตารางกระชับ** (จุดสี + ชื่อหมวด · n/ทั้งหมด สัญญา · คงเหลือ). `DebtCategoryMiniCard` เลิกใช้แล้ว.
- **Debt Ledger popup tabs** = segmented control เด่นชัด (พื้น `--ink-100`, แท็บ active เป็น gradient brand + เงา) แทน `.tabnav` จืดๆ.
- **ประวัติรับเงิน** เพิ่มตัวกรอง **ปี** (`filterYear` + `yearNos`) คู่กับเดือน.
- **Bank Daily AP** แถวที่วางแผนแล้วโชว์ **📅 จ่าย DD/MM/YYYY** (`plannedDateByRef` = map AP→PAYMENT_DATE ของ forecast ที่ผูก REF_DOC).

## 2026-06-13 — Bank Reconciliation now syncs to dedicated Sheet tabs
- **กระทบยอดธนาคารเลิกเป็น localStorage-only แล้ว** — เพิ่ม 2 entity tables ใหม่ที่ sync ผ่านระบบเดิม: **`bankReconLines`** (statement lines แบบ flat: `id,accountNo,ym,date,amount,desc,ref,balance,idx`) + **`bankReconState`** (`id`=lineId, `decision`, `forecastId`). ทีมเห็นร่วม + รอด browser clear.
- **⚠️ ต้อง re-paste `Code.gs` (หรือ `Code.standalone.gs` ตัวที่ deploy) ลง Apps Script + redeploy** — เพิ่มแล้วใน: `SHEETS`, `getEntity`, `JSON_FIELDS`, `ENTITY_HEADERS`, `_entitySheet` map, `initEmpty` writeTable, `SERVER_VERSION='20260613a-bankrecon'`. **ก่อน redeploy: recon เขียน push จะ error (`CRUD ไม่รองรับ entity`) แต่หน้ายังทำงาน local ได้ (localStorage cache)** — degrade graceful. แท็บถูกสร้างอัตโนมัติตอน write ครั้งแรก (applyDiff `insertSheet`).
- **Client wiring**: `data_sync.js` (CRUD_ENTITIES + sheetOrder ต่อท้าย + read/assemble), `data.js` (CRUD_KEYS + seed `[]`), `page_bank_recon.jsx` (`brLinesToRows`/`brRowsToLines`/`brStateToRows`/`brRowsToState` + hydration effect cloud→local + `pushReconLines`/`pushReconState` ทุก write). localStorage `BankReconStore` ยังเป็น cache/offline.
- **gviz-อ่านแท็บผิดเป็นขยะ (gotcha):** ขอ gviz `?sheet=bankReconLines` ตอนแท็บยังไม่ถูกสร้าง → Google คืน "ชีตแรก" มาเป็นขยะ (เช่น META KV). hydration จึง **กรองเฉพาะแถวที่มี `accountNo`+`ym`** (และ state ต้องมี `id`+`decision`) ก่อน — ขยะถูกทิ้ง ไม่ทับ/ล้าง cache. หลังแท็บถูกสร้าง gviz คืนค่าถูกต้อง.

## 2026-06-13 — Bank Recon: "โอนระหว่างบัญชี" bucket
- รายการ statement ที่เป็น **โอนเงินระหว่างบัญชีตัวเอง** (ไม่มี PV เพราะไม่ใช่รายจ่าย) เคยตกถัง "ขาดบันทึก" และถูกบังคับให้ "บันทึกจ่ายจริง". เพิ่ม **ถัง/แท็บ "โอนระหว่างบัญชี"** + ปุ่ม **🔁 โอนระหว่างบัญชี** ในถังขาดบันทึก → set `reconState[lineId].decision='transfer'` (sync ผ่าน `bankReconState`) → ออกจากถังขาดบันทึก **โดยไม่สร้าง forecastEntries ACTUAL**. กดยกเลิกในแท็บโอนฯ ได้ (กลับเป็นขาดบันทึก). 
- **Auto-hint**: `isLikelyTransfer(line)` = desc มี "โอน/transfer" + (ชื่อบริษัทตัวเอง `meta.shortName`/วอเทอร์ป๊อก หรือเลขท้ายบัญชีตัวเองในระบบ) → โชว์ป้าย "🔁 น่าจะเป็นโอนระหว่างบัญชี" + ไฮไลต์แถว (ยังต้องกดเอง ไม่ auto-mark). `brReconcile` คืน bucket `transfers` เพิ่ม.

## 2026-06-14 — Sync toasts silenced + polling slowed (perf/UX)
- **ALL sync toasts are now silent** (user found them annoying, popping too often). In `app.jsx` the sync-event effect: `onConfirmed`/`onRecovered` are `() => {}` no-ops; **`onBlocked` now `console.warn`s instead of `pushToast`** (still throttled 12s via `guardThrottled`). **The sync/guard logic itself is unchanged** — saving, reading, and the anti-data-loss guard all still work; only the user-facing popups are gone. If you need to re-enable a sync toast, that's the spot.
- **`AUTO_REFRESH_MS` 45000 → 120000** (`app/config.js`) — background gviz polling was reading ~24 sheet tabs every 45s (the "ทำงานพื้นหลังเยอะ" the user noticed; network capture showed **0 POSTs / 271 GET reads**, i.e. no write loop, just frequent reads). 2-min interval ≈ 60% fewer reads; team data still fresh within 2 min. Adaptive backoff (x2–x4 on 429) + Page-Visibility pause still apply on top.
- **If a user still sees the toast after this, it's stale cache** (only `app.jsx` ever emitted these texts — confirmed by grep) — hard-refresh / wait for GitHub Pages deploy.

## 2026-06-15 — Budget Control Center: new categorized "Budget.xlsx" import
- **New import file replaces the old Mango "Export HeadOfficeExpense".** New layout = single sheet, header row 1: `[ประเภทค่าใช้จ่าย, Dept. Code, Department /Project Name, Acct. Code, Description, Total Budget, รวมใช้จริง, คงเหลือ (งบ-ใช้จริง), 1, 2, 3, … N]`. Key differences from the old file:
  - **col A = ประเภทค่าใช้จ่าย (category) is pre-filled by finance** (14 categories, hand-assigned — NOT derivable from account-code prefix; e.g. prefix `5340` maps to 7 different categories). Used directly; `categoryOf(acct)` is now only a fallback.
  - **`Total Budget` is the ANNUAL budget** (no monthly budget breakdown). Parser spreads it evenly `/12` into `mb[]` (flat monthly budget line).
  - **The numbered columns (1,2,3,…) are monthly ACTUALS**, not budget. They grow one column per month (currently 5). Parser is **header-driven** (`parseBudgetCategorized`): finds columns by label, treats any header that is an integer 1..12 after `Total Budget` as a month → resilient to added months / shifted columns.
  - **Subtotal rows (รวมฝ่าย…/รวมทั้งสิ้น…) have no Acct. Code → skipped** via `/^\d{3,}/` test on the acct cell.
  - `parseWorkbook` tries `parseBudgetCategorized(aoa)` first, falls back to `parseMangoExport(aoa)` (old fixed-position layout kept as fallback).
  - Validated against the real file: total budget = ฿228,400,060 (matches the KPI exactly), 457 GL rows, 20 depts, 14 categories.
- **⚠️ Backend needs a one-time re-paste + redeploy.** `apps_script/Budget.additions.gs` `BCC_HEADERS` + the `clean` map gained a **`cat`** column so the finance category survives the reload (the page does `setTimeout(loadData, 1500)` after upload → without persisting `cat`, the categories would instantly revert to prefix-based). `parseSheetRows` reads `r.cat || categoryOf(acct)`. **Re-paste `Budget.additions.gs` into Apps Script (Code.standalone.gs project) + redeploy (Manage deployments → New version), THEN re-upload Budget.xlsx once** to populate the new `cat` column + refresh actuals. Before redeploy the page still works (degrades to prefix categories on reload).

## 2026-06-15 — Idle auto-logout + "ไม่ล็อกอิน = ไม่ push" (fix audit mis-attribution)
- **อาการ:** audit log โชว์ว่า PATIMA / NANTAWAN "แก้ไขข้อมูล" (`applyDiff invoices: 719 → 719 (edits only)` ทุก ~3 นาที) ทั้งที่ทั้งคู่แค่เปิดเว็บค้างไว้ ไม่ได้แตะอะไร — คนแก้จริงคือ BAIKAO (ล็อกอินบัญชีกลาง เช่น `acc.manager`).
- **ROOT CAUSE (สถาปัตยกรรม, 2 จุดรวมกัน):**
  1. **ทุก browser ที่เปิดค้าง = client ที่เขียนข้อมูลได้เอง.** `WTPData.save` (wrap ใน `data_sync.js`) ตั้ง `syncDiff` push อัตโนมัติทุกครั้งที่ data ในเครื่องต่างจาก `lastSnapshot` — โดยไม่สนว่าใครเป็นต้นเหตุ. เครื่องที่เปิดหน้าใดค้างไว้ poll รับข้อมูลใหม่ → หน้านั้น normalize ฟิลด์ (วันที่/ตัวเลข) ต่างจากชีตนิดหน่อย → push กลับเป็น "diff หลอก" (entity ที่ push ผูกกับหน้าที่เปิดค้าง: nantawan→invoices, patima→cashflowSnapshots).
  2. **ป้ายชื่อ "ใครแก้" = ใครล็อกอินในเครื่องที่ push** อ่านจาก `wtp-session` (`_currentMeta` ใน `data_sync.js`) แล้วเซิร์ฟเวอร์เชื่อดื้อๆ — `Code.standalone.gs` `appendAuditLog_` ใช้ `meta.user || 'unknown'` **ไม่มีการตรวจ identity เลย** (client ส่งชื่ออะไรมาก็บันทึกตามนั้น).
- **FIX (ทำแล้ว build `20260615b`, ฝั่ง client ล้วน — ไม่ต้อง redeploy Apps Script):**
  1. **"ไม่ล็อกอิน = ไม่ push"** — เพิ่ม `_hasValidSession()` (เช็ค `wtp-session` มีจริง + ยังไม่หมดอายุตาม `SESSION_TTL_MS`) แล้ว gate ทั้ง 3 จุดที่ push: `WTPData.save` wrapper, `forceSyncNow`, `beforeunload` flush. เครื่องที่อยู่หน้า LOGIN (ยังไม่ล็อกอิน / โดน auto-logout / session หมดอายุ) จะไม่เขียนชีตเลย — verified: logged-out + บังคับ diff + รอ debounce → 0 POST.
  2. **Auto-logout เมื่อไม่ได้ใช้งานเกิน `IDLE_LOGOUT_MS` (30 นาที, ใน `config.js`)** — effect ใหม่ใน `app.jsx` (deps `[isLoggedIn]`): วัด activity จาก `mousemove/mousedown/keydown/scroll/touchstart/click` (throttle 5s ลง `lastActivityRef`, ไม่ทำ re-render), `setInterval` เช็คทุก 30 วิ + เช็คตอน `visibilitychange` → idle เกินกำหนด **หรือ** session เกิน `SESSION_TTL_MS` (เดิมเช็ค TTL แค่ตอนโหลดหน้า — แท็บค้าง 8 ชม. ไม่เคยหมดอายุ) → `handleLogout()` + toast "ออกจากระบบอัตโนมัติ…" → เด้งหน้า LOGIN. ตั้ง `IDLE_LOGOUT_MS:0` เพื่อปิด.
  3. **Force-logout ทั้งทีม (สั่งได้ + บังคับ re-login ตอน deploy).** เส้นตาย = `forceLogoutThreshold()` ใน `app.jsx` = `max(config.FORCE_LOGOUT_BEFORE, override 'system.forceLogoutBefore')` — **session ใดที่ `time <` ค่านี้ → เด้งออก**. เช็คใน (ก) initial `isLoggedIn` gate (เด้งทันทีตอนโหลดโค้ดใหม่), (ข) `checkIdle` (interval 30 วิ + visibility, reason `'kick'`), (ค) effect `[data.manualOverrides]` (เด้งทันทีที่ poll ดึงค่าใหม่มา ไม่ต้องรอ interval).
     - **`FORCE_LOGOUT_BEFORE` (config, epoch ms)** = เวลา deploy → ทุกคนที่ล็อกอินก่อนหน้า พอ reload เข้าโค้ดใหม่จะถูกเด้ง LOGIN (= "re-login รอบ deploy นี้"). **อัปเดตค่านี้ทุกครั้งที่อยากบังคับทั้งทีม re-login ตอนปล่อยเวอร์ชัน** (ใส่ epoch ปัจจุบัน). build นี้ตั้ง `1781505311512` (2026-06-15).
     - **ปุ่ม "🚪 บังคับออกจากระบบทุกคน" ในหน้า Users** (`page_users.jsx`, manager-only): `WTPOverride.set('system.forceLogoutBefore', Date.now())` → sync ผ่าน `manualOverrides` → เครื่องที่รัน**โค้ดใหม่**เด้งออกภายใน ≤2 นาที (รวมเครื่อง admin เอง). verified: inject session time < threshold → reload → session ถูกลบ + แสดงหน้า LOGIN.
- **⚠️ ข้อจำกัดที่เลี่ยงไม่ได้ (static SPA):** สั่ง logout ไปหา browser ได้**ผ่านโค้ดที่มันรันอยู่เท่านั้น** — แท็บที่เปิด "โค้ดเก่า" ค้างไว้ไม่มี logic พวกนี้ จึง**สั่งทางไกลไม่ได้จนกว่าจะ reload**. ทางปฏิบัติ: deploy → ให้ทีมรีเฟรช 1 ครั้ง (หรือรอ idle 30 นาทีให้ใหม่เด้งเอง).
- **ผลรวม:** เครื่องเปิดค้าง → 30 นาทีไม่มี activity → เด้งออก → หน้าที่ churn ถูก unmount + ไม่มี session → หยุด push ในนามคนอื่นที่ต้นเหตุ. (ยังไม่ได้อุด #3 = หา/แก้ field ที่ normalize แล้ว churn ให้ idempotent — เป็นงานรากที่ทำทีหลังได้.) **ถ้าอยากให้ audit ขึ้นชื่อ BAIKAO ตรงตัว ต้องสร้าง user ของ BAIKAO เองใน `config.js`** (ตอนนี้ใช้บัญชีกลางจึงขึ้นชื่อบัญชีกลาง).
## 2026-06-15 — Presence "ใครออนไลน์อยู่" (build client `20260615c` / server `20260615a-presence`)
- **ทำไมต้องมี entity แยก (ไม่ใช่ `manualOverrides`):** heartbeat ผ่าน `WTPOverride` จะ (1) สร้าง audit row ทุกครั้ง แยกจาก edit จริงไม่ได้ → audit รกอีก (ขัดเป้าหมาย), (2) `WTPOverride.set/setRaw` มี role guard → owner/viewer (read-only) เขียนไม่ได้ จึงไม่ขึ้นรายชื่อ. เลยเพิ่ม entity `presence` เฉพาะ.
- **Server (`Code.standalone.gs` + `Code.gs`, `SERVER_VERSION='20260615a-presence'`):** เพิ่ม `SHEETS.PRESENCE`, getEntity case, `JSON_FIELDS.presence`, `ENTITY_HEADERS.presence = ['id','username','displayName','role','lastSeen']`, `_entitySheet` map. **สำคัญ: `doPost` audit block เพิ่มเงื่อนไข `&& entity !== 'presence'` → presence ไม่ลง auditLog** (กัน log รก). แท็บสร้างอัตโนมัติตอน write ครั้งแรก (applyDiff insertSheet). **⚠️ ต้อง re-paste โค้ดเซิร์ฟเวอร์ที่ deploy (standalone) + redeploy ก่อน presence ถึงทำงาน** — ก่อน redeploy: client เขียน presence จะได้ error `CRUD ไม่รองรับ entity: presence` แต่ `pushPresence` **กลืน error เงียบ** (degrade ไม่พัง).
- **Client (สำคัญ — แยกจาก sync การเงินโดยตั้งใจ):** presence **อ่าน**ผ่าน `sheetOrder` ใน `loadFromServer` (กรอง gviz-garbage ด้วย `r.username`) แต่ **เขียน**ผ่าน `WTPData.pushPresence(row)` = POST เดี่ยว **ไม่ผ่าน `CRUD_ENTITIES`/`syncDiff`** → presence push fail จะ **ไม่ poison** การ push entity การเงินในรอบเดียวกัน (ต่างจาก bankRecon ที่อยู่ใน CRUD_ENTITIES). `pushPresence` ติด guard `_hasValidSession` ด้วย. seed `data.presence=[]` + `CRUD_KEYS` ใน `data.js`.
- **Heartbeat (`app.jsx`, deps `[isLoggedIn]`):** เขียนทุก `PRESENCE_HEARTBEAT_MS` (config, 5 นาที, 0=ปิด) **เฉพาะเมื่อ ล็อกอิน + แท็บ visible + ยัง active** (`Date.now()-lastActivityRef < HB`) → คนเปิดค้างจน idle หยุด heartbeat เอง (และเดี๋ยวก็โดน auto-logout). row `{id:username, username, displayName, role, lastSeen:ms}` — id=username → 1 แถว/user (upsert).
- **View (`page_users.jsx`, manager-only):** การ์ด "🟢 กำลังออนไลน์ (N คน)" — ออนไลน์ = `lastSeen` ภายใน `max(2×HB, 6 นาที)`; โชว์ชื่อ+role+"X นาทีที่แล้ว", เรียงตาม lastSeen, ออฟไลน์จาง. ว่าง = ยังไม่ redeploy. **เพิ่ม `lastSeen` = epoch ms (ปัจจุบัน) ต่อ user; presence แสดง read-only ทุก role รวมผู้บริหาร** (เขียนผ่าน sync ปกติ ไม่ติด WTPOverride role guard).
- **Tradeoff ที่ยอมรับ:** active user เขียน 1 POST ทุก 5 นาที (ไม่ลง audit) — bounded เพราะ idle-logout 30 นาที. ปิดได้ที่ `PRESENCE_HEARTBEAT_MS:0`.

## 2026-06-15 — Bank Recon "บันทึกจ่ายจริง" หายจาก Weekly Forecast (clobber กิน actual) — self-heal + เกราะ server (build client `20260615d`/`page_bank_recon 20260615a` · server `20260615c-actualguard`)
- **อาการ:** ยอดที่กด "บันทึกจ่ายจริง" หน้ากระทบยอด (เขียน `forecastEntries` `EXPENSE_TYPE='BANK_RECON'` `STATUS='ACTUAL'` → ควรเด้งเข้า **Actual** หน้า Weekly Forecast/cashflow) **หายไป**. โค้ดเชื่อม recon→cashflow **ถูกต้อง** (`pvActualByWeekCat` รวม BANK_RECON อยู่แล้ว) — ปัญหาคือแถวถูกลบหายจากชีต `forecastEntries`.
- **ROOT CAUSE = forecastEntries โดน multi-client clobber/wedge (ปัญหาสถาปัตยกรรม sync เดิม [[data-loss-root-cause-audit]]).** ออดิตชีตจริงพบ: `forecastEntries` มี 139 แถว (AP/Manual/LOAN) — **BANK_RECON = 0** ทั้งที่ `bankReconState` มี **13 รายการ** `decision='recorded'` + `forecastId` (id_3c6, id_23d…) ครบ (พิสูจน์ว่าแถวเคยถูกสร้างจริง). auditLog 13–15 มิ.ย. โชว์ forecastEntries แกว่งรุนแรง (`148→55`, `164→39`, `20→139`…). ตอน clobber ก้อนใหญ่ลบทุกแถว แล้วเครื่องที่ "ดันกลับ" มี AP/Manual (มาจาก import เป็นชุด เหมือนกันทุกเครื่อง) **แต่ไม่มี BANK_RECON** (เกิดบนเครื่องที่กดบันทึกเครื่องเดียว) → หายถาวร. ลบทีละ ~13 แถว/150 = ต่ำกว่าเกณฑ์ guard (massDel `>max(8,50%)` / wedge `<70%`) จึงหลุด. **ยอดที่หายรวม ≈ ฿2.83M (13 รายการ ก้อนใหญ่ ฿2,592,000 บัญชี …4863).**
- **FIX 1 — Self-heal (client, `page_bank_recon.jsx`):** effect ใหม่ (deps `[data.forecastEntries, data.bankReconState, data.bankReconLines]`): ถ้า reconState entry ใด `decision='recorded'` + `forecastId` **หายจาก forecastEntries** → สร้างแถวขึ้นใหม่ด้วย **forecastId เดิม** (idempotent) จาก `bankReconLines` (หรือถอด `accountNo|date|amount` จากตัว `line.id`) → เด้งเข้า Actual อีกครั้ง. ทำเฉพาะ `!readOnly`; `brHealRef` กันยิงซ้ำชุดเดิม, `brUndoneRef` กันสร้างแถวที่เพิ่งกด undo กลับ. **กู้ 13 รายการคืนเองเมื่อ editor เปิดหน้า #bank_recon** (CATEGORY ของแถวที่กู้ = null → cashflow fallback หมวด 1/keyword; ยอด/วันที่/บัญชีถูกต้อง).
- **FIX 2 — เกราะ protect-ACTUAL (server, `Code.standalone.gs` **และ** `Code.gs`):** `applyDiff` + `replaceAll` **ไม่ลบแถว `forecastEntries` ที่ `STATUS='ACTUAL'`** ผ่าน diff/base-reconcile **เว้นแต่ `allowShrink=true`** (`protectActual = entity==='forecastEntries' && !opts.allowShrink`). หยุดต้นตอ — รายการจ่ายจริงจะไม่ถูก clobber/wedge ลบเงียบอีก. **⚠️ ต้อง re-paste `Code.standalone.gs` (ตัว deploy) + redeploy** (ping `serverVersion` ต้องขึ้น `20260615c-actualguard`); ก่อน redeploy self-heal ยังกู้ได้แต่กันลบซ้ำยังไม่ทำงาน. ตอนนี้ `Code.gs` (bound) header `forecastEntries` ก็เติม `ACTUAL_AMOUNT/ACTUAL_DATE/REF_DOC/BOOKED_AT/CFS_ACTIVITY` ให้ตรงชีตจริง (เดิม 13 คอลัมน์ ขาด actual — standalone/ชีตจริงมี 18 อยู่แล้ว).
- **FIX 3 — `WTPData.forceDeleteRows(entity, ids)` (`data_sync.js`):** POST `applyDiff` พร้อม `allowShrink:true` (ข้ามเกราะ protect-ACTUAL) + read-your-writes (อัป snapshot/local). ใช้ใน recon `undoRecord` — "ยกเลิกบันทึกจ่ายจริง" เป็นการลบที่ผู้ใช้ตั้งใจ จึงข้ามเกราะได้ (ถ้าใช้ diff ลบปกติจะโดนเกราะกัน เด้งกลับ). **ผลข้างเคียงที่ยอมรับ:** ลบแถว ACTUAL จากหน้า DataCrud ทั่วไป (`page_data_extras`) จะโดนเกราะกัน (rare; อยากลบจริงต้องผ่าน forceDeleteRows). `handleBulkUnplanAP`/`purgePaidAp` ลบเฉพาะ PLANNED/AP อยู่แล้ว ไม่กระทบ.
- **เกราะนี้แคบ + ปลอดภัย:** บล็อกเฉพาะ "delete ของ forecastEntries STATUS=ACTUAL ที่ไม่มี allowShrink" — upsert/แก้ฟิลด์ actual ยังได้, ลบแถว non-actual ยังได้ปกติ.

## 2026-06-15 — Invoice page crash + projects wedge loop (urgent fixes)
- **`page_invoices.jsx` crashed the whole IV page** (`TypeError: raw.trim is not a function` → ErrorBoundary fallback). `normalizeJobNo(raw)` called `raw.trim()` directly; a **numeric** `iv.jobNo` (e.g. `12345`) has no `.trim`. Fixed: `String(raw).trim()` + treat `null`/`''` as empty. Any shared helper doing `.trim()` on synced fields must coerce — synced cells can be numbers.
- **Projects anti-wedge loop on multiple machines** (`anti-wedge: local projects (319) < server (648)` spamming every poll). **Data was SAFE the whole time** — the sheet has 648 *real* rows (638 distinct Contract No., 0 dupes; verified) and the guard correctly kept the server's 648, refusing the stale local 319. Root: some browsers had a stale `wtp-fin-data-v8` cache with `projects=319` (from before the sheet grew to 648) that wouldn't update — `WTPData.load()` kept returning 319, the save-effect re-persisted it, anti-wedge pulled 648 but it never stuck. **Could NOT just bump `wtp-fin-data-v8` / clear localStorage — login reads the cached `users` from that key** (`handleSubmit` in `app.jsx`), so wiping it risks locking out users not in `WTP_CONFIG.USERS` bootstrap. **Fix: a one-time module-level IIFE at the top of `app.jsx`** (flag `wtp-proj-resync-v1`) that empties **only** `cache.projects` (keeps `users` → login safe) + drops the stale `wtp-proj-control-v2` snapshot → forces a clean re-sync of projects from the sheet (648), then sticks. Verified in preview: projects 648, users preserved (3), no wedge spam, no error. (Seed has only 10 projects, so 319 was never seed — it was cached.)

## 2026-06-15 — "ทุกคนข้อมูลไม่ขึ้นหน้าเว็บ" หลัง Clear site data — empty-local wedge (build `20260615e`)
- **อาการ:** ผู้ใช้บางคนกด DevTools → Clear site data → reload → หน้าเว็บว่างเปล่าทุกหน้า (KPI 0, ตารางว่าง). คนที่ไม่กดยังใช้งานปกติ. console สแปม `[WTP Sync] 🛑 row-level: pvVouchers 614→0 ผิดปกติ — ระงับการลบรอบนี้ + ดึงของจริงจากชีตมา resync` ทุก entity (pvVouchers/payables/debtLedger/receipts/invoices/projects/…) ทุก ~4.5 วิ. **ข้อมูลในชีตปลอดภัย** (614/467/3964/… ครบ; ออดิตยืนยัน) — bug เป็น client wedge ล้วน.
- **ROOT CAUSE (2 ชั้นรวมกัน):**
  1. **localStorage write กลืน error เงียบ.** `data.js save()` มี `try { localStorage.setItem(KEY, JSON.stringify(data)); } catch (_) {}` — เมื่อ JSON ใหญ่เกิน quota (รวม pvVouchers 614 + payables 467 + debtLedger 3964 + invoices 712 + projects 648 + … ≈ 5.7MB; เบราเซอร์บางตัวจำกัด 5–10MB ต่อ origin + ส่วน buffer) จะ throw `QuotaExceededError` แล้วถูกกลืน → localStorage **ยังคงว่าง** ทั้งที่ React state เต็ม.
  2. **`applyEntityGuard` ตี "local ว่าง + snap มีของ" = pending edit (เจตนาลบทั้งตาราง)** ใน `data_sync.js:558-597`. เกราะ `corruptLocal` เดิมเช็คเฉพาะ `localRows.length > 0 && < sheetRows.length * 0.5` — กรณี `localRows.length === 0` หลุดเงื่อนไข → ตกไปทาง `hasPending` (`JSON.stringify([]) !== snap-json` = true) → คืน `accepted:false, rows: localRows=[]` → React state ถูก force ว่าง → loop.
- **ลำดับลูป:** Clear site data → reload → `WTPData.load()` คืน `freshState()` (online → ทุก CRUD = []) → `loadFromServer()` สำเร็จ ตั้ง `lastSnapshot[entity] = JSON(full)` + commit React → effect `WTPData.save(full)` → quota exceeded → ส่งเงียบ localStorage ยังว่าง → 3 วิ syncTimer → `syncDiff`: `latest = load() = empty`, base = snap full → diff ลบทุกแถว → push-guard `emptying` บล็อก + `setTimeout(loadFromServer, 1500)` → resync → applyEntityGuard เห็น `localRows=[]` vs `snap=full` → **hasPending=true** → คงค่าว่าง → React state ว่าง → save effect → quota → ... วน.
- **FIX (`build 20260615e`, ฝั่ง client ล้วน, ไม่ต้อง redeploy Apps Script):**
  1. **`data_sync.js applyEntityGuard`** — ขยาย `corruptLocal` ตัด `localRows.length > 0` ทิ้ง (เหลือแค่ `Array.isArray(localRows) && sheetRows.length >= 10 && localRows.length < sheetRows.length * 0.5`) → ครอบ `localRows.length === 0` ด้วย → recover ได้: `accepted:true, recovered:true` → loadFromServer caller ตั้ง `lastSnapshot + data[e] = sheetRows` + commit React → จอเห็นข้อมูล. ปลอดภัย: ฝั่ง push เกราะ `emptying` (`base.length > 0 && ours.length === 0`) ใน `syncDiffRowLevel` ยังกันลบทั้งตารางจริงอยู่ — empty-local + sheet-full เป็น wedge ชัวร์ ไม่ใช่ "ตั้งใจลบทุกแถว" (ผู้ใช้ลบจริงผ่าน UI ทีละแถวอยู่แล้ว).
  2. **`data.js save()`** — wrap จับ exception จริงแทนกลืน: log `console.error` + dispatch `wtpStorageQuotaExceeded` (flag `_quotaWarned` กัน spam, log แค่ครั้งแรก). ผลตามมา: เครื่องที่ quota เต็ม **ข้อมูลจะถูกดึงจากชีตทุก reload, ไม่ persist** (UI ยังใช้งานได้เต็มที่ในรอบเปิด แต่หลัง reload ใหม่ ต้องโหลดใหม่หมด — bounded latency, ไม่เสีย data). คน dev จะรู้ได้จาก console ทันทีเมื่อเจอเครื่องนี้.
- **Verified ใน preview:** clear localStorage + reload → React state 614/545/3998/712/648/686 ครบ + localStorage เขียน 5.69MB สำเร็จ + sidebar นับ 145/26/686/648/712 ตรง + receipts page table render 711 แถว + console มี anti-wedge log "local เพี้ยน, รับค่าจากชีตแทน" ครั้งเดียวต่อ entity ตอนเปิด (ไม่ loop ต่อ) + ไม่มี `🛑 row-level ผิดปกติ` (push-guard ไม่ trigger).
- **ผู้ใช้ที่ติดปัญหาอยู่:** หลัง deploy → กด **Ctrl+Shift+R** หรือเปิด tab ใหม่ — โค้ดใหม่จะดึงข้อมูลจาก server มาแสดงทันที, ไม่ต้องล็อกอินใหม่ (cleared site data ก็ login ใหม่ ปกติแล้ว).

## 2026-06-15 — Quick wins จากผลตรวจสอบ (crash + หน้าโครงการ) — build `20260615g`
ออดิตทั้งแอปพบว่าอาการ "หน้าเว็บดีด / ข้อมูลโครงการไม่ขึ้น / เดี๋ยวมาเดี๋ยวหาย" มาจาก **4 กลุ่มคนละสาเหตุ**: (1) ข้อมูลหาย/กระพริบ = **สถาปัตยกรรม** (Sheets + gviz อ่าน/Apps Script เขียน + ไม่มี version control → ต้องมีเกราะ 7+ ชั้น และเกราะเองทำให้กระพริบ — แก้ที่รากต้องย้าย backend ดู [[data-backend-migration]] / [[data-loss-root-cause-audit]]); (2) crash = **global-scope name collision**; (3) หน้าโครงการ = ยึด snapshot เก่า + ตัวกรองค้าง; (4) localStorage quota + backend 2 ไฟล์ drift. **แผน: ทำ quick wins (กลุ่ม 2-3) ก่อน แล้วค่อยย้าย Supabase (กลุ่ม 1) เป็นงานหลัก.** รอบนี้แก้ quick wins 3 จุด (verify ใน preview แล้ว: คอมไพล์ผ่านทุกไฟล์ + `DailyForecastModal`/`ForecastModal` แยกเป็น 2 ฟังก์ชัน + `fmtMoney(0)==='0.00'` = ตัวจริงจาก components.jsx ชนะแล้ว):
- **`ForecastModal` ชนกัน → หน้า Daily ดีด (แก้แล้ว).** `ForecastModal` ถูกนิยามซ้ำใน `page_daily.jsx` (drill-down อ่านอย่างเดียว รับ `title/list`) **และ** `page_bank_diary.jsx` (ฟอร์มกรอก รับ `bankAccounts/onSave`). bank_diary โหลดทีหลังใน index.html → **ทับตัวของ Daily** → คลิกช่องประมาณการในหน้า Daily เรียกตัว bank_diary ที่ทำ `bankAccounts.map()` (undefined) → จอ Daily พัง (ErrorBoundary). **แก้: เปลี่ยนชื่อตัวของ Daily → `DailyForecastModal`** (นิยาม + จุดเรียกใช้ที่ [page_daily.jsx](app/page_daily.jsx)). หน้า Daily ผู้บริหาร (viewer/owner) เห็น → กระทบคนสำคัญ. ดู [[global-scope-name-collision]].
- **`fmtMoney` ชนกัน → เลขเงินเพี้ยนทั้งแอป (แก้แล้ว).** `components.jsx` `fmtMoney` (th-TH, ทศนิยม, รองรับ `{compact,digits,sign}`) ถูกทับโดย `const fmtMoney` ใน [page_projects.jsx](app/page_projects.jsx) (en-US, ไม่มีทศนิยม, `0`→`'—'`, ไม่รับ opts) — ทั้งที่ `page_projects.jsx` **เลิกใช้แล้ว** (ProjectsPage ถูกแทนด้วย ProjectControlPage) แต่ยัง `<script>`-load → กระทบ Bank Diary/ใบเสร็จ/ดอกเบี้ย/กราฟ. **แก้: เปลี่ยนชื่อภายในไฟล์ทั้ง 22 จุด → `pjFmtMoney`** (เลิก define global `fmtMoney`).
- **หน้าโครงการ: snapshot เก่าค้าง + ตัวกรองคอลัมน์ค้าง (แก้แล้ว).** (a) `baseProjects = localProjects ?? data.projects` โดย `localProjects` (`wtp-proj-control-v2`) อ่านครั้งเดียวตอน mount → ถ้า snapshot เก่า (เช่น 319 < ชีต 648) หน้ายึด 319 ไปตลอด ไม่รับของจริงจากชีต. **แก้: เลือกชุดที่แถวมากกว่า** (`_syncedProj.length > _localProj.length ? synced : local`) — เหมือน `freshestRows` ใน data_sync (หลัง 2026-06-12 ชีต sync มีคอลัมน์เต็มแล้วจึงปลอดภัย) ([page_project_control.jsx](app/page_project_control.jsx)). (b) `colFilters` ถูกจำใน `wtp-pc-gridstate-v3` ข้าม reload (pcLoadState รีเซ็ตแค่ `page`) → ถ้ากรองแล้วซ่อนทุกแถวจะค้าง "ไม่พบโครงการ" ถาวร. **แก้: เพิ่มปุ่มทางออก "ล้างตัวกรองคอลัมน์ทั้งหมด" ใน empty-state** (`setState(s=>({...s, colFilters:{}, page:1}))`) เฉพาะตอนมี active colFilter ซ่อนทุกแถว (เพิ่ม UI ล้วน ไม่แตะ logic/persistence เดิม).

## 2026-06-15 — แก้ราก: receipt ↔ invoice ไม่ sync (Daily โชว์รับเงิน แต่ IV report ไม่โชว์) — build `20260615h`/data.js `20260615i`
- **อาการ (ตรวจจากไฟล์ export จริง + ชีตสด):** หน้า "รับเงินวันนี้" (Daily) โชว์รับเงิน 1 โครงการ แต่หน้า "รายงานติดตาม IV" ไม่โชว์. **ไม่ใช่ข้อมูลหาย** — ตารางครบ (receipts 698–703, invoices 727, projects 654), receipts↔invoices ตรงกัน 693/698, ไม่มี receipt กำพร้า. เคสจริง: receipt วันที่ 15 มิ.ย. (IV2604-025 บ้านทองหลาง 3.2M) **อยู่ในชีต** แต่ใบ IV2604-025 ยัง `status='tracking'`, `actualReceive=null`.
- **ROOT CAUSE = 2 หน้าอ่านคนละแหล่ง + backfill ทางเดียว:** หน้า **Daily** "รับเงินวันนี้" นับจาก **`data.receipts`** (เห็น receipt 15 มิ.ย. → 1) · หน้า **IV report** ([page_invoices.jsx](app/page_invoices.jsx) `IvReportStandalonePage`) "รับเงินวันนี้" นับจาก **`data.invoices` ที่ `status='paid' && actualReceive.date===today`** (ใบยัง tracking → 0). แอปมี backfill ทางเดียว `ensureReceiptForPaidInvoice` (IV paid → สร้าง receipt) แต่**ไม่มีทางกลับ** (receipt → flip IV เป็น paid) → เงินเข้าจริง (มี receipt) แต่ใบไม่ถูก mark paid → 2 หน้าขัดกัน.
- **FIX (ทางกลับ, build นี้):** เพิ่ม `WTPData.markInvoicesPaidFromReceipts(invoices, receipts)` ใน [data.js](app/data.js) (pure, raw-JS เลี่ยง `?.`) — มี receipt ของใบไหน + ใบนั้นยังไม่ paid → flip `status='paid'` + เติม `actualReceive.{date,amount,bankAccount}` + `actualReceiveDate` จาก receipt ล่าสุด. เรียกผ่าน effect ใหม่ใน [app.jsx](app/app.jsx) (deps `[data.receipts, data.invoices]`, รูปแบบ sync-safe เดียวกับ backfill เดิม: probe ก่อน, อ่าน state ล่าสุดใน updater, กัน data ว่าง, `forceSyncNow`).
- **⚠️ GOTCHA สำคัญ (verify จับได้ก่อน deploy):** เวอร์ชันแรกเช็คแค่ `actualReceive.date` (JSON) → flip **685 ใบ** เพราะใบส่วนใหญ่เก็บวันรับใน **คอลัมน์แบน `actualReceiveDate`** ไม่ใช่ JSON → ถ้า deploy ไปจะเขียนทับ invoices 685 แถว = mass churn (ต้นเหตุข้อมูลหาย!). **แก้: `hasDate = actualReceive.date || actualReceiveDate`** → flip เหลือ **1 ใบ** (เฉพาะ IV2604-025) + รันซ้ำ = 0 (idempotent, verify แล้วใน preview). **บทเรียน: ใบ IV เก็บ "วันรับเงิน" 2 ที่ (`actualReceive.date` JSON มักว่าง + `actualReceiveDate` แบน มักมีค่า) — โค้ดที่กรอง/จัดกลุ่มตามวันรับต้องเช็คทั้งคู่** (IV report "received today" ที่อ่านแค่ `actualReceive.date` จึงพลาดใบส่วนใหญ่ — ดูในผลตรวจหน้าอื่น).
- **ปลอดภัยต่อ production:** verify ใน preview โดย "ไม่ล็อกอิน" → guard "ไม่ล็อกอิน=ไม่ push" กันการเขียนชีตจริงไว้ (helper รันใน state ได้แต่ไม่ push). เมื่อ deploy + ผู้ใช้ล็อกอินเปิด → flip IV2604-025 (1 write) → IV report โชว์ตรงกับ Daily.

## 2026-06-15 — ตรวจทุกหน้า + แก้ "ของเล็กผลแคบ" ชุดแรก (ไม่แตะ WARROOM) — build `20260615j`
- **ออดิตทุกหน้า (~28 หน้า) ด้วย agent 5 กลุ่ม** พบปัญหากระจุกใน 5 รูปแบบราก (ส่วนใหญ่ตระกูลเดียวกับบั๊ก receipt↔IV): (A) **กับดักวันรับเงิน 2 ที่** `actualReceive.date` (JSON มักว่าง) vs `actualReceiveDate` (แบน มักมีค่า) → IV report "รับวันนี้"/Cashflow Actual/Daily MTD-YTD อ่านแค่ JSON เลยพลาดใบส่วนใหญ่; (B) **"รับเงินวันนี้" คิดคนละฐาน** (Daily=gross, IV report=net, Home=net) → 3 หน้ายอดต่างกัน; (C) **หน้าอ่านตารางสรุปที่เป็นศูนย์** (War Room P1 §04 `warroomP1.wipTotal`, War Room P2 §02 `monthlyForecast`); (D) **debtLedger schema v1 vs v2** — `buildLookups`(ใช้ใน War Room)/`calcInterest`/`debtSummary` อ่าน field v1 (`outstandingBalance`/`debtType`) กับชีต v2 → รวมได้ 0 (หนี้จริงอยู่ `debtMaster`); (E) **วันที่ DD/MM/พ.ศ. ไม่ normalize** (Bank Diary `bdToISO`, AP due อ่าน `p.due` แทน `due2`, interest engines). + cross-page: Investor ตัดโครงการยกเลิกแต่ Project Control รวม → มูลค่า/จำนวนไม่ตรง; Bank Diary ประมาณการนับซ้ำ (ไม่ตัด `EXPENSE_TYPE` AP/BANK_RECON). **รายการเต็มอยู่ในบทสนทนา audit — ยังไม่แก้ ค้างไว้ทำเป็นชุด.**
- **ชุดแรก (build นี้) = เฉพาะ "ของเล็ก ผลแคบ ไม่ต้องตัดสินใจ business ไม่แตะ WARROOM":** (verify preview แล้ว: คอมไพล์ผ่าน + ทดสอบค่า)
  - **Daily crash (แก้):** `drNormJobNo` เรียก `raw.trim()` กับ `projectCode`/`jobNo` ที่อาจเป็น**ตัวเลข** → จอ Daily พังทั้งหน้า. แก้ `String(raw).trim()` + guard null/'' ([page_daily.jsx:8](app/page_daily.jsx:8)) — ตระกูลเดียวกับ `normalizeJobNo`/[[global-scope-name-collision]]. verify: `drNormJobNo(12345)→"12345"`, `'PP064-STIIS'→"PP064"`.
  - **STS Workflow crash (แก้):** แถวตาราง deref `g.match.sts.contractNo`/`.principalAmount` โดยไม่เช็ค null (match ที่ผ่าน WCI อย่างเดียว → `sts` ว่าง) → จอ STS Workflow พัง. แก้ guard `sts ? … : ((g.match.wci && g.match.wci.contractNo) || '—')` ([page_sts_workflow.jsx:993](app/page_sts_workflow.jsx:993)).
  - **Users presence offline ค้าง (แก้):** `Number(p.lastSeen) || 0` → ถ้า `lastSeen` sync กลับมาเป็น ISO string → NaN→0→ออฟไลน์ตลอด. แก้ `Number(p.lastSeen) || Date.parse(p.lastSeen) || 0` ([page_users.jsx:102](app/page_users.jsx:102)).
- **ค้างทำต่อ (ยังไม่แตะ):** WARROOM (ผู้ใช้สั่งห้ามแตะรอบนี้) · กับดักวันรับเงิน 2 ที่ (helper กลาง `actualReceive.date || actualReceiveDate`) · Bank Diary (due2 + date parse + double-count) · debtLedger schema · และที่ต้องให้ผู้ใช้ตัดสินใจ: "รับเงิน" gross/net, Investor นับยกเลิกไหม, ความหมายเลข 228.4M.

## 2026-06-16 — Investor §6 timeline: ค่าของจ่ายหลังเริ่มงานถัง 7 วัน + ส่งมอบงวด 1 ขยับเข้ามา (build `20260616c`)
- แก้ตามคำขอผู้ใช้: เดิม `d:61 เริ่มงานถัง` จ่าย g2 ในวันเดียวกัน + `d:98 ติดตั้งเสร็จ` รวม ส่งมอบ+เคลม 40%+g3 — ผิดธรรมชาติ (ค่าของควรมาก่อนติดตั้ง). กฎ "ค่าของจ่ายหลังเริ่มงาน 7 วัน" + "ส่งมอบนับจาก 68 ไปอีก 7 วัน" → split + ขยับ:
  - `d:61` = เริ่มงานถัง (mark only, ไม่มี cashflow)
  - `d:68` = **จ่ายค่าของ ง.2** (g2) — NEW group
  - `d:75` = ส่งมอบงานงวด 1 + เคลม 40% (mark) + ค่าติดตั้ง ง.1 (i2) + **ค่าของ ง.สุดท้าย (g3)** [ย้าย g3 จาก d:98 มาที่นี่]
  - `d:105` = รับเงินงวด 1 (40%) [เก่า 128, ลด 23 วันตามสโคป]
  - `d:128` = ส่งมอบงวด 2 + ค่าติดตั้งสุดท้าย (i3) [เก่า 151]
  - `d:158` = รับเงินงวดสุดท้าย (60%) [เก่า 181]
  - `d:731` = คืน LG (คงเดิม, 2 ปี fixed)
- ลบ groups เก่า `d:91 หลัง PnP เสร็จ` และ `d:98 ติดตั้งถังเสร็จ` (รวมเข้า d:75 หมด). จำนวน groups คงเดิม 11 (split d:61 +1, รวม d:91+d:98 −1).
- คณิตเศรษฐศาสตร์ (peak/margin/totals) ไม่เปลี่ยน — แค่ลำดับวันเปลี่ยน. คอลัมน์ timeline+ตาราง stepTable ใช้ `invCfGroups(cfg)` ทั้งคู่ จึง sync. verified ใน preview: pills 1/61/68/75/105/128/158/731 ครบ, การ์ด `d:68` โชว์ "ค่าของ งวด 2 (ผลิต) −฿338K (6.3%)", การ์ด `d:75` โชว์ "Deliver milestone 1 / เคลม 40% / ค่าติดตั้ง ง.1 + ค่าของ ง.สุดท้าย".

## 2026-06-16 — Investor §6 timeline: day-number nodes + click-through breakdown (build `20260616b`)
ตามคำขอผู้ใช้ที่หน้า §6 "เศรษฐศาสตร์โครงการ" (`InvEconomics` ใน `app/page_investor.jsx`):
- **โหนดบนแกนเวลา = "วันที่นับจากลงนาม" แทนลูกศร ↓/↑/◆.** เพิ่มฟิลด์ `d` ให้ทุก group ใน `invCfGroups` (วันลงนาม = **วันที่ 1**, ค่าติดลบ = ก่อนลงนาม): allocation −44 · มัดจำค่าของ −14 · ออก LG −6 · **ลงนาม 1** · เริ่มงานถัง 61 · หลัง PnP 91 · ติดตั้งถังเสร็จ 98 · รับ 40% 128 · ส่งมอบงวด 2 151 · รับ 60% 181 · คืน LG 731. โหนดวงกลม (ใน `column`) โชว์ caption `วันที่`/`ก่อน` + ตัวเลข (สีขอบ = ทิศ เข้า/ออก คงไว้) แทน glyph ลูกศร. ป้าย timing pill = `invDayPill(gd.g.d,lang)` ("วันที่ 61" / "ก่อนลงนาม 7 วัน") + ชื่อ phase. ⚠️ **`gd` คือ element ของ `gs` ที่ห่อ group ไว้ใน `gd.g` → ต้องอ้าง `gd.g.d` ไม่ใช่ `gd.d`** (เคยพลาดเป็น "วันที่ undefined"). Helper ใหม่ระดับ module: `invDayNode(d)`, `invDayPill(d,lang)`. ไอคอนเล็กในการ์ด (`buildEventCard`) เปลี่ยนลูกศร→ `฿` (กระแสเงิน) / `◆` (milestone).
- **แต่ละสเต็ประบุ "ค่าอะไร · งวดไหน · กี่% · กี่บาท" + คลิกแจกแจงได้.** การ์ด timeline โชว์ tag ต่อรายการ + % ของสัญญา (ย่อด้วย `invCompact`); ตาราง `stepTable` เพิ่มชิป `ค่าของ ง.2 · 7.5% · ฿1.3M` ต่อรายการเงินสด (event-only = "◆ เหตุการณ์ (ไม่มีกระแสเงิน)") + day ใน caption. **คลิกการ์ด/แถวตาราง → `setDrill(i)` เปิด modal (`drillModal`)** แจกแจงทุก item: ป้ายหมวด (`catOf`: ค่าของ/ค่าติดตั้ง/คอมมิชชั่น/LG/รับเงิน) + คำอธิบายเต็ม (งวด) + ทิศ + **% ของสัญญา + ยอดเต็มด้วย `invFmt` (ไม่ย่อ)** + รวมรับ/จ่าย/สุทธิ/คงเหลือสะสม + footer อ้างมูลค่าสัญญา+รุ่น. state `drill` (index ของ group, Esc/คลิกฉากหลังปิด). ตัวเลขทั้งหมดยัง read-only จาก `INV_CF_DEF(code, price)` เหมือนเดิม. verify ใน preview แล้ว (TH): โหนด ก่อน45/15/7 → วันที่ 1/61/98/128/151, modal "ลงนามสัญญา" โชว์ คอมมิชชั่น −฿324,000 (6.0%) + ค่าติดตั้งล่วงหน้า −฿602,592 (11.2%), ไม่มี console error.

## 2026-06-16 — แก้ "กับดักวันรับเงิน 2 ที่" (ฝั่งอ่าน) ด้วย helper กลาง — build `20260615k`
- **ต่อจากแก้ราก receipt→IV (ฝั่งเขียน):** ฝั่ง "อ่าน" ยังพลาด — โค้ดที่กรอง/จัดกลุ่ม "รับตามวัน" อ่านแค่ `actualReceive.date` (JSON) ซึ่ง verify แล้วว่า **มีค่าแค่ 2 จาก 686 ใบ paid** (99.7% วันรับจริงอยู่คอลัมน์แบน `actualReceiveDate`) → IV report "รับวันนี้"/Cashflow Actual มองเห็นเกือบ 0 ใบ = เหตุที่ Daily (อ่าน receipts) โชว์ แต่ IV report ไม่โชว์.
- **FIX = helper กลาง `ivReceivedDate(iv)` ใน [components.jsx](app/components.jsx)** (global, โหลดก่อนทุก page): คืน ISO จาก `actualReceive.date` (รองรับ object/JSON-string) **fallback `actualReceiveDate`**; normalize DD/MM/พ.ศ. ผ่าน `parseDateFlexible`. verify: JSON→ISO, คอลัมน์แบน→ISO, `12/06/2569`→`2026-06-12`, ว่าง→`''`. **impact: ใบที่ resolve วันรับได้ 2→686 (+684).**
- **ใช้แทนการอ่าน `actualReceive.date` ตรงๆ 5 จุด:** IV report "รับเงินแล้ววันนี้" ([page_invoices.jsx:1018](app/page_invoices.jsx:1018)) + 2 จุด display "รับจริง {date}" (905, 1175) · Cashflow `ivActualReceiveDate` delegate ไป helper ([page_cashflow.jsx:236](app/page_cashflow.jsx:236)) · Daily secondary pass (paid invoices ที่ยังไม่มีใน receipts) + guard `actualReceive?.amount` null ([page_daily.jsx:91](app/page_daily.jsx:91)).
- **ปลอดภัย:** ฟิกซ์ฝั่งอ่านล้วน (ไม่เขียน/ไม่ push) — เปลี่ยนแค่ "นับ/แสดง" ให้ครบ ไม่มี churn. `data.js`/`app.jsx` ฝั่ง write-path (`ensureReceiptForPaidInvoice` ที่ require `actualReceive.date`) ไม่แตะ (ตั้งใจ — pay modal เซ็ต date เสมอ; receipt→IV ทางกลับครอบคลุมแล้ว). Home (`page_home.jsx:436`) เช็คทั้งคู่อยู่แล้ว.
- **ค้างต่อ (กลุ่มเดียวกันที่ยังไม่แตะ):** Bank Diary 3 จุด (due2 + DD/MM parse + ตัด AP/BANK_RECON double-count) · WARROOM (ห้ามแตะ) · ที่ต้องตัดสินใจ: "รับเงิน" gross/net.

## 2026-06-16 — Bank Diary: AP due2 + วันที่ DD/MM/พ.ศ. (ยังไม่แตะ double-count) — build `20260615k`
- **`bdToISO` รองรับ DD/MM/พ.ศ. แล้ว** ([page_bank_diary.jsx:83](app/page_bank_diary.jsx:83)) — เดิมใช้ `Date.parse` อ่าน `05/06/2026`→6 พ.ค.(เดือนสลับ), `25/05/2569`→NaN. แก้: ลอง `parseDateFlexible` (global, รองรับ DD/MM ไทย + พ.ศ.) ก่อน, คง fast-path ISO + fallback `Date.parse`. verify: `05/06/2026`→`2026-06-05`, `25/05/2569`→`2026-05-25`, ISO คงเดิม.
- **AP due อ่าน `due2` ก่อน** ([page_bank_diary.jsx:171](app/page_bank_diary.jsx:171)) — `bdNormAP` เดิมอ่าน `p.due`; canonical field = `due2` (Home/ทั้งแอปใช้ `due2 || dueDate || due`). verify จากชีตสด: `due` เก็บ ISO / `due2` เก็บ DD/MM ไทย เป็น**วันเดียวกัน** → หลังแก้ทั้ง 545 แถว resolve ตรงกัน (differ=0, **ไม่เปลี่ยนข้อมูลปัจจุบัน**) แต่ robust + parse DD/MM ถูก + รอดถ้า `due` ว่าง (ตามที่ออดิตเตือน). 2 fix นี้ต้องคู่กัน.
- **⏸️ HOLD — ตัดประมาณการนับซ้ำ BANK_RECON (ยังไม่แก้):** ดีไซน์เดิม **ตั้งใจ** โชว์ BANK_RECON STATUS='ACTUAL' เป็น "จ่ายจริงแล้ว" ในการ์ด + มี date-gating (`f.date >= asOfRef`) คุมอยู่แล้ว → การไปแก้คณิตยอดเงิน (running = base + Σ signed) เสี่ยงทำผิดทางตรงข้าม. **ต้องให้ผู้ใช้ยืนยัน** ว่า BANK_RECON/payment ที่จ่ายจริงแล้วควรถูกหักออกจาก "ยอดคาดการณ์" อีกไหม ก่อนแตะ. (AP forecast นับซ้ำถูกคุมด้วย `paidApSet` อยู่แล้ว ไม่ต้องแก้.)

## 2026-06-16 — ข้อมูลหายจาก "แท็บเปิดค้าง auto-push" + กู้ 28 แถว + ปิด auto-push ตอน idle — build `20260616a`
- **เหตุการณ์:** เทียบ backup 15:18 vs ชีตสด → **invoices 727→712 (หาย 15), receipts 698→686 (หาย 13)** รวม ~฿74M. กู้คืนแล้วด้วย **upsert จาก backup** (applyDiff upserts=28 แถว, deletes=[] → เพิ่มเฉพาะที่หาย ไม่ทับของใหม่) → ยืนยันสด invoices 727 / receipts 698. (วิธีกู้: parse db.xlsx ด้วย `XLSX.read(..,{cellDates:true})` แปลง Date→ISO + parse JSON fields, แล้ว `fetch(APPS_SCRIPT_URL, applyDiff)` ตรง — preview ไม่ล็อกอินจึงต้อง POST ตรง). auditLog มี 2 แถว `RESTORE ... from 15:18 backup`.
- **ROOT CAUSE (แก้ความเข้าใจ): ไม่ใช่ "คนแย่งกันกรอก"** — auditLog โชว์ `invoices 719→719 (edits only)` ทุก ~3 นาทีจาก nantawan/patima ทั้งที่ไม่ได้แตะ = **"diff หลอก" จากแท็บที่เปิดค้าง**. ต้นเหตุ = `app.jsx` effect `WTPData.save(data)` (deps `[data]`) ยิงทุกครั้งที่ data เปลี่ยน **รวมตอน poll** → schedule auto-push → แท็บที่ cache เก่า/normalize ต่าง ดันของเก่าทับ → ลบแถวที่คนอื่นเพิ่ง add (การลบทีละ 7–14 แถว "รอด" เกราะ 50%). 21:36 แท็บ admin (cache เกือบว่าง) ดัน invoices 712→7 / receipts 686→22 — เกราะ server กันทัน. = ปัญหา churn #3 ที่ยังไม่อุด (ดู [[audit-misattribution-idle-push]]).
- **FIX = "auto-push activity gate" (`data_sync.js`):** `WTPData.save` wrapper จะ schedule auto-push **เฉพาะเมื่อผู้ใช้ "เพิ่งแตะ"** (keydown/input/change/mousedown/touchstart/paste) ภายใน `AUTO_PUSH_ACTIVITY_WINDOW_MS` (config, default 2 นาที). แท็บเปิดค้างไม่แตะ (`lastUserActivity===0` หรือเกิน window) → **ไม่ push** = ตัดต้นเหตุ. **★ `forceSyncNow` (ปุ่มบันทึกจริง + backfill effects) ข้าม gate เสมอ → การแก้จริงเซฟ 100%** — นี่คือเหตุผลที่ไม่ต้องไล่แก้ทุกหน้า (edit ที่ไม่กดปุ่มก็มี keydown/click อยู่ใน window). config: `AUTO_PUSH_REQUIRES_ACTIVITY` (false=ถอยกลับพฤติกรรมเดิม), `AUTO_PUSH_ACTIVITY_WINDOW_MS`. Diagnostic: `WTPData._autoPushInfo()`. verify preview: idle→`wouldAutoPush:false`, หลัง keydown→`true`.
- **⚠️ DEPLOY:** fix นี้ทำงานเฉพาะแท็บที่รันโค้ดใหม่ — **แท็บเก่าที่เปิดค้างยัง phantom-push จนกว่าจะ reload** → หลัง deploy ต้องให้ทีม **ปิดแท็บทั้งหมด + hard-refresh** (หรือกด "บังคับออกจากระบบทุกคน") ไม่งั้นแท็บเก่ายังทำข้อมูลหายได้. ทางแก้ถาวรยังเป็น Supabase (เขียนทีละแถว มี transaction).
- **บทเรียน:** เก็บ backup สม่ำเสมอ (ไฟล์ 15:18 กู้ ~฿74M กลับมาได้). ถ้าเจอข้อมูลหายอีก: เทียบ backup vs สด หา id/ivNo/receiptNo ที่หาย → upsert คืนผ่าน applyDiff (ไม่ replaceAll).

## 2026-06-16 — localStorage quota fix: SKIP_CACHE_KEYS + storageBroken false-positive (build `data.js 20260616e` / `data_sync.js 20260616c`)
- **อาการ:** `[WTPData] เขียน localStorage ล้มเหลว ... exceeded the quota` — blob `wtp-fin-data-v8` โตเกิน ~5MB หลัง `projects` ขยายเป็น 648 rows × 120 cols + debtLedger 3964 rows + pvVouchers/payables/receipts → รวม ~5.7MB.
- **FIX 1 — `SKIP_CACHE_KEYS` (`app/data.js 20260616e`):** `save()` ตัด entities ใน Set นี้ออกก่อน serialize → blob ลงเหลือ ~0.5MB. **เพิ่ม `projects` (ใหม่)** เป็น entity หลักที่ทำให้ blob ยังเกิน quota แม้จะตัด 5 entity แรกออกแล้ว (648 rows × 120 cols ≈ 4–5MB). Set สุดท้าย: `debtLedger`, `projects`, `pvVouchers`, `bankReconLines`, `receipts`, `payables`. `SKIP_CACHE_KEYS` exposed บน `window.WTPData.SKIP_CACHE_KEYS` เพื่อให้ `data_sync.js` อ้างอิงได้.
- **FIX 2 — `storageBroken` false-positive (`app/data_sync.js 20260616c`):** `syncDiffRowLevel` มี `storageBroken` check ดู ≥2 entities จาก `['pvVouchers','payables','debtLedger','invoices','projects']` ที่ `lastSnapshot ≥10 rows` แต่ `latest[k]=[]`. หลัง SKIP_CACHE_KEYS เปิด → entities เหล่านี้คืน `[]` **โดยตั้งใจ** → check ตีว่า storage เสีย → `storageBroken=true` → ใช้ `data[entity]` (React state stale ก่อน poll) → guard fires ทุก entity. **แก้: เพิ่ม `var skipCache = WTPData.SKIP_CACHE_KEYS` + `if (skipCache.has(k)) return false` ใน filter** → เฉพาะ entities ที่ไม่ได้อยู่ใน skipCache เท่านั้นที่นับ. **และเพิ่ม `var useReactState = storageBroken || skipCache.has(entity)`** → SKIP_CACHE_KEYS entities ใช้ `data[entity]` (React state) เสมอ ไม่อ่าน `latest` (ซึ่งว่างโดยตั้งใจ).
- **ผลรวม:** blob ~5.7MB → ~0.5MB, `🛑 row-level` guard ไม่ fire อีก, SKIP_CACHE_KEYS entities ดึงจาก server poll ทุก reload (~5–10 วิ) ผ่าน `applyEntityGuard corruptLocal→recover`. `projects` ยังมี `wtp-proj-control-v2` cache แยก (120 cols, ใช้โดย Project Control page).

## 2026-06-16 — `raw.trim is not a function` crash บนหน้า #invoices (build `page_invoices.jsx 20260616f`)
- **อาการ:** กด "บันทึก/อัพเดท" ใน `#invoices` → ErrorBoundary แสดง "หน้านี้เกิดข้อผิดพลาด · raw.trim is not a function". เกิดขึ้นหลัง `normalizeJobNo` ถูกแก้ไปแล้วใน build `20260615j`.
- **ROOT CAUSE:** `ImportRawIvModal` มี `{raw.trim() && ...}` ในส่วน JSX (render path) 2 จุด (lines 2186 useEffect + 2408 JSX). **JSX children ถูก evaluate ก่อน** pass เป็น prop ให้ `<Modal>` — แม้ `open=false` ก็ยังถูก eval ทุก render. หาก `raw` state กลายเป็น `null`/non-string จาก `XLSX.utils.sheet_to_csv` edge case (เช่น worksheet ว่างหรือเสีย → อาจคืน null แทน string) → `null.trim()` throw → ErrorBoundary catch. ทริกเกอร์หลัง save เพราะ save → setData → re-render → `ImportRawIvModal` re-render → `raw.trim()` throw.
- **FIX (`app/page_invoices.jsx 20260616f`):**
  - **line 2186 (useEffect):** `const rawStr = String(raw == null ? '' : raw);` แล้วใช้ `rawStr.trim()` + `parseRawIv(rawStr)` แทนการเรียก `raw.trim()` / `parseRawIv(raw)` ตรงๆ
  - **line 2408 (JSX render):** `{String(raw == null ? '' : raw).trim() && (` — defensive coerce ก่อน trim เหมือน `normalizeJobNo` pattern
- **Pattern ที่ต้องจำ (เพิ่มจาก [[global-scope-name-collision]]):** ตัวแปรที่มาจาก external library (SheetJS), synced field, หรือถูก set จาก async callback **ต้องเสมอ** coerce ผ่าน `String(x == null ? '' : x)` ก่อน `.trim()` — JS ไม่มี null-safe `.trim()`.

## 2026-06-16 — Supabase backend (Phase 1) — สลับด้วย flag เดียว (build `data_supabase 20260616s1`)
**ทำไม:** Google Sheets เป็น DB (อ่าน gviz CSV / เขียน Apps Script = สอง cache คนละตัว) คือ "ราก" ของอาการ
แก้แล้วเด้งกลับ / ข้อมูลหาย / กระพริบ ที่ต้องใส่เกราะ 7+ ชั้น (ดู [[data-loss-root-cause-audit]] / [[data-backend-migration]]).
แก้ถาวร = ย้ายไป Postgres (อ่านหลังเขียนเห็นทันที + เขียนทีละแถว + realtime push). รอบนี้ทำ **Phase 1** ครบ — โค้ดพร้อม,
รอผู้ใช้ทำ cutover ตาม [docs/supabase-setup-guide.md](docs/supabase-setup-guide.md).
- **สลับด้วย flag เดียว:** `app/config.js` → `BACKEND: 'sheets' | 'supabase'` (+ `SUPABASE_URL`, `SUPABASE_ANON_KEY`).
  default `'sheets'` = พฤติกรรมเดิมเป๊ะ (verify แล้ว: serverVersion ยังเป็น `…actualguard`, data_sync ทำงานปกติ). **ถอยกลับ = ตั้ง `'sheets'` + push.**
- **`app/data_supabase.js` (ใหม่) — adapter ที่ "คง interface `WTPData` เดิมทุกเมธอด"** → `app.jsx` + ทุก `page_*.jsx` **ไม่ต้องแก้เลย**.
  เปิดทำงานเฉพาะเมื่อ `BACKEND==='supabase'` (ไม่งั้น early-return เงียบ). `data_sync.js` ก็ได้ guard ตรงข้าม (early-return เมื่อ supabase) → เจ้าของ `WTPData` มีตัวเดียวเสมอ.
  เมธอดที่ define: `save`(diff→upsert/delete ทีละแถว) · `subscribe`(realtime push) · `getSyncStatus` · `forceSyncNow` · `refreshFromServer` · `pushPresence` · `forceDeleteRows`(ข้ามเกราะ) · `fetchSheetRows`(CRUD→Supabase, P&L/Budget/auditLog→gviz fallback) · `fetchSheetRowRaw` · `serverVersion='supabase'` · `_autoPushInfo` · `buildId`. (`load` คง = data.js เดิม).
- **Schema = JSONB ต่อ entity** ([supabase/schema.sql](supabase/schema.sql)): ทุกตาราง = `"id" text PK, "data" jsonb, "updated_at" timestamptz`.
  **★ ชื่อตารางต้อง quote (camelCase) เป๊ะ** เช่น `"forecastEntries"` ไม่งั้น PostgREST/supabase-js หาไม่เจอ. map ง่าย: `row ↔ {id, data:row}` (data เก็บทั้งแถวรวม id). 23 ตาราง (= `CRUD_KEYS` ใน data.js) + `audit_log` + trigger updated_at + realtime publication + grant anon/authenticated. **RLS ปิดไว้ก่อน** (anon key อ่าน/เขียนได้ — โพสเจอร์เดียวกับ Apps Script เดิม); RLS+policy ต่อ role = Phase 4 (comment ไว้ท้าย schema).
- **GOTCHA สำคัญ:** PostgREST `select` คืน **สูงสุด 1000 แถว/ครั้ง** → ต้อง paginate ด้วย `.range()` (debtLedger ~4000 แถว ไม่งั้นขาด). adapter มี `selectAll()` วน range ทีละ 1000.
- **ทิ้งเกราะ Sheets ได้ (Postgres consistent):** grace window/anti-flip/base-reconcile/3-way merge/CLEARABLE/hot-cold/mass-delete heuristic. **คงเกราะเบา 3 ตัว:** `_hasValidSession` (ไม่ล็อกอิน=ไม่ push) + activity-gate (แท็บค้างไม่แตะ=ไม่ push) + light mass-delete guard (กัน cache เพี้ยนสั่งลบ >50%). **whole-row upsert** (ไม่ใช่ field-patch — jsonb เก็บทั้งแถว; realtime ทำให้ state สดก่อน save → lost-field ระหว่างคน ~ศูนย์).
- **`tools/migrate-to-supabase.html`** — ย้ายข้อมูลครั้งเดียว (self-contained: โหลดแค่ config.js + supabase-js, มี gviz reader ในตัว ไม่ขึ้นกับ flag): อ่าน 23 ชีต → parse JSON fields (invoices/stsCalcResult) → `upsert {id,data}` batch 500 → verify count. รันตอน RLS ปิด, anon key.
- **Phase 1 ไม่ย้าย:** P&L / Budget / Audit Log ยังอ่านจาก Google Sheet (hybrid ผ่าน `fetchSheetRows` gviz fallback) → ต้องคง `SHEET_ID` ใน config. **index.html** เพิ่ม `<script>` supabase-js CDN (`@supabase/supabase-js@2.45.4` UMD → `window.supabase`) + `app/data_supabase.js`. **ยังไม่ตัด** Apps Script/data_sync.js (เก็บเป็น fallback). Phase 4: RLS + Supabase Auth + ย้าย P&L/Budget/audit + ตัด Sheets.

## 2026-06-16 — Supabase Phase 4 (Auth + RLS) — ✅ LIVE แล้ว (เปิดใช้จริง)
**สถานะล่าสุด:** `USE_SUPABASE_AUTH:true` + ลบ password ทั้งหมดออกจาก config.js (public) + `FORCE_LOGOUT_BEFORE` บังคับทุกคน re-login ด้วยรหัสใหม่ + รัน `supabase/rls-phase4.sql` เปิด **RLS แล้ว**. **verify สด (curl):** anon อ่าน projects=`[]` (บล็อก), anon เขียน=401, admin(manager) login→อ่าน 648 + เขียน 201 ✓. → **anon key ที่อยู่ใน repo public ใช้แตะ DB ไม่ได้แล้ว** (ต้อง login จริง). `tools/supabase-backup.html` แก้ให้ login ก่อนใช้ (anon โดน RLS บล็อก). **⚠️ ต้องเช็ค: Supabase → Authentication → "Allow new users to sign up" = OFF** (ไม่งั้นคนนอกสมัครเองแล้วกลายเป็น authenticated → อ่านข้อมูลได้ ทะลุ RLS). รายละเอียด rollout/ถอยกลับ: [docs/supabase-phase4-auth-guide.md](docs/supabase-phase4-auth-guide.md) (ROLLBACK block ปิด RLS อยู่หัว rls-phase4.sql). ด้านล่างคือบันทึกตอนสร้าง scaffolding:

## 2026-06-16 — Supabase Phase 4 (Auth + RLS) scaffolding (build `data_supabase 20260616s3`)
ต่อจาก cutover Phase 1 — ล็อกความปลอดภัยจริง (anon key เป็น public ใน repo → ตอนนี้ RLS ปิด ใครมี key ก็แตะ DB ได้).
รอบนี้สร้าง "โครง" ครบ + **flag-gated ปิดไว้** (ยังไม่กระทบ live — verify no-regression แล้ว) รอ rollout:
- **`tools/supabase-backup.html`** — ดาวน์โหลดทุกตารางเป็น JSON (กันข้อมูลหาย แทน version history ชีต) + restore (upload→upsert non-destructive). verify: ดึง 23 ตาราง 9,012 แถว ~7.7MB. ⚠️ ขณะ RLS ยังปิด = ปุ่มนี้ดึงข้อมูลได้ด้วย anon (โพสเจอร์เดิม) — จะถูกล็อกเมื่อเปิด RLS.
- **`tools/supabase-auth-setup.html`** — สร้าง/อัปเดต Supabase Auth users (จาก `USERS` ใน config.js + เพิ่มคนใหม่ได้) ด้วย **service_role key (วาง runtime ในเบราว์เซอร์ ไม่เก็บ/ไม่ขึ้น repo)** → `admin.createUser`/`updateUserById` email `<username>@AUTH_EMAIL_DOMAIN` + รหัส (พิมพ์เอง/สุ่ม) + **`app_metadata.role`** (ใช้กับ RLS) → โชว์รหัสครั้งเดียวให้แจกทีม. **★ มีช่องติ๊ก "ทำ" รายคน (2026-06-17): กดปุ่มจะดำเนินการ "เฉพาะแถวที่ติ๊ก" เท่านั้น — คนเดิม default ไม่ติ๊ก (ไม่ถูกแตะ), คนใหม่ (ปุ่ม + เพิ่มผู้ใช้ใหม่) ติ๊กอัตโนมัติ → ตั้งรหัสใหม่/เพิ่มแค่บางคนได้โดยไม่กระทบรหัสคนอื่น.** ปุ่มเลือกทั้งหมด/ไม่เลือก + ดาวน์โหลด .txt (สรุปรหัส). **★ ปุ่ม "↻ โหลดผู้ใช้ที่มีอยู่จาก Supabase" (2026-06-17):** ตารางตอนเปิดดึงจาก `config.js USERS` เท่านั้น → **คนที่เคยเพิ่มผ่านปุ่ม "+เพิ่มผู้ใช้ใหม่" จะไม่อยู่ใน config → เปิด tool มาแล้วไม่เห็น → แก้รหัสเขาไม่ได้**. ปุ่มนี้เรียก `admin.listUsers()` (ต้องวาง service_role ก่อน) merge คนใน Supabase ที่ยังไม่อยู่ในตาราง (username/role/displayName จาก `app_metadata`, username readonly) → ครบทุกคนที่ login ได้จริง. **รหัส Supabase Auth = bcrypt hash ดูย้อนหลังไม่ได้** (Dashboard/DB ก็ไม่โชว์) — ลืม = ตั้งใหม่ทับเท่านั้น; ดาวน์โหลด/ก็อปเก็บก่อนปิดแท็บเสมอ. (คนที่เพิ่มผ่าน tool ควรเพิ่มลง `config.js USERS` ด้วยเพื่อเป็น directory ให้แอป — ไม่งั้น login ได้แต่บางหน้าไม่รู้จักชื่อ.)
- **`supabase/rls-phase4.sql`** — เปิด RLS ทุกตาราง: `auth_role()` อ่าน `app_metadata.role` จาก JWT → SELECT=ทุก authenticated, write(insert/update/delete)=staff/manager, presence=ทุก authenticated, audit_log อ่าน=manager. มี ROLLBACK block (ปิด RLS) หัวไฟล์. **ห้ามรันจนกว่า auth users + โค้ด login auth deploy + ทุกคน re-login** (ไม่งั้น anon ถูกบล็อก = จอว่าง).
- **flag-gated code (ปลอดภัย):** `config.js` เพิ่ม `AUTH_EMAIL_DOMAIN:'waterpog.app'` + `USE_SUPABASE_AUTH:false`. `data_supabase.js` createClient `persistSession:true` + เพิ่ม `WTPData.authSignIn(username,password)` (signInWithPassword `username@domain` → คืน {username,displayName,role} จาก app_metadata) / `authSignOut()` + `onAuthStateChange` SIGNED_IN→reload (request พก JWT ผ่าน RLS). `app.jsx` `handleSubmit` ถ้า `USE_SUPABASE_AUTH` → ใช้ authSignIn (ไม่งั้น login เดิม); `handleLogout` เรียก authSignOut. **flag=false → พฤติกรรม login เดิมเป๊ะ.**
- **Rollout (ดู [docs/supabase-phase4-auth-guide.md](docs/supabase-phase4-auth-guide.md)):** A) รัน auth-setup tool สร้าง users+รหัส · B) ปิด "Allow new users to sign up" ใน Supabase · C) ตั้ง `USE_SUPABASE_AUTH:true` + ลบ password ออกจาก `USERS` (config.js, public!) + bump ?v= + push → ทีม re-login รหัสใหม่ (RLS ยังปิด) · D) รัน `rls-phase4.sql` เปิด RLS (ล็อกจริง) + verify incognito เห็นข้อมูลไม่ได้. ถอย: ROLLBACK block / revert / `BACKEND:'sheets'`.

## 2026-06-16 — Daily "รับเงิน" เบิ้ล = receipt ซ้ำในตาราง (deterministic id + dedup + ลบ 12 แถว) — build `data.js 20260616g` / `page_daily.jsx 20260616g`
- **อาการ:** หน้า Daily (`#daily`) ยอด/จำนวน "รับเงิน" เบิ้ล — MTD มิ.ย. โชว์ 21 ใบ/56.2M (จริง 11/29.7M), YTD โชว์ 104 ใบ/218.2M (จริง 93/190.4M) = บวมเกิน ~28–30M.
- **ROOT CAUSE = แถว receipt ซ้ำในตาราง `receipts` เอง** (ไม่ใช่บั๊ก dedup ข้าม receipt↔invoice — อันนั้นทำงานถูก 0 เคส). พบ **12 ใบมี receipt 2 แถว** เหมือนกันเป๊ะ (receiptNo/ยอด/บัญชี/note เดียวกัน) **ต่างแค่ `id`** → Supabase PK=`id` เก็บทั้งคู่. note ทุกแถว = "auto-generated เมื่อ mark IV เป็น paid" = มาจาก `ensureReceiptForPaidInvoice`. id แบ่ง 2 ชุด (`id_9xo…id_9xz` รวด 12 + `id_150…158`/`id_62d/e/f`) = backfill รัน **2 รอบ** (ตอน flip ใบ→paid + ตอน migrate Supabase) แต่ละรอบยังไม่เห็น receipt ของอีกรอบใน list (sync race) → `find(r=>r.invoiceNo===ivNo)` ไม่เจอ → สร้างใหม่ + `id()` สุ่มใหม่ทุกครั้ง → 2 แถว. **Daily Pass 1 วน `data.receipts` ทุกแถวโดยไม่ dedup กันเอง** ([page_daily.jsx](app/page_daily.jsx) `paidInvoices`) → นับซ้ำ.
- **FIX 3 ชั้น (verify ครบ — preview live: MTD 11/29.7M, YTD 93/190.4M, 0 ใบซ้ำ, ไม่มี console error):**
  1. **ลบแถวซ้ำ 12 แถวออกจาก Supabase** (เก็บใบละ 1 — เลือกแถวที่ receiptDate ตรงกับวันรับของใบ; เสมอกัน=เก็บ id เล็กสุด). receipts 711→699, 0 dup groups. backup รายแถวที่ลบไว้ที่ `%TEMP%\wtp-fix\` ก่อนลบ. ลบตรงผ่าน PostgREST DELETE (anon, RLS ปิด). **ปลอดภัยต่อ recurrence: หลังลบ ทุกใบเหลือ ≥1 receipt → `ensureReceiptForPaidInvoice` รอบหน้า `find`-เจอ → UPDATE ไม่ใช่ create.**
  2. **`data.js ensureReceiptForPaidInvoice` → id เป็น DETERMINISTIC `'rcp-'+ivNo`** (ไม่ใช่ `id()` สุ่ม) → 2 รอบ/2 เครื่องที่ race ได้ id เดียวกัน → upsert ทับเป็นแถวเดียว (กันเกิดซ้ำที่ราก). path `find`-เจอ (import/id เดิม) ยัง UPDATE keep id เดิมตามปกติ — deterministic id ใช้เฉพาะตอน "สร้างใหม่".
  3. **Daily Pass 1 dedup ภายใน receipts** ด้วย key `receiptNo|invoiceNo` (เกราะกันแถวซ้ำ receiptNo เดียวกันหลุดมา; ใส่ invoiceNo ใน key → receipt จริงคนละใบ/จ่ายหลายงวด receiptNo ต่างกันไม่ถูกยุบ; receiptNo ว่าง=ไม่ dedup).
- **บทเรียน (ตระกูล [[global-scope-name-collision]] / sync race):** entity ใดที่ "auto-generate แล้ว mint `id()` สุ่ม" บน backend ที่ PK=id + เขียนหลายเครื่อง = เสี่ยงแถวซ้ำเมื่อ list ยังไม่ sync — ใช้ **id แบบ deterministic จาก natural key** เสมอ. และหน้าใดที่ aggregate จาก `data.receipts` (Daily, War Room §01, ประวัติรับเงิน) ควร dedup ภายในด้วย ไม่พึ่งความสะอาดของตารางอย่างเดียว.

## 2026-06-17 — Phase 5: ย้าย P&L + Budget จาก Google Sheet ไป Supabase (build `data_supabase 20260617a` / `page_pnl 20260617a` / `page_budget 20260617a`)
- **ทำไม:** หลัง Phase 1 (23 CRUD entities) + Phase 4 (Auth+RLS) แอปยังอ่าน P&L + Budget จาก Google Sheet ผ่าน gviz → 2 backend คนละชุด; ย้ายให้ครบจบใน Supabase เดียว.
- **2 ตารางใหม่ (`supabase/pnl-budget.sql`):** `pnlBase` (ฐาน DATA P&L) + `budgetHo` (BUDGET HO) — JSONB schema เดียวกับ 23 entity เดิม + RLS (read=authenticated, write=staff/manager). รันใน SQL Editor แล้ว.
- **ย้ายข้อมูล:** P&L 169 แถว + Budget 457 แถว migrate จาก Google Sheet → Supabase ผ่าน preview browser (อ่าน gviz → upsert). verify: อ่านกลับได้ครบ (HTTP 200 count ตรง).
- **`app/data_supabase.js`** (`SHEET_TABLES = ['pnlBase','budgetHo']`, `SHEET_TABLE_SET`): `fetchSheetRows` route — entity ใน `TABLE_SET` (23 CRUD) หรือ `SHEET_TABLE_SET` (2 analytics) → อ่านจาก Supabase; ที่เหลือ → gviz fallback. เพิ่ม **`WTPData.writeTable(entity, rows, idOf)`** — full-sync (upsert all + delete missing ids, ข้าม RLS ด้วย session ปัจจุบัน) → ใช้โดย P&L + Budget import.
- **`app/page_pnl.jsx`:** `PL_SHEET = 'pnlBase'` (แทน `'ฐาน DATA'`). `postImport` ใหม่ = client-side port ของ Apps Script `plUpsertBase_`: โหลด current pnlBase → upsert `m{month}` ต่อ code → clear `m{month}=0` สำหรับ code ที่หายไปจากไฟล์ → `writeTable('pnlBase', out, r => String(r.code))`. ไม่ต้อง POST Apps Script อีก.
- **`app/page_budget.jsx`:** `BCC_SHEET = 'budgetHo'` (แทน `'BUDGET HO'`). `onUpload` ใหม่ = `writeTable('budgetHo', rows, r => r.dept+'|'+r.acct)` แทน Apps Script POST.
- **verify (preview ก่อน push):** P&L `hasRealAcct:true, sampleBadge:false` (169 แถวจริง) + Budget `hasBudgetContent:true, sampleBadge:false` (457 แถว) + ไม่มี console error.
- **ผลรวม:** ไม่ต้อง Apps Script + Google Sheet อีก (hybrid P&L/Budget ปิดแล้ว). ทุก read/write ผ่าน Supabase เดียว.

## 2026-06-17 — auditLog: อ่านผ่าน Supabase แล้ว (เดิมเขียน Supabase แต่อ่าน gviz) — build `data_supabase 20260617b` / `page_audit_log 20260617a`
- **อาการ/ช่องโหว่:** หลัง cutover Supabase, audit **เขียน**ลงตาราง `audit_log` แล้ว (`logAudit` ทุก push + `writeTable` ตอน import P&L/Budget) แต่ `page_audit_log` ยัง **อ่าน**ผ่าน `fetchSheetRows('auditLog')` ซึ่ง `'auditLog'` ไม่อยู่ใน `TABLE_SET`/`SHEET_TABLE_SET` → ตกไป **gviz fallback → อ่าน Google Sheet เก่า** → หน้าโชว์ของเก่าก่อนย้าย ไม่เห็น log ใหม่ใน Supabase เลย (เหมือนแช่แข็ง).
- **FIX (`data_supabase.js fetchSheetRows`):** เพิ่ม branch `entity==='auditLog'` ก่อน gviz fallback → `sb.from('audit_log').select('*').order('ts',{ascending:false}).limit(3000)` แล้ว map คอลัมน์ (columnar `ts/username/display_name/role/action/entity/summary`) → key ที่ `_norm` เข้าใจ (`timestamp/user/displayName/role/action/entity/summary`). **ตาราง `audit_log` schema ไม่ใช่ `{id,data}` JSONB** (เป็น columnar: `id bigint identity, ts, username, display_name, role, action, entity, summary, detail`) จึงใช้ `recToRow` ไม่ได้ ต้อง map เอง. ดึง 3000 ล่าสุดพอ (page โชว์ tail 200; limit dropdown สูงสุด 2000). **RLS อ่าน=manager เท่านั้น** (`rls-phase4.sql`) — ตรงกับ route ที่ manager-only อยู่แล้ว; non-manager ได้ `[]`.
- **page_audit_log:** แก้ข้อความหัวข้อ + empty-state จาก "ดึงจากชีต auditLog / Google Sheet" → "ดึงจากตาราง audit_log (Supabase)" + "ต้องเข้าสู่ระบบด้วยสิทธิ์ manager".
- **verify (preview, login=admin/manager):** `fetchSheetRows('auditLog')` คืน 66 รายการ (64 applyDiff + 2 writeTable = การ import P&L/Budget ของ Phase 5 เอง), หน้า render KPI/แท็บ/ตารางครบ, ไม่มี console error.
- **ผลรวม:** gviz fallback เหลือไว้เผื่อชีตอื่นในอนาคตเท่านั้น — ตอนนี้ **ทุก entity (CRUD + P&L/Budget + auditLog) อ่าน/เขียนผ่าน Supabase ครบ 100%** ไม่แตะ Google Sheet อีกเลย.

## 2026-06-17 — Budget §4 เปลี่ยน Treemap → บาร์แนวนอน Budget vs Actual + §5 Top 5 ประเภท คชจ. (build `page_budget 20260617b`)
- **§4 "การจัดสรรงบประมาณ" (Treemap) → "งบประมาณ vs ใช้จริง ตามประเภทค่าใช้จ่าย" (CatBars).** บาร์แนวนอนต่อหมวด: ชื่อ+ยอด ซ้าย, track งบ (ฟ้าอ่อน) + bar ใช้จริง (น้ำเงิน=ปกติ / เหลือง=≥90% / แดง=เกินงบ) ตรงกลาง, % + เกิน/เหลือ ขวา. คลิกแถว → `CatPopup` (drill รายแผนก) เหมือนเดิม. `Treemap`/`squarify` ลบทิ้ง.
- **§5 ใหม่ "อันดับประเภทค่าใช้จ่ายที่ใช้เยอะที่สุด" (TopCats).** sort categories ด้วย `actual` desc → 5 อันดับแรก: medal chip (🥇🥈🥉/4/5) + ชื่อ + ยอด `actual` + % ของยอดใช้รวม + bar ratio · ขวาเทียบ % vs งบ. คลิก → `CatPopup` เหมือน §4. wiring: ใช้ `view.categories` ที่มีอยู่แล้ว ไม่ต้องคำนวณเพิ่ม.
- **Renumber §5→6 (แจ้งเตือนงบเกิน), §6→7 (คาดการณ์สิ้นปี), §7→8 (ข้อมูลเชิงลึก).** verify preview: 7 หัวข้อเรียงถูก (2→3→4→5→6→7→8), 0 console error, sample data render ต้นทุนงาน 103.5% แดง / เงินเดือน 100.0% เหลือง / ค่าเดินทาง 102.5% แดง ตามเงื่อนไข.

## 2026-06-17 — เช็คจ่ายล่วงหน้า: ดอปดาวลิสต์ผู้รับเงิน+บัญชีบริษัท (build `page_checks 20260617c`)
- **ฟอร์มเพิ่ม/แก้เช็ค (`ChkAddModal`/edit)**: 3 ดอปดาวลิสต์ + 1 quick-select.
- `payeeOptions` (useMemo จาก `rawChecks`) — distinct ผู้รับเงินจากเช็คเดิม, sort by frequency desc → ใช้บ่อยอยู่บนสุด → แสดงผ่าน `<datalist id="chk-payee-list">` (HTML5 autocomplete แบบ native).
- `companyBanks` (useMemo จาก `data.bankAccounts`) — บัญชีบริษัทแบบ `{bankName, accountNo, nick}` (dedupe by bank+ac) → 3 จุด: (a) `<select>` "เลือกจากบัญชีบริษัท" เหนือ ธนาคาร/เลขบัญชี → เลือก 1 ครั้ง เติม 2 ฟิลด์อัตโนมัติ (b) datalist `chk-bank-list` (ชื่อธนาคารไม่ซ้ำ) (c) datalist `chk-ac-list` (เลขบัญชี + ชื่อธนาคารในป้าย).
- input ทุกตัวเป็น `<input list="...">` + `autoComplete="off"` → ผู้ใช้พิมพ์เองได้ตามปกติ ดอปดาวลิสต์เป็น suggestion ล้วน. select ขึ้นเฉพาะเมื่อ `companyBanks.length > 0`.

## 2026-06-17 — เช็ค: ประเภทเช็ค + datalist ธนาคารไทย + relabel ฟิลด์ (build `page_checks 20260617d`)
- **`CHECK_TYPES`** (constant ระดับ module, 5 ตัว: `principal_guarantee` ค้ำเงินต้น / `interest` ดอกเบี้ย / `principal_interest` เงินต้นและดอกเบี้ย / `expense` ชำระค่าใช้จ่าย / `other` อื่นๆ) + helper `CHECK_TYPE_LABEL(v)`. ฟิลด์ใหม่ `checkType` ใน emptyForm. ใน modal มี `<select>` กว้างเต็ม (gridColumn 1/-1) อยู่ใต้ "สถานะ". view modal โชว์ผ่าน `fld('ประเภทเช็ค', CHECK_TYPE_LABEL(view.checkType))`.
- **`COMMON_TH_BANKS`** (constant 17 ธนาคารไทย: กสิกร/SCB/KTB/BBL/BAY/TTB/GSB/GHB/KKP/CIMB/UOB/LH/TISCO/TCR/ICBC/HSBC/SCBT) — merge เข้า `chk-bank-list` datalist เดิม (companyBanks + COMMON, dedupe ด้วย Set) → กรอกธนาคารแล้วได้ suggestion ครบ.
- **Relabel** "อ้างอิง / PO" → "เลขที่สัญญาเงินกู้ / PO" (พร้อม placeholder เช่น `LN-2569-001 หรือ PO-XXX`) · "หมายเหตุ" → "หมายเหตุเพิ่มเติม (ถ้ามี)". อัปเดต view modal ให้ตรงกัน.

## 2026-06-17 — `WTPData.newId()` แก้ราก id ชน → แถวใหม่ทับกัน/หาย (build `data.js 20260617e`)
- **อาการ (ผู้ใช้รายงานหน้า #checks):** เพิ่มเช็คใบที่ 1 ขึ้นในตาราง → เพิ่มใบที่ 2 → **ใบที่ 1 หาย เหลือแต่ใบที่ 2**. (กรณี "เพิ่มเช็ค")
- **ROOT CAUSE = `newId()` เป็น counter ที่รีเซ็ตเป็น 1000 ทุกครั้งที่โหลดหน้า + ใช้ร่วมทุก entity** ([data.js](app/data.js)). เดิม: `const id = (() => { let n = 1000; return () => 'id_' + (++n).toString(36); })();` → แถวใหม่ใบแรกได้ `id_rt` ใบสองได้ `id_ru` **เหมือนกันเป๊ะทุก session / ทุกเครื่อง / ทุกรีโหลด**. บน Supabase ที่ **PK = id** การ upsert id ที่ซ้ำ = **"เขียนทับ" แถวเดิม** ไม่ใช่เพิ่มแถวใหม่. ลำดับบั๊ก: เพิ่มใบ 1 (`id_rt`) → หน้า reload/refresh (counter รีเซ็ต) → เพิ่มใบ 2 ได้ `id_rt` ซ้ำ → ทับใบ 1. **บั๊กเดียวกันยังทำให้เช็ค/แถวของคนอื่นที่เพิ่มพร้อมกันหาย** (สองคนต่างอ้าง `id_rt`). เป็นบั๊กตระกูลเดียวกับ receipt-dup (2026-06-16) แต่หนักกว่าเพราะ counter รีเซ็ต = ซ้ำแน่นอน ไม่ใช่แค่ race.
- **FIX = `newId()` ไม่ซ้ำทุกมิติ:** `'id_' + Date.now().toString(36) + '-' + (++n).toString(36) + Math.random().toString(36).slice(2,8)` → timestamp (ต่างทุกรีโหลด/เวลา) + counter ต่อ session (กันซ้ำใน ms เดียวกัน) + random (กันซ้ำข้ามเครื่องที่เรียก ms เดียวกัน). ตัวอย่าง: `id_mqhlob9e-2ophxl07`.
- **กระทบทุกหน้าที่เพิ่มแถว** (เช็ค/IV/หนี้/เจ้าหนี้/daily_balance/users/debt_ledger/bank_diary/bank_recon) เพราะใช้ `newId` ตัวเดียว — แก้จุดเดียวครอบคลุมหมด. **ปลอดภัย:** ไม่มีโค้ดไหน parse รูปแบบ `id_<base36>` (grep ยืนยัน — `page_cashflow.jsx:893 row.slice(3)` เป็น row-key `out1` ไม่ใช่ id); id ยาวขึ้นไม่กระทบ React key / Postgres text PK. id แบบเก่าใน DB ปลอดภัย (id ใหม่ format ต่าง + timestamp อนาคต = ไม่ชนของเก่า). **แถวที่หายไปก่อนหน้านี้กู้ไม่ได้** (ถูกเขียนทับใน DB แล้ว) แต่จากนี้ไม่หายอีก. verify preview: newId 6 ครั้งไม่ซ้ำ, ไม่มี `id_rt`-style, embed timestamp ปัจจุบัน, ไม่มี console error.
- **DEPLOY:** แท็บที่ cache โค้ดเก่า (data.js v เดิม) ยังมีบั๊ก → ทีมต้อง **hard refresh (Ctrl+Shift+R)** 1 ครั้งหลัง deploy. (ดีเทล id-collision ตระกูลเดียวกัน: [[global-scope-name-collision]] / receipt-dup ใน CLAUDE.md 2026-06-16.)

## 2026-06-17 — Bank Diary: ย้าย "โอนระหว่างบัญชี" ลงใต้ประมาณการ + ย่อได้ (build `page_bank_diary 20260617f`)
- **ตามคำขอผู้ใช้:** การ์ด `ReconcilePanel` ("⇄ ตรวจสอบการโอนเงินระหว่างบัญชี") เดิมอยู่ **บนสุด** (เหนือการ์ดบัญชี) → ย้ายลงไปอยู่ **ใต้ `BDForecastPanel`** (📊 ประมาณการกระแสเงินสด) + ทำให้ **ย่อ/กางได้** เหมือนพาเนลประมาณการ.
- **เปลี่ยน:** (1) เพิ่ม `const [collapsed, setCollapsed] = React.useState(true)` ใน `ReconcilePanel` (ต้องวางก่อน early-return `if (pairs.length===0) return null` เพื่อคง hook order) — **ย่อไว้เป็น default**. (2) หัวการ์ดเป็น `onClick={()=>setCollapsed(c=>!c)}` + ลูกศร ▶ หมุน + ป้าย "กดเพื่อดู ▾ / ย่อ ▴"; badge "⚠ N รอ Reconcile" / "✓ ยืนยันครบ" ยังโชว์บนหัวแม้ย่อ. (3) ห่อตาราง `{!collapsed && (<div…>…</div>)}`. (4) ย้าย `<ReconcilePanel/>` ใน render จากเหนือ Account Cards → ต่อจาก `<BDForecastPanel/>`.
- **Sort เดิมมีอยู่แล้ว** (กดหัวคอลัมน์ วันที่/จากบัญชี/ไปบัญชี/จำนวนเงิน/อ้างอิง/สถานะ — `sort`/`toggleSort`/`sortedPairs`) → ทำงานต่อหลังย้าย/ย่อ ไม่ต้องแก้.
- **verify (isolated render ใน preview, mock 3 คู่โอน):** default = ย่อ (เห็นแต่หัว+summary+badge, ไม่มี `<table>`) · กดหัว → กาง 3 แถว 7 คอลัมน์ · default sort วันที่ desc (15→12→10) · กด "จำนวนเงิน" → sort 120k→50k→8k. ไฟล์ compile ผ่าน Babel (globals `ReconcilePanel`/`BankDiaryPage`/`BDForecastPanel` defined, ไม่มี syntax error). *(screenshot tool time out — known flakiness ของ in-browser-Babel app, verify ด้วย DOM inspection แทน.)*

## 2026-06-17 — Bank Recon: ปุ่ม "🔗 จับคู่ PV เอง" (manual match) — เคสเลขเอกสารเปลี่ยน + กันยอดนับซ้ำ (build `page_bank_recon 20260617g`)
- **อาการ (ผู้ใช้):** มี PV ในระบบ (`PV20260600076` ฿2,592,000 → นาย กฤตวัฒน์) ค้างถัง **"ยังไม่ออกจริง"** (`unmatchedPv`) **พร้อมกับ** บรรทัด statement เดียวกัน (โอน ฿2,592,000 → SCB x8512 คนเดียวกัน) ที่ผู้ใช้กด **"บันทึกจ่ายจริง"** ไปแล้ว ค้างถัง **"บันทึกจาก statement"** (`recorded`) → **เงินก้อนเดียวถูกนับ Actual 2 รอบใน Cashflow** (PV via `pvActualByWeekCat` Pmt_Date + BANK_RECON `forecastEntries` STATUS=ACTUAL). กด **"↩ ยกเลิก"** แล้ว **เด้งกลับ**.
- **ROOT CAUSE 2 ชั้น:** (1) **auto-match พลาด** — เอกสารถูกแก้ เลขที่ PV เปลี่ยน → `brReconcile` pass 0 (จับด้วยเลข PV บน statement) ไม่ตรง; พอผู้ใช้กด "บันทึกจ่ายจริง" บรรทัดนั้นถูกล็อกออกจากการจับคู่ (`decision='recorded'`) เลยไม่ถึง pass 1 (ยอด+วันที่) ที่ปกติจะช่วยได้. (2) **"ยกเลิก" เด้งกลับ** — `undoRecord` **ลบ** reconState row ทิ้ง แต่แท็บค้าง/เครื่องอื่น sync `decision='recorded'` กลับมา → **self-heal** (กู้เฉพาะ `decision==='recorded'` ที่ `forecastId` หาย) สร้างแถว BANK_RECON ใหม่ → เด้งกลับ. `brUndoneRef` กันได้แค่ในแท็บเดียว (in-memory).
- **FIX = ปุ่ม "🔗 จับคู่ PV เอง" (manual match) — ทนทานเพราะ "เปลี่ยน decision" ไม่ใช่ "ลบ":**
  - reconState ได้ decision ใหม่ **`'matched'`** + ฟิลด์ใหม่ **`pvRef`** (id หรือเลขที่ PV). **ปลอดภัยบน Supabase (JSONB) — ไม่ต้อง redeploy/แก้ schema**; เพิ่ม `pvRef` ใน `brStateToRows`/`brRowsToState` (roundtrip verify แล้ว).
  - `brReconcile`: split เพิ่ม `manualMatched` (decision='matched') → **จองก่อน pass อัตโนมัติ** หา PV ใน `pvAvail` ด้วย `id` หรือ `pvNo`===`pvRef` → mark used (ออกจาก `unmatchedPv`) + push เข้า `matched` (`via:'manual'`, `manual:true`). บรรทัดออกจาก `missing`/`recorded` ทั้งคู่ — **ไม่สร้าง `forecastEntries` ใหม่** (PV เป็น Actual อยู่แล้ว) → ไม่นับซ้ำ.
  - handler `manualMatchPv(line, pv)`: ถ้าบรรทัดเคย `decision='recorded'` → ลบ `forecastEntries` (BANK_RECON) ที่ซ้ำทิ้งครั้งเดียว (`forceDeleteRows` + ตั้ง `brUndoneRef`) **แล้วเปลี่ยน decision เป็น `'matched'`** → self-heal เลิกยุ่งถาวร (มันกู้เฉพาะ `'recorded'`) + เป็น "อัปเดต row" ไม่ใช่ "ลบ row" จึงไม่ชนเกราะกันลบ/ไม่เด้งกลับ.
  - **UI:** ปุ่ม "🔗 จับคู่ PV" ในถัง **"ขาดบันทึก"** + "🔗 จับคู่ PV แทน" ในถัง **"บันทึกจาก statement"** (แก้แถวที่ค้างอยู่ได้ในคลิกเดียว ไม่ต้องยกเลิกก่อน) → เปิด **`BRMatchModal`**: ลิสต์ `unmatchedPv` (ในบัญชี/เดือน เรียงตามความใกล้เคียงยอด+วันที่, pre-select ตัวยอดตรง) + ช่องค้นทุก PV ในระบบ (`pvRefPool`, ค้น pvNo/ผู้รับ/ยอด). แมตช์แบบ manual โชว์ในถัง "แมตช์แล้ว" ป้าย **"จับคู่เอง"** + ปุ่มยกเลิกคืน (undo ของ matched = ลบ reconState เฉยๆ ไม่มี forecastEntries ให้เก็บกวาด).
- **verify (preview, brReconcile ด้วยเลขจริง):** ก่อนแก้ `recorded:1,unmatchedPv:1` (= อาการเป๊ะ) → หลังจับคู่ `matched:1,unmatchedPv:0,recorded:0,missing:0` (`via:manual`, pvNo ถูก) · จับด้วย pvNo ก็ได้ · `pvRef` roundtrip ผ่าน serialize · ไฟล์คอมไพล์ผ่าน ไม่มี console error.

## 2026-06-17 — Cashflow: ตารางประมาณการรายสัปดาห์ (Section 01) รายจ่าย = "คงเหลือต้องจ่าย" (หักจ่ายจริงแล้ว) (build `page_cashflow 20260617h`)
- **ตามคำขอผู้ใช้:** หน้า `#cashflow` ตาราง **"ประมาณการรายสัปดาห์" (Section 01 — ด้านบน)** ช่อง **"กระแสเงินสดออก" (Outflow Details)** เดิมโชว์ประมาณการ**เต็ม** (เช่น สัปดาห์ปัจจุบัน W3 = 16.5M) ทั้งที่จ่าย Actual ไปแล้วบางส่วน (2.8M) → อยากให้ **หักยอดที่จ่ายจริงไปแล้ว แสดงยอดคงเหลือประมาณการ** (13.7M). **"ติดตามจ่ายจริงรายสัปดาห์" (Section 02 — ด้านล่าง)** ที่โชว์ Forecast vs Actual คู่กัน = **ถูกต้องแล้ว ไม่แตะ**.
- **FIX (ฝั่งแสดงผลล้วน — ไม่เขียน/ไม่ sync):**
  - เพิ่ม memo **`forecastRemainingByWeekCat`** = `max(0, forecastByWeekCat[w][cat] − pvActualByWeekCat[w][cat])` (ราย week × cat, floor 0) — วางหลัง `pvActualByWeekCat`.
  - **`planOut`** (ตัวป้อนแถวรายจ่าย Section 01) เปลี่ยนจาก `forecastByWeekCat` → **`forecastRemainingByWeekCat`** (mirror ฝั่งรับเงิน `ivCombinedByWeek = ivInflowByWeek.forecastRemaining` ที่ทำแบบนี้อยู่แล้ว).
  - **เหตุผลทางคณิตศาสตร์ (ไม่ใช่แค่ความชอบ UI):** `weekBF` ของสัปดาห์ปัจจุบัน = **ยอดเงินสดสด (liveAvailable)** ซึ่ง**สะท้อนเงินที่จ่ายไปแล้ว**อยู่แล้ว → ถ้าหักประมาณการเต็มอีก = **นับซ้ำ** → `netEndOfCurrentWeek`/`netEndOfMonth`/"ยอดคงเหลือสุทธิปลายงวด" ต่ำเกินจริง. หักเฉพาะ "คงเหลือ" จึงถูกต้องกว่า (เหมือนเหตุผลฝั่ง IV ที่ comment ไว้: เงินที่รับแล้วอยู่ในยอดยกมาแล้ว).
  - **`outflowForecast` (KPI การ์ด) + Section 02 (ติดตามจ่ายจริง) + Grand Total ยังใช้ `forecastByWeekCat` เต็ม** — ตัวเลขบน(คงเหลือ)/ล่าง(เต็ม) จึงต่างกัน**โดยตั้งใจ**.
  - **Drill-down ช่องรายจ่าย** (`openDrillDown('out…')`): แนบ `outRecon = {forecast, actual, remaining}` (คำนวณด้วย `currentRestSplit` จาก 3 grid ให้ตรง period) → modal โชว์กล่องสรุป 3 ช่อง **ประมาณการเต็ม − จ่ายจริงแล้ว = คงเหลือต้องจ่าย** (= ตัวเลขในช่อง) แทน "ยอดรวม" เดิม + โน้ตชี้ไปดู Actual รายตัวที่หัวข้อ 02. **และตัด `EXPENSE_TYPE='BANK_RECON'` ออกจาก drill 'out'** ให้ตรงกับ `forecastByWeekCat` (เดิมไม่ตัด — mismatch เล็กๆ ที่มีอยู่ก่อน).
  - ป้ายหัวแถว Outflow ใน Section 01 เพิ่ม "· ยอด**คงเหลือต้องจ่าย** (หักที่จ่ายจริงแล้ว)".
- **verify (preview, render `CashFlowDashboard` จริงด้วย mock W3: forecast 16.5M + actual 2.8M):** Section 01 แถวรายจ่ายหมวด 1 / รวมรายจ่าย ช่อง W3 = **(13,700,000)** ✓ · Section 02 การ์ด W3 = Forecast **16,500,000** / Actual **2,800,000** ✓ (ไม่เปลี่ยน) · คลิกช่อง → drill recon = เต็ม 16.5M − จ่ายจริง 2.8M = คงเหลือ 13.7M ✓ · ไม่ crash (ErrorBoundary ไม่เด้ง) · ไม่มี console error.

## 2026-06-17 — Supabase `selectAll` เพิ่ม `.order('id')` + ลำดับบัญชีจัดเอง (manual reorder) — (build `data_supabase 20260617s2` / `?v=20260617c` · `components 20260617m` · `page_daily_balance 20260617a` · `page_bank_diary 20260617g`)
- **อาการ (ผู้ใช้):** บัญชี SCB x4863 (เลข `1362684863`) ที่อยู่ลำดับ **#1 มาตลอด** ในหน้า **บันทึกยอดธนาคาร** (`#daily_balance`) อยู่ๆ **เด้งลงไป #7** — และลำดับ # นี้มีผลกับหน้า **Bank Diary** ด้วย (ทั้งสองหน้าแสดงบัญชีตาม index ของ array `data.bankAccounts` ตรงๆ ไม่มี sort).
- **ROOT CAUSE = `selectAll` ใน `data_supabase.js` ดึงด้วย `.range()` โดยไม่มี `.order()`.** `SELECT` ที่ไม่มี `ORDER BY` ใน Postgres คืนแถวตาม heap order ซึ่ง**ขยับเมื่อแถวถูก UPDATE** → พอกด "บันทึกยอด" บัญชี 4863 (upsert) แถวมันถูกเขียน tuple ใหม่ไปท้าย heap → รอบโหลดถัดมาโผล่ท้ายสุด = #7. (เดิม doc เขียน entry นี้ไว้แต่ **โค้ดจริงยังเป็น `20260616s1` ไม่เคยมี `.order()`** — fix นี้คือตัวจริงที่ลง code.)
- **FIX 1 — `.order('id', { ascending: true })` ก่อน `.range()` ใน `selectAll`** → ลำดับแถวคงที่ทุกครั้ง. ยังจำเป็นต่อ **ความถูกต้องของ pagination** ของ entity ใหญ่ (debtLedger ~4000 แถว เกิน PAGE=1000) ไม่ให้แถวซ้ำ/หายข้ามหน้า. (realtime `applyRealtime` แทนที่แถวแบบ in-place อยู่แล้ว → UPDATE ไม่ขยับลำดับใน session).
- **FIX 2 — ลำดับบัญชี "จัดเอง" (เพราะ `.order('id')` นิ่งจริงแต่ = id-order ไม่ใช่ลำดับที่ทีมเคยจัดใน Sheet — Postgres ไม่มีลำดับแถวในตัว ลำดับแบบ Sheet หายตอนย้าย backend):**
  - เก็บลำดับเป็น **JSON array ของ Bank_AC** ใน `manualOverrides` key **`'bankSortOrder'`** (sync ทั้งทีม, ไม่แตะแถว entity) ผ่าน `WTPOverride.setRaw`.
  - **helper กลางใน [components.jsx](app/components.jsx)** (global, โหลดก่อนทุก page): `wtpBankSortOrderFrom(manualOverrides)` (parse), `wtpSortBankAccounts(list, acOf, manualOverrides)` (เรียง stable — ตัวที่ไม่อยู่ในลำดับคงตำแหน่งเดิม → ต่อท้าย), `wtpMoveBankAccount(acNo, dir, groupAcs, allAcs, manualOverrides)` (สลับ 2 ตัว แล้ว setRaw).
  - **★ GOTCHA timing:** helper อ่านลำดับจาก **`data.manualOverrides` (prop ที่ส่งเข้ามา) โดยตรง** — **ห้ามอ่านผ่าน `WTPOverride.get`** เพราะ `_load()` merge `{...local, ...cloud}` แล้ว cloud (`window.__wtpData`) อัปเดตใน effect *หลัง* commit ขณะที่ memo คำนวณตอน render → จะได้ค่าเก่าค้าง 1 จังหวะ (คลิกแล้วลำดับไม่ขยับ). prop = React state ที่สดตอน render เสมอ.
  - **UI:** [page_daily_balance.jsx](app/page_daily_balance.jsx) เพิ่มปุ่ม **▲▼** ในเซลล์ `#` (เฉพาะ `canEdit` = manager/staff) ทั้งตารางหลัก+สำรอง → `moveAccount(ac, dir, group)` → sync. ทั้ง daily_balance (`accountsByType` memo) และ [page_bank_diary.jsx](app/page_bank_diary.jsx) (`accounts` memo) เรียงด้วย override ตัวเดียวกัน → **เลข # ตรงกันทั้งสองหน้า**.
  - หน้าอื่นที่โชว์ bankAccounts (Home bank card / DATA BANK grid / page_daily) ยังเรียงแบบ id-order (นิ่งแล้วจาก FIX 1 แต่ไม่ตามลำดับจัดเอง) — ขยายให้เรียงตาม override ได้ภายหลังถ้าต้องการ (ใช้ helper เดิม).
- **verify (preview):** ไม่มี Babel error + React mount ปกติ · `wtpSortBankAccounts` — ไม่มี override=คงลำดับเดิม / pin 4863=ขึ้น #1 / override บางส่วน=4863 ขึ้นบน ที่เหลือ stable · `wtpMoveBankAccount` — เลื่อนขึ้นสลับถูกตัว / ตัวแรกเลื่อนขึ้น=return false ไม่เขียน. (ปุ่มจริงบนหน้า render กับข้อมูลสด verify ไม่ได้ในเพรวิว เพราะ RLS บล็อกอ่านถ้าไม่ล็อกอิน).

## 2026-06-17 — HOLD รายวัน + "เงินสดใช้ได้/ยอดยกมา" อิง snapshot รายวัน (หลักการ: ใช้ได้ = เงินวันนั้น − HOLD วันนั้น) — build `page_cashflow 20260617q` / `page_daily_balance 20260617c` / `data_supabase 20260617d`
- **อาการ (ผู้ใช้):** หน้า `#cashflow` การ์ด **"เงินสดใช้ได้ปัจจุบัน" ติดลบ −1.8M** + การ์ด **"ยอดยกมา B/F" โชว์ 0** — ทั้งที่หน้า **บันทึกยอดบัญชี** มียอดจริง.
- **ROOT (2 ชั้น):** (1) cashflow อ่าน field `bankAccounts.BALANCE`/`HOLD_AMOUNT` ที่**ค้างค่าต้นเดือน** (BALANCE = ยอด ณ วันที่บันทึกล่าสุดของ field นั้น ซึ่ง saveOne/saveAll เขียนทับด้วยวันที่บันทึกล่าสุด รวมแก้วันย้อนหลัง) — ไม่ใช่ยอดบันทึกรายวันใน `cashflowSnapshots`. (2) B/F เอา **เงินต้นเดือน − HOLD วันนี้** (คนละวัน): HOLD ต้นเดือน ~2.7M แต่วันนี้ 6M → ติดลบผิด แล้วโดน `Math.max(0,…)` ปัดเป็น 0.
- **หลักการที่ผู้ใช้ยืนยัน:** **ยอดใช้ได้ของวันใดๆ = เงินวันนั้น − HOLD วันนั้น** → ทั้ง B/F (ต้นเดือน) และ ปัจจุบัน (วันล่าสุด) ต้องจับคู่ balance↔HOLD ของวันเดียวกัน. **HOLD จึงต้องเก็บรายวัน.**
- **FIX `page_daily_balance` (build c):** เก็บ `hold` ลง `cashflowSnapshots` **รายวัน** (เพิ่มใน saveOne + saveAll). คอลัมน์ใหม่ **"HOLD ล่าสุด | HOLD วันนี้"** (เหมือน ยอดเมื่อวาน|วันนี้) — `latestHold` (snapshot วันก่อน, fallback `HOLD_AMOUNT` + ป้าย "ค่าตั้งต้น"), ▲▼ ส่วนต่าง, footer รวม `totalLatestHold` (colSpan 10→11). `holdDraft` ยกค่าล่าสุดมา (วันนี้→ล่าสุด→HOLD_AMOUNT), deps `[entryDate, accounts.length, snapshots.length]`.
- **FIX `page_cashflow` (build q):** เพิ่ม `getHoldAtDate(snapshots, dateISO)` (คู่ `getBalanceAtDate` — รวม `hold` ของ snapshot ล่าสุด≤วันที่ต่อบัญชี). **`liveBalance`/`liveHold` = ยอด+HOLD วันล่าสุดต่อบัญชีจาก snapshot** (memo, fallback BALANCE/HOLD_AMOUNT) → "ใช้ได้ปัจจุบัน" = วันล่าสุด, ไม่พึ่ง field ที่ค้าง/ถูกทับ. **B/F = `getBalanceAtDate(bfCutoff) − getHoldAtDate(bfCutoff)` โดย `bfCutoff = วันที่ 1 ของเดือน`** (เลิกใช้ `openingBalanceAt` cutoff สิ้นเดือนก่อน, เลิกปัด 0) → ตรงกับ "ใช้ได้จริง" ของวันที่ 1 ในหน้าบันทึกยอดเป๊ะ.
- **verify (preview, login=patima):** B/F = **1,399,283** (=วันที่1: 4,099,283 − HOLD 2,700,000) · ใช้ได้ปัจจุบัน = **14,932,531** (=วันล่าสุด: 20,932,531 − HOLD 6,000,000) · ตรงหน้าบันทึกยอด · แก้ HOLD วันที่ 1 แล้ว B/F ขยับตาม · หน้าบันทึกยอด 11 คอลัมน์ มี HOLD ล่าสุด|วันนี้ · ไม่มี console error (cashflow). **end-to-end mock:** บัญชีมีประวัติ→"HOLD ล่าสุด 500,000·16/06" + ยกค่ามาช่องวันนี้; ไม่มีประวัติ→"ค่าตั้งต้น".
- **`data_supabase 20260617d` (งานก่อนหน้า, รวม push):** `onAuthStateChange` จับ `INITIAL_SESSION` (เปิดแท็บที่ session ค้าง → supabase-js restore JWT) → `loadFromServer()` ซ้ำให้ผ่าน RLS (first load ยิงก่อน JWT พร้อม = อ่านไม่ได้).
- **⚠️ ค้าง (รู้แล้ว, ยังไม่แก้ — ผู้ใช้รับทราบ):** `saveOne`/`saveAll` ยัง set `bankAccounts.BALANCE`/`HOLD_AMOUNT` = ค่าของ "วันที่กำลังบันทึก" เสมอ → **แก้วันย้อนหลังจะเขียนทับ field "ปัจจุบัน"**. Cashflow + Daily Balance **ไม่กระทบ** (อ่าน snapshot รายวันตรงๆ แล้ว) แต่หน้าอื่นที่อ่าน `BALANCE`/`HOLD_AMOUNT` ตรงๆ อาจเพี้ยนชั่วคราว (self-heal เมื่อบันทึกวันปัจจุบัน). harden = ตอนบันทึกให้ตั้ง field = snapshot วันล่าสุด (ไม่ใช่วันที่กำลังแก้).

## 2026-06-18 — Bank Diary: หัวกลุ่มวันโชว์ ↑รับรวม · ↓จ่ายรวม (build `page_bank_diary 20260618a`)
- **ตามคำขอผู้ใช้:** หัวกลุ่มวัน (`BDDayGroup` ในการ์ดบัญชี) เดิมโชว์แค่ "N รายการ" + **net** (+/−) + คงเหลือสะสม → วันที่มีทั้งรับทั้งจ่าย เห็น net เป็นบวกแต่ดู "ยอดจ่ายสุทธิรวม" ไม่ได้. เพิ่มบรรทัดย่อยใต้ "N รายการ" = **↑{รับรวม}** (เขียว) · **↓{จ่ายรวม}** (แดง) — คำนวณจาก `day.items` ที่มีอยู่: `dayIn = Σ signed>0`, `dayOut = Σ −signed<0`. โชว์เฉพาะฝั่งที่ >0 (ไม่โชว์ 0). net เดิมยังอยู่บนขวา. ฝั่งแสดงผลล้วน (ไม่แตะ compute/sync). render จริงที่บรรทัด ~1127 (`visibleGroups.map(... <BDDayGroup/>)`). verify: logic dayIn/dayOut ถูก + Babel compile ผ่าน (preview ไม่มีข้อมูลจริงเพราะ RLS — verify ด้วย DOM/eval).

## 2026-06-18 — Bank Diary: ปุ่ม "📥 Export Excel" ในพาเนล AP (ดึงรายการที่วางแผนจ่ายแล้วออก Excel) (build `page_bank_diary 20260618b`)
- **ตามคำขอผู้ใช้ ("รายการที่เตยวางแผนจ่ายแล้ว อยากดึงออก Excel"):** เพิ่มปุ่ม **📥 Export Excel** (สีเขียว) ในแถบสถานะของ `BDApPanel` (พาเนล "เจ้าหนี้ต้องจ่าย (AP)" หน้า Bank Diary). **Export = WYSIWYG ตามที่กรองอยู่** (`rows` = หลัง search/สถานะ/ช่วงวันที่/filter รายคอลัมน์ + sort) → กดตัวกรอง **"วางแผนแล้ว"** ก่อนแล้ว Export = ได้เฉพาะรายการที่วางแผนจ่ายแล้ว (ป้าย count ข้างปุ่มบอกจำนวนที่จะ export, โปร่งใส).
- **ใช้ตัวช่วยกลางที่มีอยู่แล้ว `exportRowsToExcel(rows, cols, opts)` (global จาก [components.jsx](app/components.jsx))** — ไม่ต้องเขียน XLSX เอง. cols 8 ช่อง: วันจ่าย(วางแผน) [`plannedDateByRef[vchno]`→fmtDate] · ครบกำหนด · ผู้ขาย · เลขที่ AP · ประเภท CF [`code + bdCatLabel`] · **ยอดสุทธิ (`type:'number'` → SUM ได้ใน Excel)** · สถานะ [วางแผนจ่ายแล้ว/ยังไม่วางแผน] · หมายเหตุ. filename `AP-<scope>_<วันที่>.xlsx`, sheet "รายการจ่าย", มี title row สรุป (scope · จำนวน · รวมยอด). ปุ่มโชว์ทุก role ที่เห็นพาเนล (read-only action; bank screens ซ่อนจาก owner/viewer อยู่แล้ว).
- **verify (isolated render ใน preview + ดัก `XLSX.writeFile`, mock 3 รายการ/วางแผน 2):** Export "ทั้งหมด" → 3 แถว, ยอดเป็น number, วันที่ DD/MM/YYYY ค.ศ., สถานะ+วันจ่ายถูกต้อง · กด "วางแผนแล้ว" → Export เหลือ **2 แถว** (AP-001/AP-003) + วันจ่ายที่วางแผน · ไม่มี error. (RLS บล็อกข้อมูลจริงตอนไม่ล็อกอิน → verify ด้วย mock + DOM/eval.)

## 2026-06-18 — Weekly Forecast Section 01: โหมด "แผนจ่ายจริง (AP)" ที่ช่องรายจ่าย (build `page_cashflow 20260618r`)
- **Port จาก BIOAXEL** — เหมือนเป๊ะ ต่างแค่ localStorage key `wtp-cf-s01outmode` (BIO ใช้ `bio-cf-s01outmode`).
- **toggle 2 โหมดที่หัวข้อ "2: กระแสเงินสดออก"** (`s01OutMode` ∈ `'remaining'|'apPlan'`, default `'remaining'` = พฤติกรรมเดิมเป๊ะ):
  - **`remaining` (เดิม):** `forecastRemainingByWeekCat` = ตั้งมือ − จ่ายจริง (ไม่แตะ).
  - **`apPlan` (ใหม่):** `apPlanCombinedByWeekCat` = **`apPlanByWeekCat`** (forecastEntries `EXPENSE_TYPE='AP'`, PLANNED, `REF_DOC ∉ paidApSet` — ตัด AP ที่จ่ายผ่าน PV แล้ว) **+ `manualIncludedByWeekCat`** (ตั้งมือที่ user ติ๊กเลือกรวมเอง).
- **กลไกเลือกรวมตั้งมือ = ติ๊กราย entry ผ่าน `WTPOverride cf.sec1Inc.<feId>`** (helper `cfSec1IncKey`, sync ทั้งทีม) — default ไม่รวม. กดที่ช่องในโหมด apPlan → `openApPlanDrill` → modal โชว์ 3 ช่องสรุป + ตาราง AP (read-only) + ตาราง ตั้งมือ (ติ๊กได้). **ไม่รวมทุกรายการตั้งมือเพราะตั้งมือนับใน Section 02 ยอด FORECAST อยู่แล้ว** — ติ๊กรวมเฉพาะที่ต้องการ (กันนับซ้ำ).
- **Section 02 + KPI การ์ด ไม่กระทบ** (ยังใช้ forecast เต็ม).
- `cfSec1IncKey` constant (หลัง `cfPvCatKey`): `const cfSec1IncKey = (id) => 'cf.sec1Inc.' + String(id || '').trim();`
- **(divergence จาก BIO):** WP ใช้ UI toggle แบบ **pill** + grid filter ง่ายกว่า (เช็ค `STATUS==='PLANNED'`); BIO ใช้ segmented control + filter เข้ม (LOAN/BANK_RECON/sign<0). ตัวเลขสำหรับข้อมูลจริงเท่ากัน (Bank Diary เขียน STATUS=PLANNED + AMOUNT ติดลบเสมอ).

## 2026-06-18 — Weekly Forecast Section 01: เพิ่ม "ขอบเขตสัปดาห์ปัจจุบัน" + ซ่อนปุ่มตอนนำเสนอ (build `page_cashflow 20260618s`)
- **คำขอผู้ใช้ (เตย, ต่อจากด้านบน):** (1) ในโหมด **แผนจ่ายจริง (AP)** บางครั้งอยากเห็น**เฉพาะสัปดาห์ปัจจุบัน** (2) **ปุ่มสลับต้องไม่ขึ้นตอนโหมดนำเสนอ**.
- **(1) sub-toggle ขอบเขต** (`s01ApScope` ∈ `'month'|'week'`, localStorage `wtp-cf-s01apscope`, default `'month'`, **แสดงเฉพาะเมื่อ `s01OutMode==='apPlan'`**): `'week'` → `apPlanScopedByWeekCat` = zero ทุกสัปดาห์ยกเว้น `nowWeek` → ผ่าน `currentRestSplit` ได้ current = สัปดาห์ปัจจุบัน, **rest = 0, total = current เท่านั้น**. `_outGrid` ใช้ `apPlanScopedByWeekCat`. **drill** (`openApPlanDrill`) เพิ่ม helper `wkOK(d)` scope-aware: `'week'` → ทุก period เห็นเฉพาะ `nowWeek`.
- **(2) ซ่อนปุ่มตอนนำเสนอ** = ใส่ **`className="no-present"`** ที่ `<span>` ปุ่มสลับ (CSS เดิม `body.present-mode .no-present { display:none !important }`). subtitle ยังโชว์ (+ " · เฉพาะสัปดาห์นี้" เมื่อ scope=week).
- **verify (preview, isolated render `CashFlowDashboard` จริง + mock AP 2 สัปดาห์):** month: current=100k/rest=50k/total=150k → คลิก "เฉพาะสัปดาห์นี้": **current=100k/rest=0/total=100k** (AP สัปดาห์ถัดไป 50k ถูกตัด) ✓. compile ผ่าน (ไม่มี Babel error). present-mode + scope-only-in-apPlan verify บนฝั่ง BIO (โค้ดเดียวกัน).

## 2026-06-18 — การ์ด IV กรองเฉพาะ invType='P' (โครงการ) ตัด 'O' (อื่นๆ) ออก (build `page_cashflow 20260618x`)
- **ตามคำขอผู้ใช้:** "รายการที่ดึงมาส่วนการ์ดรับเงินโครงการ ต้องเป็น ประเภทงานโครงการเท่านั้น" — เพิ่ม `ivIsProject(iv)` helper (`String(iv.invType || iv.invtype || 'P').trim().toUpperCase() !== 'O'`) และกรองทุกจุดที่คำนวณ KPI/modal IV:
  - `ivInflowByWeek` (KPI Plan+Actual numbers): กรองหลัง bySafe entry (bySafe ยังมีทุก IV — ไว้ lookup lock เก่า), ก่อนคำนวณ forecast/actual
  - `doLockIvPlan` (ล็อกแผน baseline): กรองก่อนประมวลผล
  - `IvPlanDrillModal.planItems` locked branch: กรองใบที่ `iv && !ivIsProject(iv)` (ป้องกัน lock เก่าที่อาจมี 'O'); non-locked branch: กรองต้น forEach
  - `IvPlanDrillModal.actualItems`: กรองต้น forEach
- **ทำใน BIOAXEL ด้วย** (`page_cashflow.jsx?v=20260618p`) — โค้ดเหมือนกันทุกจุด.

## 2026-06-18 — การ์ด "รับเงินโครงการ (IV)" คลิกได้ → modal รายละเอียด IV (build `page_cashflow 20260618w`)
- **ตามคำขอผู้ใช้:** การ์ด **"รับเงินโครงการ (IV)"** (Section KPI แถวล่าง) เดิมโชว์แค่ Plan vs Actual + % → **คลิกชื่อการ์ดได้** (ชื่อมี `›` + underline dotted) เปิด **`IvPlanDrillModal`** แสดงรายละเอียด IV แยก 4 tab.
- **`IvPlanDrillModal`** (function ก่อน `PlanVsActualCard`): รับ props `invoices/ivPlanLock/ivForecast/ivActual/financeByCode/weeks/year/month/monthNames/onClose`. **3 KPI บน:** แผน · รับจริงแล้ว · ▲เกินแผน/▽ขาดแผน. **4 tabs:**
  - **แผนทั้งหมด** — ทุกใบที่วางแผนรับเดือนนี้ (จาก ivPlanLock.items ถ้าล็อก / else liveForecast), sort วันคาดรับ; คอลัมน์ #/IV No/โครงการ/ลูกค้า/แผน฿/วันคาดรับ/สถานะ (badge + วันรับจริงถ้ารับแล้วในเดือน)
  - **รับแล้ว ✓** — IV ที่ `ivActualReceiveDate(iv)` ตกในเดือนนี้, sort วันล่าสุดก่อน; คอลัมน์ IV/โครงการ/ลูกค้า/รับจริง฿/วันรับจริง
  - **ยังไม่รับ** — planItems ที่ `!ivIsPaid(iv)`
  - **⚡ นอกแผน** — actualItems ที่ `!inPlan` = รับจริงแล้วแต่ไม่ได้อยู่ในแผน (= ต้นเหตุที่ Actual > Plan "เกินแผนมา"); มีป้าย "นอกแผน" แดง
  - แต่ละ tab มี **footer รวมจำนวนใบ + ยอดรวม**
- **`PlanVsActualCard`** ได้ prop `onClick` (optional): ชื่อการ์ดกลายเป็น `cursor:pointer` + `underline dotted` + `›` suffix + `title="คลิกเพื่อดูรายละเอียด"` เมื่อมี `onClick`; ถ้าไม่มี = พฤติกรรมเดิม. เฉพาะ IV card ที่ผูก `onClick={() => setIvDrill(true)}`.
- **State `ivDrill`** (`cfState(false)`) ใน `CashFlowDashboard`; modal render ก่อน detailItem modal.
- **ทำใน BIOAXEL ด้วย** (`page_cashflow.jsx?v=20260618o`) — โค้ดเหมือนกันทุกจุด.
- **verify:** modal เปิด/ปิด ✓, 4 tabs ✓, ไม่มี console error ✓, `IvPlanDrillModal`/`PlanVsActualCard` defined ✓.

## 2026-06-19 — fix(projects): synthetic code limit 36→120 — กัน 2 โครงการชื่อยาวถูก merge เป็น 1
- **อาการ:** อัปโหลดไฟล์ที่มี 2 โครงการชื่อยาวเหมือนกันแต่ต่างเฉพาะท้าย (เช่น "…(จุดที่ 1)" / "…(จุดที่ 2)") → ระบบดึงเข้าแค่ 1 รายการ.
- **ROOT CAUSE:** `_clean()` ใน `parseProjectControl` (`pc_engine.jsx:1010`) ตัดชื่อเหลือ **36 ตัวอักษร** ก่อนสร้าง synthetic code (`WS-<ปี>-<ชื่อ>`) → ชื่อไทยยาวๆ ที่ต่างกันตรงท้ายได้ key เดียวกัน → Phase 2 (`byCode`) merge เป็น 1 โครงการ.
- **FIX:** `slice(0, 36)` → `slice(0, 120)` — รองรับชื่อยาวได้สบาย synthetic code ยังเป็น text ธรรมดา ไม่มี constraint ความยาว (Postgres `text` PK). **ไม่กระทบ id ของโครงการที่มีเลขสัญญาจริง** (path แยกกัน).
- **⚠️ หลังแก้: ต้องอัปโหลดไฟล์ใหม่อีกครั้ง** — โครงการที่เคยถูก merge เป็น 1 จะถูกสร้างเป็น 2 รายการ (id ใหม่); Finance Master (`pcfin.*`) ของโครงการเดิมไม่ติดมาด้วย (ต้องกรอกใหม่).

## 2026-06-19 — cf-print-mode: เลย์เอาท์เหมือนโหมดนำเสนอ (build `page_cashflow 20260619c` / `styles.css 20260619e`)
- **ปัญหา:** ผลลัพธ์ PDF ไม่สวย เลย์เอาท์และสีพื้นหลังการ์ดไม่ตรงกับโหมดนำเสนอ.
- **แนวทางใหม่:** ปุ่มพิมพ์ใน `page_cashflow.jsx` เพิ่ม **ทั้ง `present-mode` และ `cf-print-mode`** พร้อมกัน → `present-mode` จัดการ card styling ที่สวยงาม (`border-radius:20px`, box-shadow แบบนำเสนอ, ซ่อน sidebar/no-present) — เหมือนกับที่เห็นบนจอตอนนำเสนอจริง; `cf-print-mode` override เฉพาะ print-specific: compact padding, ซ่อน topbar, แสดง gradient header, zoom 0.78, A4 page.
- **CSS simplification:** ลบ rule ที่ duplicate กับ present-mode ออก (`border: 1px solid`, `border-radius: 12px`, sidebar hiding, etc.) — ตอนนี้ cf-print-mode เป็นแค่ "print overrides on top of present-mode" ไม่ใช่ full standalone mode.
- **Verify (preview):** `heroCard_bg=linear-gradient ✓` / `border-radius=20px ✓` / `printHdr=flex ✓` / `topbar=none ✓` / `pageHead=none ✓` / `grandCard_bg=#0f244d ✓`.

## 2026-06-19 — cf-print-mode: แก้การ์ดไม่มีสีพื้นหลัง (ROOT CAUSE: background !important ทับ inline gradient) — build `styles.css 20260619d`
- **ROOT CAUSE:** `body.cf-print-mode .cf-page .card { background: var(--surface) !important; }` ใน cf-print-mode block ทับ inline style `style={{ background: 'linear-gradient(...)' }}` ของ `BalanceCard` — CSS `!important` ชนะ inline style ทุกกรณีถ้า specificity เท่ากัน → การ์ด hero (B/F + ยอดใช้ได้ปัจจุบัน) โชว์สีพื้นขาว ไม่มี gradient.
- **FIX:** ลบ `background` และ `padding` ออกจาก rule `.card` ทั่วไปใน cf-print-mode → แต่ละการ์ดควบคุม background ตัวเอง (inline gradient ปรากฏปกติ). `.grid-2 .card` ยังมี `padding: 10px 14px !important` (เฉพาะ hero cards) + `.grid-3 .card` มี `padding: 6px 10px !important`. Rule ทั่วไปเหลือแค่ `box-shadow`/`border`/`border-radius`.
- **Pattern:** ถ้า `card` ใดมี inline `background: linear-gradient(...)` ห้ามมี CSS rule `background: ... !important` ครอบ `.card` ทั่วไป — ต้องระบุ selector เฉพาะ (เช่น `.cf-grand-card` ที่ต้องการ `background: #0f244d !important`).

## 2026-06-20 — หน้าใหม่ "พรีเซนต์ Cash Flow" (#cashflow_present) — Executive Cash Flow Dashboard พอร์ตจาก BIO ป้อนด้วยไฟล์ของเตย (build `page_cashflow_present 20260620h` / `styles.css 20260620b`)
- **คือหน้าอะไร:** พอร์ต `page_cashflow_present.jsx` ของ BIOAXEL (Executive Cash Flow Dashboard 4 แท็บ: 📊 ภาพรวม · 🔬 สรุปกิจกรรม · 📑 งบกระแสเงินสด · 🔎 รายการ) มาเป็นหน้าใหม่ของ POG. **ไม่ใช่หน้าเดียวกับ Weekly Forecast (`#cashflow`)** — นี่เป็นแดชบอร์ดผู้บริหารแยกต่างหาก ป้อนด้วยการ **อัปโหลด Excel** (ไม่ดึงจาก live app data) เพราะข้อมูลที่จัดหมวด **กิจกรรม op/inv/fin** มีเฉพาะในไฟล์ Cash Flow ของเตย ไม่มีในตาราง entity ของแอป. UI/คอมโพเนนต์ (waterfall SVG, monthly grouped bars, KPI, explorer, modal) = เหมือน BIO เป๊ะ. prefix `cfp`/`Cfp`. self-contained (พึ่ง `window.React` + `window.XLSX`).
- **★ ข้อมูลมาไม่เหมือน BIO — จุดที่ต่าง (ทั้งหมดอยู่ในพาร์ส):** BIO อัป **2 ไฟล์** (STM รายการเดินบัญชี + งบสรุป) คอลัมน์แบบ Mango. POG อัป **ไฟล์เดียว** = `WTP-Cash Flow 20XX.xlsx` ของเตย → อ่านชีต **`DATA`** (header-driven, `cfpParseStm` ที่เขียนใหม่ → `cfpFindHeaderRow`/`cfpDataColMap`): คอลัมน์ `BANK · ACCOUNT NO. · Department · Document No. · DD/MM/YYYY · Vender · Description · Amount · รหัสค่าใช้จ่าย · Account Code · สูตร · ประเภทกิจกรรม · รับ-จ่าย · เดือน`.
  - **`flow` = คอลัมน์ `Amount` ตรงๆ** (วงเล็บ = ติดลบ/จ่าย) — **source of truth** (sum = ฿4,637,974.85 ตรงกับชีต REPORT เป๊ะ; verify แล้ว). **★ ห้าม override sign ด้วยคอลัมน์ "รับ - จ่าย"** — มี 4 แถวที่ป้ายทิศทางไม่ตรงเครื่องหมายยอด (เช่น credit memo ยอด+แต่ป้ายจ่าย) ถ้า flip ตามป้ายยอดรวมเพี้ยน 119,134 จาก REPORT. (เคยใส่ reconcile แล้วถอดออกตอน verify.)
  - **กิจกรรม (`cfpActKey`)** อ่านจากชื่อ ไม่ใช่เลข: ประเภทกิจกรรม POG = `01.กิจกรรมดำเนินการ`(op) / `02.กิจกรรมจัดหาเงิน`(fin) / `03.กิจกรรมลงทุน`(inv) — regex แก้เป็น `/ดำเนิน/`→op (POG ใช้ "ดำเนินการ" ไม่ใช่ BIO "ดำเนินงาน"), `/จัดหา/`→fin, `/ลงทุน/`→inv. **หมวด** = คอลัมน์ `Account Code` (ชื่อไทย).
  - **เงินสดต้นงวด** ไม่อยู่ในชีต DATA → `cfpOpeningFromAoa` อ่าน Σ คอลัมน์ "ต้นงวด" ของชีตเดือนแรกสุดที่มี (Jan./Feb./…) = ฿70,617,582.77 (22 บัญชี). ending = opening + net.
  - **งบกระแสเงินสด (แท็บ 3) "สังเคราะห์จากรายการ"** — POG ไม่อัปไฟล์งบแยก; `cfpBuildModel(stm, catGroups)` สร้าง `model.stmt` เอง (section→leaf→net รายกิจกรรม→grand net/opening/ending). leaf ผูก **`catNames` (array)** → กดแถวเปิดรายการตรงๆ. ยอดงบ tie กับ REPORT (เวลโกร 22.89M / คอมมิชชั่น 9.24M ฯลฯ).
  - **★ สีไล่ระดับชั้น + แถบสีซ้ายแยกรับ/จ่าย (`20260620h`)** — เพื่อ "ดูกลุ่มง่าย": แต่ละ row พก `side` ('in'/'out'); CfpStatementTable ทำ (a) **bg ไล่เข้ม**: section `#cdddf7` > grand > net > หัวรับ-จ่าย `#e8f0fb` > รวม > codegroup `#f4f8fe` > leaf ขาว; (b) **แถบสีซ้าย (borderLeft 4px)** = เขียว(`C.pos`)ฝั่งรับ / แดง(`C.neg`)ฝั่งจ่าย บนหัวรับ-จ่าย·รวม·codegroup (เข้ม) + leaf (จาง rgba) → เห็นบล็อกกลุ่มชัด; (c) **สีตัวอักษร**หัวรับ-จ่าย+รวม = เขียว/แดงตามฝั่ง, ▾/▸ ของ codegroup = สีฝั่ง; (d) borderTop: net 2px brand · รวม 1.5px สีฝั่ง · codegroup 1px เส้นคั่น. ดูกลุ่มง่ายสุดเมื่อกด "ย่อทุกกลุ่ม" (เห็นยอดรวมรายกลุ่มรหัส). verify ไฟล์จริง: รับ=เขียว/จ่าย=แดง, รวมรับ 32.87M เขียว, ย่อ/กางทำงาน.
  - **★ ย่อยตามรหัสค่าใช้จ่าย 5 หลัก + คอลัมน์รหัส + ย่อ/ขยาย (`20260620g`)** — เพิ่มชั้น **codegroup**: ในแต่ละฝั่ง รับ/จ่าย จัดรายการย่อยเป็นกลุ่มตาม**รหัส 5 หลัก** (10001.01/.02 → กลุ่ม "10001") ชื่อกลุ่มจาก **chart of accounts**. โครง 4 ชั้น: section → group(รับ/จ่าย หัว) → **codegroup(รหัส 5 หลัก + ยอดรวมกลุ่ม)** → leaf(รหัสเต็ม). **ชื่อกลุ่มอ่านจากชีต "ค่าใช้จ่าย" ในไฟล์ตอนอัปโหลด** (`cfpParseCodeGroups`: รหัส 4-6 หลักไม่มีจุด + ชื่อเซลล์ถัดไป) → แก้ในไฟล์แล้ว re-upload ได้ ("เผื่อปรับแก้ไข"); fallback = `CFP_CODE_GROUPS` (ฝัง 63 กลุ่ม). เก็บใน `stored.codeGroups`, ส่งเป็น param ที่ 3 ของ `cfpBuildModel(stm, catGroups, codeGroups)`. **`CfpStatementTable` เพิ่มคอลัมน์ "รหัส"** (sticky คู่กับ "รายการ" ที่ left:64) + ทุก row มี `r.code`/`r.indent` (section 6 · group 22 · codegroup 34 · leaf 48 · subtotal/net 22). **codegroup ย่อ/ขยายได้** (state `collapsed` ใน table, คลิกชื่อ → toggle ▾/▸ ซ่อน leaf ที่ `prefix` ตรงกัน) + ปุ่ม "ย่อทุกกลุ่ม/กางทุกกลุ่ม"; คลิก**ยอด**ของ codegroup → drill breakdown (มี catNames). `cfpCodePrefix(code)` = เลข 4-6 หลักหน้าจุด. **★ ใช้เฉพาะโหมดปกติ (ไม่มี catGroups จัดเอง)** — ถ้า user จัดกลุ่มเอง (จัดหมวด) จะใช้กลุ่มนั้นแทน code-grouping. verify ไฟล์จริง (5 เดือน): codegroup 48 · leaf 165 · ชื่อจากชีต ค่าใช้จ่าย 63 กลุ่ม · 10001 รวม 120.3M = POG TANK+DRINK+ค่าบริการ · ย่อ/กาง + คอลัมน์รหัส ทำงาน.
  - **★ โครงงบ 3 ชั้น (`20260620f`, ตามไฟล์ "งบกระแสเงินสด" ของเตย)** — `cfpBuildModel` สังเคราะห์เป็น **กิจกรรม → เงินสดรับ/เงินสดจ่าย (หัว+รวม) → รายการย่อย**: ต่อกิจกรรม push `section` → `group` (หัว "เงินสดรับจากกิจกรรมX" ไม่มียอด) → `leaf` รายหมวด (เรียงรหัส) → `subtotal` ("รวมเงินสดรับจากกิจกรรมX") → ทำซ้ำฝั่งจ่าย → `net`. **รับ/จ่าย แยกด้วยเลขหน้าของรหัสค่าใช้จ่าย** (`catSide`): op 1xxxx=รับ/2xxxx=จ่าย · inv 3/4 · fin 5/6 (เลขคี่=รับ คู่=จ่าย; fallback net sign) — robust กว่าใช้ net sign (credit memo ยอด+แต่เป็นบัญชีจ่าย). `subtotal` พก `catNames` → กดได้เหมือน leaf (เปิด breakdown ของฝั่งนั้น). **จัดหมวด (catGroups) ยังทำงาน**: กลุ่มถูกวางใต้ฝั่ง รับ/จ่าย ตาม majority ของหมวดสมาชิก + "อื่นๆ" แยกฝั่ง. CfpStatementTable รองรับ type `section/group/leaf/subtotal/net/grand` อยู่แล้ว (indent 0/14/26/14/14/14). verify (op 1/2·inv 3/4·fin 5/6): หัวรับ/จ่าย+รวม+สุทธิ ครบ, ยอด tie, indent ถูก.
  - **★ ลำดับรายการในงบ = เรียงตาม "รหัสค่าใช้จ่าย" (c9) ไม่ใช่ตามยอด** (`20260620d`) — ผู้ใช้ทักว่างบเรียงไม่เหมือนไฟล์. parser เก็บ `code` (รหัสค่าใช้จ่าย) ต่อ txn → cat.code (รหัสน้อยสุด) → งบ (โหมดไม่จัดกลุ่ม) เรียง leaf ด้วย `byCode` (`localeCompare numeric`): 10001.xx (เงินสดรับ) ก่อน 20xxx (เงินสดจ่าย จัดกลุ่มตามประเภท) = ลำดับเดียวกับไฟล์/ชีต REPORT. **`catList` ยังเรียงตามยอด** (แท็บสรุปกิจกรรม "องค์ประกอบหลัก" โชว์หมวดใหญ่ก่อน) — เปลี่ยนแค่ลำดับในงบ. (verify ไฟล์จริง 5 เดือน 3323 รายการ: leaf เรียง code ขึ้น, net=Σ Amount เป๊ะ, ไม่เบิ้ล.)
  - **★ drill-down งบแบบ 2 ชั้น (`20260620e`)** — กดแถว "กลุ่ม" (หลายหมวด) หรือ "สุทธิรายกิจกรรม" → `CfpModal` โชว์ **สรุปแยกตามหมวด** ก่อน (เรียงยอดมากสุด + แถบ + ปุ่ม "📋 ดูรายการทั้งหมดรวมกัน") → กดหมวด → รายการจริง + ปุ่ม "← กลับ" (Esc ถอยทีละชั้น). `openStmt` สร้าง `breakdown=[{name,count,net,txns}]` (กรองเดือนถ้ากดช่องเดือน); leaf หมวดเดียว/grand = รายการตรงเหมือนเดิม. **`CfpModal` รับ prop `breakdown`** (state `sel`: null=สรุป·-1=ทั้งหมด·≥0=หมวด). ⚠️ ตอนแรกลืม forward `breakdown={modal.breakdown}` ในจุด render → โชว์ "ไม่พบรายการ" (verify จับได้ แก้แล้ว).
  - **★ "จัดหมวด" (รวมหมวดย่อยเป็นกลุ่มงบ) — `CfpGroupModal`** (ปุ่ม ⚙ ในแท็บงบ, canEdit เท่านั้น; POG-native ไม่ใช่ port ตรงของ BIO `CfpMapModal` ที่ตัดทิ้งแล้ว). 130 หมวดย่อยเยอะไปตอนนำเสนอ → ผู้ใช้ตั้ง "กลุ่มนำเสนอ" เอง (ชื่อ + กิจกรรม) แล้วเลือกกลุ่มให้แต่ละหมวดผ่าน `<select>` (ราย activity). เก็บ **`stored.catGroups = [{id,name,actKey,cats:[catName…]}]`** (sync Supabase ทั้งทีม ผ่าน `persist`/`saveGroups` เหมือน entity อื่น). `cfpBuildModel`: ถ้ามี catGroups → งบแสดง **กลุ่มเป็น leaf** (vals/total = Σ หมวดสมาชิก) + หมวดที่ไม่ถูกจัด = leaf **"อื่นๆ"** ต่อกิจกรรม; ถ้าไม่มี → แสดงราย 130 หมวดเหมือนเดิม. กดแถวกลุ่ม → เปิดรายการของทุกหมวดในกลุ่ม. คงไว้ตอน re-upload (`persist({stm, catGroups: stored.catGroups})`). verify: กลุ่ม "ค่าสั่งซื้อถัง"=Σเวลโกร+เคพีเอส, "อื่นๆ" = หมวดที่เหลือ, ยอด net/grand ตรง.
- **Team-share:** เก็บ 1 แถว `id='current'` ในตาราง Supabase **`cashflowPresent`** (เพิ่มใน `SHEET_TABLES` ของ `data_supabase.js` แล้ว) ผ่าน `WTPData.fetchSheetRows`/`writeTable`. localStorage `wtp-cfpresent-v1` = cache/offline. session key = `wtp-session`. **⚠️ ต้องรัน `supabase/cashflow-present.sql` ใน Supabase SQL Editor ครั้งเดียวก่อน** (สร้างตาราง + RLS read=authenticated/write=staff·manager) — **ก่อนรัน: หน้ายังใช้ได้ (degrade เป็น local-only), อัปโหลดเห็นเองคนเดียว, toast เตือน "แชร์ส่วนกลางไม่สำเร็จ · รัน SQL?"**.
- **Wiring:** index.html (`<script>` + `?v`), `app.jsx` 6 จุด (viewer pages Set + order + routes + render switch + sidebar nav + `isPresentation`), `styles.css` (print `:has(.cfp-page)` + present-mode `.cfp-card` spotlight). **viewer/owner (ผู้บริหาร) เห็นหน้านี้** (อยู่ใน `isPresentation` → มีปุ่มโหมดนำเสนอ + ปริ้น PDF A4). canEdit (staff/manager) เท่านั้นที่อัปโหลด/ล้างได้.
- **โทนสี = แบรนด์ Water POG (น้ำเงิน `--brand-*`)** — รีธีมจากเขียวของ BIO (`20260620b`): palette `C` (primary `#2a6fdb`/primaryD `#1a4490`/teal `#1f56b8`), `ACT_COLOR.op`=น้ำเงิน (inv ส้ม/fin ม่วง คงไว้เป็น accent แยกกิจกรรม), tab gradient/insights/statement bands/present box-shadow → น้ำเงิน. **★ คง เขียว=เงินเข้า / แดง=เงินออก (`C.pos`/`C.neg`) ตามหลักการเงิน** — ไม่เปลี่ยนเป็นน้ำเงิน (semantic ไม่ใช่ brand).
- **verify (preview, ไฟล์จริง):** net ฿4,637,974.85 = REPORT · ต้นงวด ฿70.62M · ปลายงวด ฿75.26M · ม.ค. net −8.21M (ตรง "Cash added" ชีต Jan.) · 1347 รายการ · 12 บัญชี · งบ 139 แถว · ทั้ง 4 แท็บ render ไม่ error · โทนน้ำเงิน POG.

## 2026-06-21 — Cash Flow Present: เอา legend ซ้ำในกราฟรายเดือนออก (build `page_cashflow_present 20260621a`)
- คำขอผู้ใช้ ("เอาในกรอบสีแดงออก ทั้ง POG และ BIO"): กราฟ "กระแสเงินสดรายเดือน (แยกตามกิจกรรม)" (`CfpMonthly`) มี legend ดำเนินงาน/ลงทุน/จัดหาเงิน **2 ชุด** — ชุดในหัวการ์ด (มุมขวาบน) + ชุดซ้ำใน SVG (มุมซ้ายบนในตัวกราฟ). ลบ **ชุดใน SVG** (บล็อก `{/* legend */}{acts.map(...)}` หลังแท็ก `<svg>`) ออก เหลือ legend หัวการ์ดชุดเดียว. ไม่แตะ `padTop`/ขนาดแท่ง/ป้ายค่า/ป้ายเดือน; `const acts` ยังใช้วาดแท่งกราฟต่อ (ไม่ลบ). verify: Babel transform ผ่าน, legend หาย, `<svg>`→`{mo.map}` ตรง. (แก้คู่กับ BIO build เดียวกัน.)

## 2026-06-21 — Cash Flow Present: การ์ด "เงินสดนี้ใช้ได้จริงเท่าไร" — แยก **5 ประเภทบัญชี** (ใช้ได้/ค้ำประกัน/นักลงทุน/บัญชีร่วม/OD) + เพิ่ม-ลดรายเดือนรายบัญชี (build `page_cashflow_present 20260621h` · ทำคู่กับ BIO `20260621f`)
- **คำขอผู้ใช้:** ผู้บริหารเห็นเงินสด 70+ ล้าน (KPI hero) แล้วเข้าใจผิดว่าใช้ได้ทั้งหมด — อยากให้เห็นว่า "ไม่ใช่เงินใช้ได้ทั้งหมด เป็นเงินอะไรบ้าง". วิวัฒนาการ: (รอบ 1) กรอกก้อนเอง `cashBreakdown` → (รอบ 2) แยกตามบัญชี ตารางเต็มในการ์ด → (รอบ 3) **ย่อการ์ด + กดดู drill** → (รอบ 4) **5 ประเภทจริง + แก้ยอดปลายงวดให้ถูก**. (`cashBreakdown` เลิกใช้.)
- **★ 5 ประเภทบัญชี (`CFP_ACCT_TYPES`):** `usable` สามารถใช้ได้ (= "ใช้ได้จริง" เท่านั้น) · `guarantee` วงเงินค้ำประกัน(ติดภาระ) · `investor` เงินนักลงทุน · `joint` บัญชีร่วม · `od` OD เบิกเกินบัญชี. ผู้ใช้ส่งลิสต์จริง 22 บัญชี → ฝัง **ค่าเริ่มต้น `CFP_ACCT_TYPE_DEF`** (จับด้วยเลขท้ายบัญชี 4 หลัก) เพื่อให้ขึ้นถูกเลยไม่ต้องติดเอง; แก้ทับได้ผ่าน ✏️ จัดประเภทบัญชี (`acctTypes` เก็บเฉพาะที่ต่างจาก default). **POG `CFP_ACCT_TYPE_DEF` มีค่าจริง 16 บัญชี · BIO = `{}` (ทีม BIO จัดเอง).**
- **★★ บั๊กใหญ่ที่แก้ — "ปลายงวดต้องอ่านจากไฟล์ ห้ามคำนวณจาก flow":** เวอร์ชันแรกคำนวณ `ending = opening + Σ flows(รวม transfer)` → บัญชีดำเนินงานที่เงินไหลผ่านเยอะ/มีโอนระหว่างบัญชี **ยอดสะสมเพี้ยนมหาศาล** (SCB ‑4863 โชว์ **‑฿180M**, KTB ‑2441 **฿108M**; บัญชีค้ำประกันไม่เพี้ยนเพราะเงินไม่ไหล จึงดูเหมือนถูก). **แก้: parser อ่านคอลัมน์ "ปลายงวด" + "ต้นงวด" รายบัญชีจากชีตเดือน*ทุกเดือน*ตรงๆ** (`cfpMonthBalances` + `cfpBuildAcctBalances`): opening=ต้นงวดเดือนแรก · ending=ปลายงวดเดือนล่าสุด · Δรายเดือน = ปลาย−ต้น ต่อเดือน (foots: opening+ΣΔ=ending). เก็บใน **`stm.acctBalances`** + `stm.balMonths`. **model ใช้ `stm.acctBalances` ตรงๆ (ไม่แตะ txns/flow สำหรับยอดบัญชี)**; `monthsAll`=balMonths; `hasAcctBalances`=มีข้อมูล. **เลิกใช้ `cfpOpeningFromAoa`/openingAccts/flow-derivation.** *(BIO ต่างออกไป — ดูด้านล่าง)*
- **คอมโพเนนต์ `CfpCashUsable`** (แท็บ ภาพรวม — **ใต้กราฟ** Waterfall+รายเดือน ก่อน Executive Insights; ย้ายจากใต้ KPI hero ตามคำขอ build `20260621h`): การ์ดย่อ = total + **ใช้ได้จริง ฿X (Y%) · ติดเงื่อนไข ฿Z (W%)** + แถบ 100% หลายสี (สีต่อประเภท) + **ชิปประเภท** (icon+ชื่อ+ยอด+%+จำนวนบัญชี) = คำตอบ "เป็นเงินอะไรบ้าง". กดยอด/แถบ/ชิป → `drill` (`'all'|typeKey`) → modal ตารางรายบัญชี (บัญชี·ประเภท·ต้นงวด·Δรายเดือน·ปลายงวด+แถวรวม, sticky). read-only ทุก role; **canEdit ✏️ จัดประเภทบัญชี** → modal โหมดแก้ (ทุกบัญชี, ช่อง ประเภท เป็น `<select>` 5 ตัวเลือก) → บันทึก. helper: `cfpCard`,`cfpDeltaM`,`cfpTypeMeta`,`CFP_ACCT_TYPES`,`CFP_ACCT_TYPE_DEF`.
- **(build `20260621i`) แท็บ "สรุปกิจกรรม" `CfpActivityDetail` — "องค์ประกอบหลัก" กางดูทุกหมวดได้:** เดิมโชว์ top 7 + ลิงก์เปิด modal. เพิ่ม state `expanded` + ปุ่ม **▼ ดูอีก N หมวด / ▲ ย่อ** กางหมวดที่เหลือ inline ในกรอบเลื่อน (`maxHeight 420 overflowY auto`) + คงลิงก์ "ดูรายการทั้งหมด →" (onAll modal). verify: op 121 หมวด กาง 7→121 แถว + ปุ่มย่อ + กรอบเลื่อน ✓. **(build `20260621j`) ชื่อหมวดแสดงเต็ม** — เลิก `cfpShort`(ตัด 28 ตัว)+ellipsis ในแถวหมวด, ใช้ `c.name` เต็ม + `wordBreak:break-word` (wrap ได้) + ขยายคอลัมน์ชื่อ `1.5fr→2.4fr` (บาร์ `1fr→0.8fr`). `cfpShort` ยังใช้ในข้อความ "จุดเฝ้าระวัง" เหมือนเดิม.
- **เก็บใน `stored.acctTypes`** `{accountName:typeKey}` (เฉพาะที่ ≠ default) — sync ผ่าน `persist` (ตาราง `cashflowPresent` JSONB ไม่ต้องรัน SQL). **★ GOTCHA:** `persist` ไม่ merge ฟิลด์เดิม → `saveGroups`/`onUpload` (POG) + `saveCatMap`/`onUpload` (BIO) ต้องส่ง `acctTypes` มาด้วยทุกครั้ง. `saveAcctTypes(at)`=ฟังก์ชันใหม่.
- **★ POG ต้องอัปไฟล์ใหม่ 1 ครั้งหลัง deploy** ให้ `stm.acctBalances` มีค่า (parser เดิมไม่เก็บ) — ก่อนอัป การ์ดโชว์ป้ายแนะนำให้อัปใหม่ (ไม่โชว์ตัวเลขเพี้ยน). **BIO ไม่ต้อง** (STM เป็นบัญชีเดินบัญชีรายบัญชี → `Σflow ต่อบัญชี = ยอดจริง reconcile`; BIO คงใช้ flow-derivation จาก `stm.openingByAcct`+txns โดยตั้งใจ — ต่างจาก POG ที่ชีต DATA เป็น list จัดหมวดไม่ reconcile รายบัญชี จึงต้องอ่านปลายงวดจากชีตเดือน).
- **verify (POG preview, ผู้ใช้อัปไฟล์จริง 22 บัญชี):** ✅ ใช้ได้จริง **฿15.17M (20%) · 13 บัญชี** · 🔒 ค้ำประกัน **฿54.78M (72%) · 4** · 💼 นักลงทุน ฿0.18M · 👥 บัญชีร่วม ฿0.47M · ➖ OD ฿5.37M · 2 — **default map จับถูกตรงลิสต์ (4/1/2/2)** · ปลายงวดรายบัญชีถูกหมด **ไม่มี garbage** (SCB ‑4863 = ฿14.16M เดิมเพี้ยน ‑฿180M, KTB ‑2441 = ฿0.06M เดิม ฿108M) · การ์ดย่อ(ไม่มีตาราง) · กดชิปค้ำประกัน→modal 4 บัญชี opening≈ending · กดใช้ได้จริง→13 บัญชี · edit modal `<select>` 5 ประเภท. **BIO:** Babel transform ผ่าน (มี CFP_ACCT_TYPES/CFP_ACCT_TYPE_DEF/hasAcctBalances) + cfpBuildAcctBalances snippet foots (14−6−3=5). *(screenshot timeout = flakiness in-browser-Babel → verify ด้วย DOM/eval)*
- **★★ แก้ "เดือนนอกงวดโผล่ + ปลายงวดผิดงวด" (build `20260621g`, POG-only):** ผู้ใช้ทักว่าข้อมูลมีถึง พ.ค. แต่การ์ดโชว์เดือน 6-12. ตรวจพบ **ไฟล์มีชีตยอดรายเดือน มิ.ย.–ธ.ค. (ข้อมูลคนละงวด/ปีก่อน) ปนมา** ทั้งที่ txns (ชีต DATA) มีถึงแค่ พ.ค. — `cfpBuildAcctBalances` ดึงทุกชีตเดือน + `ending` = ปลายงวด **ธ.ค.** (ผิดงวด!). **แก้ที่ model:** (1) `monthsAll = balMonths ที่ ≤ เดือนสุดท้ายของ txns` (`txnMonthMax`) → ตัด มิ.ย.–ธ.ค. ออกจากคอลัมน์ · (2) **parser เก็บ `byClose[m]` (ยอดปลายงวดรายเดือน) เพิ่ม** → model หยิบปลายงวด = `byClose[เดือนสุดท้ายในงวด]` ตรงๆ + Δ รายเดือนแบบ **running** (`close−prevClose`) ที่ foots (opening+ΣΔ=ปลายงวด). **★ ชีตรายเดือนไม่ใช่ running balance ต่อเนื่อง** (verify: opening+ΣΔ(1-12) ≠ ปลายงวด ธ.ค. ที่ parser เก็บ) → ห้าม reconstruct ปลายงวดจาก Δ ต้องอ่าน close ของเดือนนั้นจากไฟล์. **ข้อมูลเก่าที่ยังไม่มี `byClose` → fallback (cap เดือน + reconstruct ปลายงวดแบบประมาณ) ⇒ ต้องอัปไฟล์ใหม่ 1 ครั้งให้ปลายงวดแม่น.** verify: เดือนเหลือ ม.ค.–พ.ค. ✓, byClose snippet ending=close พ.ค. ตัด มิ.ย./ธ.ค. ทิ้ง + foots ✓. **BIO ไม่กระทบ** (monthsAll มาจาก txns อยู่แล้ว ไม่อ่านชีตเดือน).

## 2026-06-22 — P&L: แถวล่าง Trend % → "% net margin" (กำไรสุทธิ/รายได้) — ทำคู่กับ BIO (build `page_pnl 20260622a` · BIO `20260621d`)
- ตามคำขอผู้ใช้ (ทำทั้ง POG+BIO พร้อมกัน): แถวสุดท้ายของงบ (เดิม `Trend %` = MoM growth ของ net) เปลี่ยนเป็น **`% net margin` = กำไรสุทธิ ÷ รายได้** รายงวด + คอลัมน์รวม = net รวม ÷ รายได้รวม. คู่ขนานกับ `% margin` (gross).
- 4 จุด: `PL_compute` เพิ่ม `netMargin` (เลิกใช้ `trend`); reportRows แถวสุดท้าย; `cellValue` recompute net/rev ต่องวด; render รวมทั้งปี (else-if `% net margin`). label สั้น `% net margin`.
- verify preview (ข้อมูลจริง POG): คอลัมน์รวม **(25.8%)** = net −49.1M / รายได้ 190.2M, ไม่มี error. (BIO ทำชุดเดียวกัน รวม (114.0%).)

## 2026-06-22 — Bank Recon: รายการ STM จับคู่ PV แล้วเด้งกลับ + นับซ้ำหน้า Cashflow — self-heal รู้จัก PV + persist ทันที (build `page_bank_recon 20260622a`)
- **อาการ (ผู้ใช้, บัญชี 1362684863):** บรรทัด statement "โอนไป SCB x8512 นาย กฤตวัฒน์ ฿2,592,000" (08/06/2026) ค้างถัง **"บันทึกจาก statement" (`recorded`)** + โน้ต **"กู้คืนอัตโนมัติจากกระทบยอด (self-heal)"** → เด้งเข้า **Actual หน้า Cashflow ซ้ำกับ PV จริง**ของก้อนเดียวกัน. กด **"🔗 จับคู่ PV แทน"** ไปหลายรอบแล้ว**ยังเด้งกลับ** เป็น recorded เหมือนเดิม.
- **ROOT CAUSE = self-heal ([[bankrecon-actual-clobber]]) ปั๊มแถว BANK_RECON กลับวนไม่จบ.** ลำดับ: (1) เลขที่ PV ถูกแก้ → auto-match พลาด → เคยกด "บันทึกจ่ายจริง" (decision=`recorded` + สร้าง forecastEntries `EXPENSE_TYPE='BANK_RECON' STATUS='ACTUAL'`). (2) ผู้ใช้กด "จับคู่ PV แทน" → `manualMatchPv` ลบ F (forceDeleteRows) + ตั้ง decision=`matched`. **แต่ decision=`matched` ไม่ติดทน** (push เป็น debounce 1.5s — หายระหว่างทาง / แท็บค้างดันค่าเก่ากลับ) เหลือ **decision=`recorded` + F ถูกลบ**. (3) reload → `brUndoneRef` (in-memory) เคลียร์ → self-heal เห็น `recorded` + forecastId หาย → **ปั๊ม F กลับ** → นับซ้ำกับ PV → วน. (self-heal เดิม **ไม่รู้จัก PV** เลยปั๊มทับของที่มี PV รองรับอยู่แล้ว).
- **FIX (ฝั่ง client ล้วน, ไม่ต้องแตะ DB/redeploy):**
  1. **self-heal รู้จัก PV (ตัวหลัก):** ก่อน resurrect ทุกแถว เช็ค `lineHasPv(line)` = มี PV ในระบบ (`bdNormPV`) ที่ **ยอดตรง ±0.01 + บัญชีตรง (`bdAcctMatchesCheck` เลขท้าย4, ข้ามถ้า bankAc ไม่มีเลข) + วันใกล้กัน ≤31 วัน** → ถ้ามี **ไม่ปั๊มซ้ำ** (PV เป็น Actual อยู่แล้ว) + `console.info` จำนวนที่ข้าม. **กันนับซ้ำถาวร** ไม่ว่า decision จะเด้งกลับจากแท็บไหน. ไม่พัง use case เดิม: แถว manual ที่**ไม่มี** PV ตรง → ยัง resurrect ได้. เพิ่ม `data.pvVouchers` ใน deps.
  2. **persist ทันที** ใน `manualMatchPv` + `undoRecord`: หลังตั้ง decision เรียก `WTPData.forceSyncNow({ ...data, forecastEntries: nextFe, bankReconState: brStateToRows(ns) })` (ข้าม debounce/activity-gate) — push ด้วย data ที่**ตัด F ออกแล้ว** (`nextFe`) กัน upsert F คืน. ทำให้ decision=`matched`/ลบ ติดทันที ไม่หาย.
- **verify (preview, เทียบ helper ตัวจริง):** `lineHasPv` — เคสคุณกฤตวัฒน์(ยอด+บัญชี+วันตรง)=`true` (ข้าม resurrect) · ยอดคนละก้อน=`false` · ยอดตรงคนละบัญชี=`false` · ยอด+บัญชีตรงห่าง5เดือน=`false` (กันจ่ายซ้ำรายเดือน) · manual ไม่มี PV=`false` (ยัง resurrect). ไม่มี console error, `BankReconPage` defined (compile ผ่าน). *(RLS บล็อกข้อมูลจริงตอนไม่ล็อกอิน → verify logic ด้วย eval แทน E2E)*
- **วิธีใช้หลัง deploy:** เปิดหน้า #bank_recon แล้วกด **"🔗 จับคู่ PV แทน"** รายการนี้ **อีกครั้งเดียว** → F ถูกลบ + decision=`matched` ติดทน + self-heal จะไม่ปั๊มกลับ (มี PV รองรับ) → ออกจากถัง recorded ไป "แมตช์แล้ว" ถาวร. (แท็บเก่าที่ยังเปิดค้างต้อง hard-refresh ก่อน ไม่งั้นโค้ดเก่ายังปั๊มได้.)

## 2026-06-23 — ชื่อไฟล์ตอนเซฟ PDF: `BRAND_CODE` + ตั้งชื่อตามหน้า (Forecast/War Room 1/2) — ทำคู่กับ BIO (build `config/page_cashflow/page_warroom_p1/page_warroom_p2 20260623a`)
- **คำขอผู้ใช้:** เซฟ PDF แล้วชื่อไฟล์สื่อความ + วันที่ = วันกดบันทึก (ค.ศ.). POG ใช้ prefix `WTP` (BIO ใช้ `BIO`). ทำคู่กับ BIO (ฝั่ง BIO ตั้ง `BRAND_CODE:'BIO'` + ปุ่ม War Room 1/2).
- **`BRAND_CODE: 'WTP'` ใน `config.js` (ใหม่):** อ่านผ่าน `(window.WTP_CONFIG && WTP_CONFIG.BRAND_CODE) || 'WTP'` ในปุ่มพิมพ์ → ไฟล์ jsx เหมือน BIO ต่างแค่ค่า config (แยกต่อ repo).
- **กลไก:** เบราเซอร์ใช้ `document.title` เป็นชื่อไฟล์ default ตอน Save as PDF → ปุ่มพิมพ์ (Forecast/WR1/WR2 ที่มี+ทำงานอยู่แล้ว) **เพิ่มแค่ logic ตั้ง `document.title` ชั่วคราว** ก่อน `window.print()` + afterprint listener `_restoreTitle` คืนค่าเดิม (แยกจาก `cleanup` เดิม ไม่ยุ่ง present-mode/wr-print-mode). **ไม่แตะโครงสร้างปุ่ม/print CSS เดิม** (WR1/WR2 ของ POG ใช้ `wr-print-mode` ซ่อน chrome อยู่แล้ว).
- **ชื่อไฟล์ (วันที่ = `new Date()` DD.MM.YYYY):** Forecast → `WTP-FORECAST <วันที่>` · War Room 1 → `WTP - รายรับสะสมประจำปี <วันที่>` · War Room 2 → `WTP - ประมาณการรับเงินจากโครงการ <วันที่>`.
- **verify (preview 8011, login manager, stub `window.print` + กดปุ่มจริงทุกหน้า):** ทั้ง 3 หน้า `match:true` — `WTP-FORECAST 23.06.2026` / `WTP - รายรับสะสมประจำปี 23.06.2026` / `WTP - ประมาณการรับเงินจากโครงการ 23.06.2026`. `BRAND_CODE='WTP'` โหลด, ไม่มี console error. (หน้า War Room render หนัก → ต้องรอ `.page-title` สลับเป็นหน้าที่ถูกก่อนกดปุ่ม ไม่งั้นไปเจอปุ่ม Forecast ของหน้าเดิม.)

## 2026-06-23 — แผนที่จังหวัดไทยจริง 77 จังหวัด (`ThaiMap`) แทนแผนที่ styled หน้านักลงทุน (build `thaimap_data/thaimap 20260623a` · `page_investor 20260623a`)
- **คำขอผู้ใช้:** อยากได้แผนที่ไทยแบบรายจังหวัด (ตัวอย่างคือ repo `BorntoDev/Thailand-Map-Vector`) แต่ **"สร้างเอง COPY มาไว้ที่เรา ไม่เอาไฟล์ของเขาเข้ามา"** → vendor geometry จาก **แหล่งอื่นที่ permissive** (ไม่ใช่ repo ที่ลิงก์มา) แล้วเขียน component เองทั้งหมด.
- **2 ไฟล์ใหม่:**
  - **`app/thaimap_data.js`** (plain JS, ~167KB) — vendored GeoJSON `window.TH_GEO` (77 จังหวัด, Polygon+MultiPolygon, `properties.name` = ชื่อ EN). **ที่มา: `apisit/thailand.json` → `thailandWithName.json` (simplified vector) · MIT License © 2012 Apisit Toompakdee** (คนละผู้เขียนกับ BorntoDev). copy ฝังในเรา = self-contained ไม่มี runtime dependency. boundary = geographic facts.
  - **`app/thaimap.jsx`** (babel, global **`window.ThaiMap`**) — โค้ดเราเองล้วน: อ่าน `TH_GEO` → **projection equirectangular + latitude-correction** (cos(midLat)) fit → viewBox `1000×1816` (สัดส่วนไทยจริง) → path ต่อจังหวัด. ลงสี **ตามภาค (`REG_META` 6 สี harmonize โทนเดค) + เข้มตามค่า** (`mode` = `regionValue` default / `region` / `value`), hover ไฮไลต์+tooltip (ชื่อ TH/EN · ภาค · จำนวน), legend 6 ภาค (hover เน้นภาค + dim ภาคอื่น), badge ตัวเลขบนจังหวัดที่มีข้อมูล, footer รวม. การจัดกลุ่มภาค (`PROV` EN→[TH,region]) **มิเรอร์ `pc_engine.REGION`** (ภาคเหนือล่าง=กลาง) ให้ legend ตรงกับ "โครงการแยกตามภาค" ที่อื่น. `normProv` ตัด `จ.`/`จังหวัด` + `ALIAS` (กรุงเทพ/กทม→กรุงเทพมหานคร, อยุธยา→พระนครศรีอยุธยา) เพื่อจับคู่ชื่อจาก data.
- **Props:** `palette`(p) · `byProvince`(คีย์ชื่อจังหวัดไทย) · `byRegion`(คีย์ภาค EN, legend fallback) · `lang` · `mode` · `maxWidth`(380) · `onSelect`.
- **Integration:** `InvThaiMap` (page_investor §7 "โครงการแยกตามภูมิภาค") **delegate ไป `window.ThaiMap`** เมื่อมี `TH_GEO`+`ThaiMap` (ส่ง `byProvince: m.byProv` คีย์ไทย + `byRegion: m.byRegion` คีย์ EN) — **คงแผนที่ bubble แบบเดิมไว้เป็น fallback** ถ้าโหลด geometry ไม่ได้. `index.html` เพิ่ม `thaimap_data.js` (plain ก่อน babel) + `thaimap.jsx` (ก่อน page_investor) ทั้งคู่ `?v=20260623a`, bump `page_investor` 20260616c→20260623a.
- **⚠️ GOTCHA verify:** preview server โฮมเมด `.claude/server.ps1` เป็น single-thread + ครั้งคราวโยน `ProtocolViolationException "bytes exceed Content-Length"` ตอน browser cancel/parallel write (ไม่ใช่บั๊กโค้ด — prod GitHub Pages เสิร์ฟปกติ) · **screenshot tool time out ประจำกับแอป in-browser-Babel นี้** → verify ด้วย DOM inspection (render `ThaiMap` ลง overlay + อ่าน DOM). first load ช้า (เสิร์ฟ ~30 สคริปต์ + babel compile) อาจเกิน 30s eval → รอแล้วลองใหม่.
- **verify (preview, render isolated + mock byProvince ข้ามภาค):** `TH_GEO.features=77` · `ThaiMap=function` · **path 77 เส้น** · viewBox `1000×1816` · distinctFills 15 (ภาค+เข้มตามค่า) · badge 9 จังหวัด · **alias merge** (กรุงเทพมหานคร 12 + กรุงเทพ 1 = badge "13") · footer "รวม 45 โครงการ · 9 จังหวัด" · legend ครบ 6 ภาค · **orientation เหนือ-อยู่-บน** (เชียงใหม่ cy 219 < กรุงเทพ 827 < สงขลา 1660/1816) · ไม่มี console error.
- **⚠️ FIX "ดำปี๋" (โหมดมืด) — build `thaimap 20260623b`:** เวอร์ชันแรก `fillFor` fade สีภาคเข้าหา **`card` ของธีม** → จังหวัด**ไม่มีข้อมูล** (ซึ่งเป็นส่วนใหญ่ของ 77) ยุบเป็นสี card = **ดำ** (dark theme `card=#131d33`) / ขาวจาง (light). (เทสต์รอบแรกใช้ palette สว่างเลยไม่เจอ.) **แก้: anchor tint ที่ neutral คงที่ `TINT='#eef2f7'` (ไม่อิงธีม)** → ทุกจังหวัดมีสีภาคชัดทั้ง 2 ธีม (no-data `t=0.40`, data `t=0.55..1.0` ตาม `sqrt(v/maxV)`); สีฐานจังหวัดที่ไม่ match ใช้ `#9aa7bd` (เลิก fallback `sub` ที่อิงธีม). + **badge text + tooltip bg เปลี่ยนจาก `ink` → `#10233f` คงที่** (ink สว่างในธีมมืด = มองไม่เห็น). bump thaimap.jsx `20260623a→b`.

## 2026-06-23 — เอาสัญลักษณ์เงินบาท ฿ ออกจากทุกหน้า (ทำคู่กับ BIO) — build `20260623d`
- **คำขอผู้ใช้:** "ทุกหน้าที่มีรูปสัญลักษณ์เงินบาท ฿ เอาออกจากทุกหน้าเลย ทั้ง POG และ BIO" → ลบ ฿ ที่ **แสดงผลบนจอ** ทุกจุด (ตัวเลขเงินเหลือไว้ ตัด ฿ ทิ้ง).
- **ขอบเขต:** 20 ไฟล์ · 252 จุด. รูปแบบที่ลบ: ป้ายคอลัมน์/ฟอร์ม `(฿)` → ``; ต่อท้ายค่า `{fmtNum(x)} ฿` → `{fmtNum(x)}`; prefix สกุลเงิน `'฿' + x` / `฿${x}` (template) / `฿{x}` (JSX) → ตัด ฿; เครื่องหมายติดบาท `'−฿'`/`'+฿'` → `'−'`/`'+'`; literal `'฿958M'` → `'958M'`; ไอคอน/glyph เดี่ยว `<span>฿</span>` → `<span></span>` (เหลือ span ว่าง มองไม่เห็น), `icon = ... ? '◆' : '฿'` → `: ''`, `suffix:'฿'` → `suffix:''`, `{c.type==='money'?'฿':...}` → `''`.
- **★ ฿ ที่ "เก็บไว้โดยตั้งใจ" (ไม่ render — grep `฿` จะเหลือ 9 บรรทัดนี้):** (1) **regex แกะค่า** `replace(/[,%\s฿]/g,'')` / `replace(/[฿\s]/g,'')` ใน `data_extras`(3) · `warroom_p2` · `data_sync` · `bank_recon` · `pc_engine` · `cashflow_present` — พวกนี้ "strip ฿ ออกจาก input string" (ฟังก์ชัน ไม่แสดงผล; ลบแล้วเสี่ยง regress การ parse). (2) **คอมเมนต์** ใน `data_extras:1876`, `data_sync:784`, `investor:601`. **ถ้าเจอ ฿ ใหม่บนจอภายหลัง = จุดที่เพิ่มทีหลัง ต้องลบเพิ่ม; แต่ 9 บรรทัดนี้ห้ามแตะ.**
- **วิธีทำ (เครื่องนี้ไม่มี Python/Node):** สคริปต์ **PowerShell** (`%TEMP%\debaht_wtp.ps1`, ASCII-only ใช้ char-code กัน encoding เพี้ยน, อ่าน/เขียน UTF-8 รักษา BOM+line-ending) วนกฎ regex เรียงลำดับ + ข้ามบรรทัดที่ขึ้นต้น `//` หรือมี `replace(/[`. **gotcha PowerShell:** `@(@(),@())` ถูก flatten → ใช้ `[pscustomobject]@{P;R}` แทน; replacement ที่มี `$` ต้อง `$$`.
- **★ จุดสำคัญ — การเว้นวรรค (hybrid):** ` ฿` ที่อยู่หน้า **closing quote** (เคส concat `'ป้าย ฿' + เลข` เช่น `'รับแล้ว ฿'+x`, `'60 วัน ฿'+x`, `'จ่ายก่อนรับงวดแรก ฿'+x`) → คงสเปซ `' '` (กัน "ป้ายเลข" ติดกัน); ` ฿` ตำแหน่ง suffix อื่น (หน้า `<`/`)`/`}`/space/ท้ายบรรทัด) → ลบสเปซด้วย (กัน `({fmtNum} ฿)`→"(เลข )" และ double-space ใน toast/confirm/template ที่ HTML ไม่ collapse). `฿{`/`฿${` ลบเฉพาะ ฿ คงตัวอักษรหน้าไว้ (`} ฿{`→`} {`, `>฿{`→`>{`).
- **verify:** 252 ลบครบ, เหลือ ฿ แค่ 9 บรรทัด non-display; ทั้ง 20 ไฟล์ผ่าน `Babel.transform` (เช็คตรง) + แอป boot ถึงหน้า login ไม่มี console error. (เคสที่ต้องเช็คเป็นพิเศษ verify แล้วสะอาด: debt_ledger toast/confirm, cashflow detail modal template, bank_recon "(รวม X)", investor §6 timeline, pc_engine report HTML.)
- **ไม่แตะ:** ตัวเลข/logic/`fmtBaht`-`baht`-ชื่อฟังก์ชัน (คงชื่อ แค่ output ไม่มี ฿), `$`/USD (`USD ($)` คงไว้ — ผู้ใช้ขอแค่ ฿), empty span ที่เหลือไม่ลบ (minimal change). BIO ทำชุดเดียวกัน (ไฟล์ fork คนละเวอร์ชันเล็กน้อย — รันสคริปต์เดียวกันบน repo BIO).

## 2026-06-23 — Cash Flow Present: บาร์ "องค์ประกอบหลัก" จุดเริ่มไม่ตรงกัน (ทำคู่กับ BIO) — build `page_cashflow_present 20260623e` (BIO `20260623j`)
- **อาการ (ผู้ใช้):** หน้า `#cashflow_present` แท็บ "สรุปกิจกรรม" การ์ดรายกิจกรรม (`CfpActivityDetail`) ส่วน "องค์ประกอบหลัก" — บาร์แนวนอนแต่ละแถว **จุดเริ่ม (ซ้าย) ไม่ตรงกัน** (เหลื่อม ~6px).
- **ROOT CAUSE:** แต่ละแถวเป็น **grid แยกของตัวเอง** `gridTemplateColumns: 'minmax(0,2.4fr) minmax(0,0.8fr) auto'` (ชื่อ·บาร์·ยอดเงิน). คอลัมน์ยอดเงิน = **`auto` = กว้างตามความยาวตัวเลข** → ยอด "−36,693,644" (มี `−`+หลักเยอะ) คอลัมน์กว้าง (75.7px) ดันคอลัมน์ `fr` (ชื่อ+บาร์) ให้แคบลง บาร์เริ่มซ้ายขึ้น · ยอดสั้นคอลัมน์แคบ บาร์เลื่อนขวา → แต่ละแถว fr แบ่งพื้นที่ไม่เท่ากัน → จุดเริ่มบาร์ต่างกัน. (verify สด: barLeft 349/353/355, col ยอด 67.9/75.7/70.4px).
- **FIX:** `auto` → **`92px`** (ตรึงคอลัมน์ยอดเงินคงที่) → ทุกแถวคอลัมน์ชื่อ+บาร์เท่ากัน → บาร์เริ่มจุดเดียว. 92px พอดีกับยอดที่ยาวสุดในข้อมูลจริง (สูงสุด ~76px) + เผื่อถึง ~12 หลัก; ตัวเลขยัง `textAlign:right` (ชิดขวาเหมือนเดิม, ขอบขวาตรงกันด้วย — โบนัส). ถ้าตัวเลขเกิน 92px จะล้นชิดขวาแต่ **track คงที่ บาร์ยังตรง**.
- **verify (BIO live, 21 แถว):** หลังแก้ barLeft = **337 จุดเดียว** (เดิม 349/353/355) · ยอดชิดขวาตรงที่ 507 · ไม่มีเลขล้นคอลัมน์. POG component เดียวกัน ผลเหมือนกัน.

## 2026-06-24 — Bank Recon: ตัดช่องใส่รหัสออกจากไฟล์ KTB เข้ารหัส → แสดงคำแนะนำ Save As ทันที (build `page_bank_recon 20260624b`)
- **ROOT CAUSE (ข้อจำกัดถาวร):** SheetJS 0.18.5 ในเบราว์เซอร์รองรับแค่ encryption แบบ RC4/Standard เก่า — ไฟล์ KTB ใช้ **CryptoAPI/Agile** → **ถอดรหัสไม่ได้เลย ไม่ว่าจะใส่รหัสถูกหรือเปล่า**. เดิมระบบเด้ง modal ให้ใส่รหัส → ใส่ถูกก็ยังบอก "รหัสไม่ถูกต้อง" → ผู้ใช้เสียเวลาเปล่า.
- **FIX:** ตัด `BRPasswordModal` ช่องใส่รหัสออกทั้งหมด + ลบ `onSubmit`/`wrongPwd` logic ทิ้ง. เมื่อไฟล์ต้องการรหัส (`needsPwd`) → **แสดง modal คำแนะนำ 3 ขั้นตอนทันที**: (1) เปิดไฟล์ใน Excel แล้วใส่รหัส (2) Save As .xlsx/.csv ไม่ใส่รหัส (3) อัปโหลดไฟล์ใหม่. ไม่มีช่องกรอกรหัส ไม่มี "ลองใหม่".
- **หมายเหตุ:** เบราว์เซอร์ไม่มีทางถอดไฟล์เข้ารหัสชนิดนี้ได้ — workaround เดียวคือ Save As จาก Excel ก่อน.

## 2026-06-24 — Bank Recon: popup นำเข้าหลายไฟล์/หลายบัญชี + re-link สถานะเก่า (กันงานหาย) + preset KTB + auto-map single-column (build `page_bank_recon 20260624d`)
- **คำขอผู้ใช้ (3 ข้อ):** (1) "ยังอ่านไม่ได้" คืออะไร + ไฟล์ KTB ไม่ติดรหัสแล้ว (Save As สำเร็จ) · (2) นำเข้าหลายไฟล์ = **popup โยนไฟล์ → สรุปว่าได้แบงค์ไหนบ้าง** (ไม่ใช่ทีละ modal) · (3) **นำเข้าใหม่แล้วงานเก่า (แมต/โอนระหว่างบัญชี/บันทึกจ่ายจริง) หาย ต้องทำใหม่ทุกครั้ง** — ขอให้ "รายการที่เคยทำไม่กระทบ".
- **(1) ROOT CAUSE "ยังอ่านไม่ได้ = อ่าน 0 รายการ":** ไฟล์ KTB Historical ยอดอยู่ **คอลัมน์เดียว** ("ถอนเงิน/ฝากเงิน" = signed บวก=เข้า ลบ=ออก) แต่ `brAutoMapping` เดิมจับชื่อนี้ได้ทั้ง `/ถอน/`→debit และ `/ฝาก/`→credit **คอลัมน์เดียวกัน** → mode=split → `amount = credit−debit = 0` ทุกแถว → ข้ามหมด. **FIX `brAutoMapping`:** เพิ่ม `sameCol = debitCol===creditCol` → ถ้าจริง = **single mode** (`amountCol = คอลัมน์รวมนั้น`, `outflowPositive=false` ไม่ flip; flip เฉพาะ "ยอดถอนล้วน" = เจอ debit ไม่เจอ credit). **GOTCHA ที่ verify จับได้:** `/^ยอด/` ของ amountCol ดันไป match **"ยอดคงเหลือ"** → ได้ amountCol ผิดเป็นคอลัมน์ balance → แก้ให้ `sameCol ? debitCol : ...` (คอลัมน์รวมชนะก่อน). **+ เพิ่ม `BR_PRESETS.KTB`** (`mode:'single', dateCol:0, amountCol:4, outflowPositive:false, balanceCol:6, descCol:2, refCol:3`, header แถว 10/idx9 — แต่ `buildMappingFor` override headerRow ด้วย `brGuessHeaderRow` เสมอ).
- **(2) Popup `BRImportModal` (แทน flow ทีละ modal):** drop-zone (ลากวาง/กดเลือก, `multiple`) → parse แต่ละไฟล์ async → **ตารางสรุปต่อไฟล์**: ชื่อไฟล์ · `<select>` บัญชี (เดาด้วย `brDetectAccountForFile` เปลี่ยนได้) · จำนวนรายการ · สถานะ (`reading`/`ready ✓+เดือน`/`empty ⚠ อ่าน 0`/`error ✕`/`pwd 🔒`) · ปุ่ม **⚙** (เปิด `BRMappingModal` แก้คอลัมน์รายไฟล์ reuse เดิม) + **✕** (เอาออก). footer "N/M พร้อม" + **นำเข้าทั้งหมด (N)**. ไฟล์มีรหัส = ขึ้นสถานะ 🔒 + โน้ต Save As (ไม่บล็อกไฟล์อื่น). item state เก็บ `sheets/sheetNames/sheetIdx/mapping/accountNo` → `recompute()` คำนวณ aoa+lines ใหม่เมื่อเปลี่ยนบัญชี/คอลัมน์/ชีต. **เปลี่ยน flow คิวเดิมทั้งหมด** (ลบ `parseFileForPreview/beginFile/advanceQueue/onPickFile/applyImport/preview/pwdPrompt/fileRef`); `buildMappingFor` คงไว้ (ส่งเป็น prop `buildMapping`). `BRMappingModal` ยังรับ `accounts/onChangeAccount/onSkip` (popup ใช้ `_total:1` → ไม่โชว์ "ไฟล์ x/y"/ข้าม). `BRPasswordModal`/`BRImportHelp` ยังอยู่ (help text ปรับเป็น popup).
- **(3) ★ Re-link `brRelinkState(reconState, oldLines, newLines)` (module-level) — กันงานเก่าหาย:** **ROOT CAUSE:** `line.id = บัญชี|วันที่|ยอด.toFixed(2)|(ref||desc).slice(0,18)#n` → reconState (key by line.id) ผูกสถานะ matched/transfer/recorded. นำเข้าใหม่ถ้า map/ข้อความ/ลำดับต่างนิดเดียว → id เปลี่ยน → `brReconcile` (`reconState[l.id]`) หาไม่เจอ → เด้งกลับ "ยังไม่ทำ" **+ เสี่ยงบันทึกจ่ายจริงซ้ำ = forecastEntries ACTUAL ซ้ำใน Cashflow**. **FIX:** ตอน `commitImport` ก่อนทับ lines เดือนนั้น → ย้าย decision จาก line เก่า → line ใหม่ที่เป็น "รายการเดียวกัน" จับด้วยลายเซ็นที่ทน **`วันที่|ยอด`** (ไม่ขึ้น desc/ref/วิธี map) + tie-break **เลขเช็ค/ref** (`bdDigits`); id เดิมที่ยังอยู่ในไฟล์ใหม่ = ข้าม (stable); รายการที่หายจากไฟล์จริง = คงสถานะเก่าไว้ (ไม่ทิ้ง). คืน `{ns, moved}`. push ผ่าน `pushReconState` (sync row-level: key เก่าหาย→ลบ, key ใหม่→เพิ่ม — ปลอดภัยระดับเดียวกับ match/transfer เดิม). toast บอก "คงสถานะที่เคยทำไว้ N รายการ".
- **`commitImport(readyItems)`**: group ตามบัญชี → ต่อไฟล์ bucket รายเดือน + `brRelinkState` + ทับเดือน → `setLinesAll/pushReconLines` + `setMapAll` (จำ mapping ต่อ brand) + `setReconState/pushReconState` (ถ้า relMoved) → สลับไปบัญชี/เดือนล่าสุด.
- **verify (preview, render `BankReconPage` จริง + mock 3 บัญชี, `_wtpRoleIsReadOnly=false`):** คอมไพล์ผ่าน · **`brAutoMapping`**: KTB header → `mode:single, amountCol:4, outflowPositive:false` (หลังแก้ amountCol bug); KBANK เดบิต/เครดิตแยก → split (ไม่กระทบ) · **`brRelinkState` 4 เคส:** ย้าย matched/PV→id ใหม่ ✓, ลบ key เก่า ✓, id เท่าเดิมคงอยู่ ✓, รายการหายจากไฟล์คงสถานะไว้ ✓ · **popup E2E:** เปิด → drop 2 ไฟล์ KTB-layout → ตาราง: `KTB-2441-jun.csv`→เดา **0001112441**, อ่าน **3** ✓ "✓ มิ.ย. 2026"; `export(3).csv` ไม่มี hint→fallback SCB→อ่าน **0** "⚠ ปรับคอลัมน์" → เปลี่ยน select เป็น KTB → **re-read 3** "2/2 พร้อม" → กดนำเข้าทั้งหมด → toast "นำเข้า 2 ไฟล์ · 6 รายการ" + ปิด · ⚙ เปิด `BRMappingModal` (มีตัวเลือกบัญชี + radio split/single) · ไม่มี console error. *(login-gate+RLS → mock render + DOM/eval; ไม่กดเขียน localStorage จริง)*

## 2026-06-24 — หน้าใหม่ "BACK TO BACK · AR↔AP" (#payment_recon) จาก Claude Design handoff "Back to Black" (build `page_payment_recon 20260624m`)
- **★ build `m` (รอบ 9) — สีพื้น (ผู้ใช้ว่าเทาจืดตอนกาง):** เพิ่มความเข้ม `PR_TINT` (ready เขียว `#e6f9ee`/`#86e0a6` · waiting เหลือง `#fdf4dc` · planned น้ำเงิน `#e8f1ff` · paid/pending น้ำเงิน-สเลตอ่อน `#e9f0f8`/`#eef2f8` แทนเทาเกือบขาว) + **พื้นส่วน expand = ขาว** (เดิม `var(--ink-50)` เทา → การ์ดสีเด้งขึ้น) + ป้าย "จ่ายแล้ว/ค้างจ่าย" ในแถวรายละเอียดเป็น **ชิปสี** (เขียว/เหลือง) + AR row ที่รับแล้วเขียวขึ้น. verify: พื้น expand ขาว ✓ · ชิป ✓ จ่ายแล้ว เขียว `#dcfce7` ✓.
- **★ build `l` (รอบ 8) — แก้ราก "PV เดียวจ่ายหลาย JOB" (ผู้ใช้ชี้):** pvVouchers sync มาแบบ **รายบรรทัด** (ตรวจ backup: 614 บรรทัด/299 PV จริง · 63 PV หลายบรรทัด/หลาย JOB) — แต่ละบรรทัดมี **`Project_Dpt` (ช่อง A)=JOB ของบรรทัดนั้น** + **`cc_remark` (ช่อง S)=remark รายบรรทัด**; ส่วน **`Remark` (ช่อง R)=remark รวมทั้ง PV** (เหมือนกันทุกบรรทัด เช่น "INS057/IV…, INS058/…"). **2 บั๊กที่แก้:** (1) จัดงวดเดิมอ่าน `Remark` (รวม) → ใช้ **`cc_remark` ก่อน** (`prInst(v.cc_remark||v.Remark)` + display remark = cc_remark). (2) หา JOB เดิม fallback ไป `Remark` รวมเมื่อ Project_Dpt ว่าง → **PV หลาย JOB ถูกเหมาเป็น JOB แรกใน Remark** → แก้เป็น `prCodeFromText(v.Project_Dpt)||prCodeFromText(v.jobname)` **ยึดช่อง A อย่างเดียว ไม่ใช้ Remark รวม** (payables ก็ใช้ `pre_des` อย่างเดียว ไม่ fallback remark). **ผลลัพธ์ (verify mock "PV เดียว 3 บรรทัด"):** ENC156 → ADV(cc"Advance 40%") + งวด1(cc"งวดที่ 1-65%") แยกถูก · บรรทัดสินค้า (col A="Department: SOD", โครงการอยู่แค่ใน Remark) **ไม่ถูกดึงเข้า INS057** ✓. **ข้อสังเกต:** บรรทัดที่ col A เป็น Department/Warehouse (เช่น PV สินค้า booked เข้าคลัง) จะไม่ผูกโครงการ (ถูกต้องตาม "ช่อง A เป็นเจ้าของ JOB") — ถ้าต้องการให้ขึ้น ต้องแก้ tagging col A ใน ERP.
- **★ build `j`/`k` (รอบ 7) — แก้ "ADV มี 3 รายการ" + การ์ดจืด:** (1) **จัดงวด (`prInst`) แม่นขึ้น** — เรียงลำดับใหม่: เช็ค **"งวดที่ N" ก่อนสุด** (remark ที่มีทั้ง "Advance" + "งวดที่ 1" → งวด 1 ไม่ใช่ ADV) → ตัด `คืน.*ประกัน/ขอคืนเงิน`→คืนประกัน → `ตัดมัดจำ/เคลีย`→อื่นๆ → `advance/มัดจำ/ล่วงหน้า`→ADV. แก้อาการ PV ที่จริงเป็นงวด 1/2 แต่ remark มีคำว่า "Advance/มัดจำ" ถูกเหมารวมเข้า ADV หมด (งวด 1/2 เลยว่าง รอตั้งเบิก). (2) **โชว์ remark ของ PV/AP ในแต่ละแถว** (italic, ellipsis, title=เต็ม) ให้ผู้ใช้เห็น/ตรวจว่าจัดงวดถูกไหม. (3) **การ์ด AP มีพื้น+กรอบสีตามสถานะ** (`PR_TINT`: ready เขียว `#f3fbf6`/`#bbf7d0` · waiting เหลือง · planned น้ำเงิน · paid/pending เทาอ่อน) ตามดีไซน์ Back to Black เดิม (เดิมขาว+เส้นซ้ายบางๆ ดูจืด). detail bg = `rgba(255,255,255,.6)` ซ้อนบน tint. **หมายเหตุ:** ถ้า PV จริงไม่ระบุ "งวดที่ N" ใน remark เลย จะยังจัดเข้า ADV ตามเดิม (ขึ้นกับคุณภาพ remark) — remark ที่โชว์ช่วยให้เห็นเหตุ. verify: combo "Advance 35%, งวดที่ 1-65%(หักประกันผลงาน)" → งวด 1 ✓ · ตัดมัดจำ/service → อื่นๆ · การ์ด ready พื้นเขียว.
- **★ build `i` (รอบ 6) — ปรับตาม feedback:** (1) **เปลี่ยนชื่อเมนู → "BACK TO BACK"** (app.jsx routes label + sidebar nav · title "BACK TO BACK · กระทบยอดการจ่าย AR↔AP") (2) **กรองเฉพาะโครงการลงนามแล้ว** = allow-list `status === 'Work in progress' || 'Finish'` (แทน deny-list เดิม — ตัด placeholder/ยังไม่ลงนาม/ยกเลิกชัดเจน) (3) **เอา "เกินกำหนด" (overdue) ออกทั้งหมด** — KPI card + tab + status (covered → 'ready' เสมอ ไม่มี aged>60; ตัด overdue จาก totals/okey/readySum/date-search) ตามที่ผู้ใช้บอก "ไม่ได้ใช้" (4) **เพิ่มช่องค้นหา** (`state.search`) กรอง code+ชื่อโครงการ+ผู้รับเหมา+ลูกหนี้ (substring, lowercase) ในแถบเครื่องมือ. verify: WS-69(ยังไม่ลงนาม)ถูกตัด · ไม่มี 'overdue' ใน statuses · ค้นหา "สันต้นกอก"→เหลือ ENC301 ✓.
- **★ build `g`/`h` (รอบ 5) — layout "การ์ดต่อโครงการ" (ตาม mockup ผู้ใช้):** แต่ละโครงการ = การ์ดแยก (rounded 14 + shadow-sm + เว้นช่อง 12px) + **แถบสีซ้าย 4px ตามสถานะรวม** (เขียว=ready/ส้ม=waiting/แดง=overdue/น้ำเงิน=planned/เทา=paid/teal=done) + **หัวการ์ด = grid คอลัมน์คงที่** `92px minmax(0,1fr) 132px 120px 158px 28px` (code badge · ชื่อ/ผรม. · มูลค่าสัญญา+label · mini badges · readySum + pill สถานะรวม "พร้อมจ่าย N งวด" · chevron) แทนตารางแถวต่อกัน (ตัด column header global ออก). **build h: เปลี่ยน flex→grid** เพราะ flex ทำให้จำนวน badge ต่างกัน (2 vs 3 vs ไม่มี AP) ดันคอลัมน์ขวาเลื่อนไม่ตรงแนว → grid คงที่ทุกการ์ดตรงแนวกัน. verify: คอลัมน์ value/badges/status/chevron x เท่ากันทั้ง 3 การ์ด (badge 2/3/ไม่มี AP) ✓ · borderLeft เขียว(ready)/ส้ม(waiting) + chip PL/STIIS + รายละเอียด PV/AP + รอตั้งเบิก ✓ (screenshot timeout = flakiness → DOM inspect).
- **★ build `f` (รอบ 4) — งวดต่อประเภทผลิตภัณฑ์ (data เงื่อนไข แก้ได้):** `PR_PRODUCT_NGUAD` (module const) = จำนวนงวดที่คาดหวังต่อ product type (ไม่รวม ADV): **PD/PDP/PDH = 1 (ADV + งวด 1 เท่านั้น)** · ประเภทอื่น default 2 (ADV+งวด1+งวด2). product type = suffix ท้ายรหัส (`prProductType`: `TTI043-PDH` → PDH) ดึงจาก invoices.jobNo / PV Project_Dpt / payable pre_des → `prodByCode[ncode]`. **เติม "งวดที่คาดหวัง" (scaffold) ที่ยังไม่มีเอกสาร AP/PV** = สถานะ `pending` ("รอตั้งเบิก": รับ AR แล้ว→รอผู้รับเหมาวางบิล / ยังไม่รับ→รอรับเงินก่อน) — เฉพาะโครงการที่มี AP จริงอยู่บ้าง (`rawGroups.length>0` กันโครงการเปล่ารก). **"จ่ายครบ" (done) = ทุกงวดที่คาดหวังจ่ายครบ** (PDH จ่าย ADV+งวด1 → done; จ่ายแค่ ADV → งวด 1 pending → ไม่ done). ชิปประเภทในหัว expand ("PDH · 2 งวด"). อนาคตเปลี่ยนงวดต่อ product ได้ที่ `PR_PRODUCT_NGUAD`. verify: PDH-จ่ายแค่ ADV→งวด1 pending/overall pending · PDH-จ่ายครบ→done · PL→คาด 3 งวด (งวด2 pending) · ชิป+โน้ตรอตั้งเบิก ✓.
- **★ build `e` (รอบ 3) — ปรับ UX ตาม feedback ผู้ใช้ (8 จุด):** (1) **expand view สวยขึ้น** = AP เป็น card ราย งวด (left-border สีตามสถานะ + chip งวด + section header มีจุดสี) (2) **ฝั่งจ่ายแล้วโชว์ PV no + วันจ่าย** · (3) **ฝั่งค้างจ่ายโชว์ AP no + ครบกำหนด** (รายละเอียดราย PV/AP ใต้แต่ละงวด — ไม่ต้องกดวางแผนก่อนถึงเห็นเลข AP) (4) modal วางแผนจ่าย **default บัญชี = 4863** (`prDefaultBankAc` หาเลขท้าย 4863) (5) **AR รับครั้งเดียว/รับครบ → ทุก AP งวดปลดล็อก (ready)** — `arFull = (arItems ทุกใบ received) || (Σรับ ≥ มูลค่าสัญญา×0.99)`; งวด N unlocked = `recvByNo[n] || arFull` (condLabel 'รับเงินครบแล้ว' + badge "รับครบแล้ว") — แก้บั๊กงวด 1 ขึ้น "รอเงื่อนไข" ทั้งที่รับเงินครบ (6) **ตัดงวด 'อื่นๆ'/'คืนประกัน' ทิ้ง** (โฟกัส ADV/งวด N เท่านั้น — งานเพิ่ม/refund ไม่เกี่ยว) (7) เพิ่มแท็บ **"จ่ายครบ"** (`overall==='done'`) (8) **แท็บกรอง multi-select** (`state.filters` array, OR logic, "ทุกโครงการ"=ล้าง; tab count = projCount ราย project). verify (mock): arFull→งวด1+2 ready ✓ · อื่นๆ หาย ✓ · PV-9@วันที่ + AP no@ครบ ✓ · default SCB·4863 ✓ · จ่ายครบ tab + multi-select active ✓.
- **★ build `d` (รอบ 2) — รื้อโมเดล AP ใหม่ทั้งหมด (แทน build b/c ด้านล่าง) ตามข้อมูลจริงจากไฟล์ผู้ใช้:** **ROOT FINDING (สแกน backup 15 มิ.ย. + ไฟล์ export จริง):** `payables.jobcode`/`pvVouchers.jobcode` = **เลขหมวดค่าใช้จ่าย (1–22) ไม่ใช่รหัสโครงการ** (0 ใบ match โครงการ). **รหัสโครงการฝังอยู่ใน 2 ที่:** `pvVouchers.Project_Dpt` (col A — `Project : (id) ENC165-PL-…` — **sync เก็บไว้แล้ว ใช้ได้เลย**) + `payables.pre_des` (col AF จาก export `v_ap_rptg14` — `TTI043-PDH-…` — **ไม่อยู่ใน synced payables ต้อง re-import**). `prCodeFromText()` แกะรหัสได้ทั้ง 2 รูปแบบ (verify 55/55 ตรงโครงการ).
- **AP มาจาก 2 แหล่ง · กรอง APS เท่านั้น** (`prIsApsPV`/`prIsApsPayable`: PV Type='APS'|AP_No เริ่ม APS · payable vchno เริ่ม APS — ตัด APO/APV ทิ้ง): **จ่ายแล้ว** = `pvVouchers` (Net_Amount/Pmt_Date) · **ค้างจ่าย** = `payables` (netpayment/due2, ตัดที่จ่ายผ่าน PV แล้วด้วย AP_No↔vchno). **แยกงวด ADV/งวด N จาก remark** (`prInst`: Advance/มัดจำ/ล่วงหน้า→ADV · "งวดที่ N"→งวด N · คืนประกัน/อื่นๆ). **จับคู่รายงวด (gate ที่ผู้ใช้เลือก "จับคู่รายงวด"):** ADV ← เซ็นสัญญา (`project.start`) · งวด N ← รับ AR งวด N (`installments[no==N].invoice.receivedDate`). สถานะราย group: `outAmt>0` → planned / waiting(AR งวดนั้นยังไม่รับ) / overdue(ปลดล็อก>60วัน) / ready ; ไม่งั้น `paidAmt>0` → paid. KPI: paid=Σ paidAmt · ready/overdue/waiting/planned=Σ outAmt ตามสถานะ. การ์ดโครงการ expand → ตาราง AR (งวด/ใบแจ้งหนี้/รับจริง) + **ตาราง AP รายงวด** (งวด · เงื่อนไข AR · จ่ายแล้ว · ค้างจ่าย · สถานะ+ปุ่ม).
- **ปุ่ม "วางแผนจ่าย" (เฉพาะงวด ready/overdue ที่มี payable ค้าง):** เขียน `forecastEntries` 1 แถวต่อ payable ในงวด (`EXPENSE_TYPE:'AP', REF_DOC:vchno, STATUS:'PLANNED', AMOUNT:'-net', JOB_NO:รหัสโครงการ, CATEGORY:'2'`) — รูปแบบเดียวกับ Bank Diary `BDApPanel` → โผล่หน้า **Bank Daily** + งวดขึ้น "วางแผนแล้ว" + ยกเลิกแผนได้. gate `canEdit`.
- **Phase 2 (import) — ไม่ต้องแก้โค้ด:** AP import (`DataPayablePage.handleImport`, หน้า DATA เจ้าหนี้คงค้าง) เป็น **header-name based + ไม่ whitelist field + Supabase JSONB เก็บทั้งแถว** → **อัปโหลดไฟล์ AP ที่มีคอลัมน์ `pre_des` (col AF, เช่น export v_ap_rptg14) 1 ครั้ง** payables จะมี `pre_des` เอง → AP ค้างจ่ายผูกโครงการทันที. (ก่อน re-import: ฝั่งจ่ายแล้ว/PV ขึ้นได้เลย; ฝั่งค้างจ่ายว่าง + banner เตือน.)
- **verify build d (preview, mock 2 โครงการครบเคส, isolated render เพราะ RLS):** Babel ผ่าน · 0 error · `prCodeFromText` ENC165(Project_Dpt)/TTI043(pre_des) ✓ · `prInst` ADV/งวด 1 ✓ · prBuildAll: ENC165 ADV+งวด1 paid(PV) · งวด2 ค้าง→**waiting**(AR งวด2 ยังไม่รับ) · ENC166 งวด1 ค้าง→**ready**(AR งวด1 รับแล้ว) · **APO ถูกตัดทั้ง PV+payable** ✓ · KPI จ่ายแล้ว1.5M/พร้อมจ่าย800k/รอเงื่อนไข700k · plan write `REF_DOC='APS2026060050' JOB_NO='ENC166' AMOUNT='-800000' STATUS='PLANNED'` ตรง Bank Diary ✓.
- ────────── (ด้านล่าง = บันทึก build b/c เดิม — โมเดล AP เก่า [payables.jobcode + running-coverage] ถูกแทนด้วย build d แล้ว) ──────────
- **คืออะไร:** หน้าใหม่ project-centric จับคู่ **AR (เงินรับจากลูกหนี้) ↔ AP (จ่ายผู้รับเหมา)** ต่อโครงการที่เซ็นสัญญา (กรองรายปี). รีธีมจาก Claude Design handoff "Back to Black · รายการจ่าย" (ผู้ใช้ส่งไฟล์ zip มา → อ่านผ่านไฟล์ ไม่ผ่าน `/design-login` ที่ติด auth) เป็นโทนแบรนด์น้ำเงิน `--brand-*` (คงสี semantic เขียว/แดง/เหลือง/เทา ของสถานะ). identifiers prefix **`PR/pr`** กัน [[global-scope-name-collision]]. self-contained — reuse `window.PCU`, `fmtMoney/fmtDate/parseDateFlexible/Modal/exportRowsToExcel`. **สิทธิ์:** staff/manager (แก้+วางแผนจ่ายได้) · **owner ดูได้อย่างเดียว** (ปุ่มเขียนถูก `canEdit` gate ปิด) · viewer ไม่เห็น (เดิม build `b` กัน owner ด้วย แต่ผู้ใช้เป็น owner เลยมองไม่เห็นเมนู → build `c` เอา `payment_recon` ออกจาก `owner.excludePages`). อยู่ใน sidebar "รายงาน/วิเคราะห์" ต่อจากกระทบยอดธนาคาร. **เมนูถูกกรองด้วย `WTPAuth.canViewPage` → deploy แล้วต้อง hard-refresh (Ctrl+Shift+R) เพราะ GitHub Pages cache `index.html` ~10 นาที** (รีเฟรชธรรมดาได้ตัวเก่า = ไม่เห็นเมนูใหม่).
- **แหล่งข้อมูล (อ่านอย่างเดียว ยกเว้นปุ่มวางแผนจ่าย):** `PCU.deriveProjects(baseProjects, invoices, receipts)` (dedup + fy + status + **AR ผ่าน `installments[].invoice`** = ivNo/balance/receivedDate) → กรอง `status !== 'ยกเลิก' && !== 'ยังไม่ลงนาม'` = เซ็นแล้ว. baseProjects เลือกชุดแถวมากกว่า (synced vs `loadLocalProjects`) กัน snapshot เก่าค้าง. **AP = `payables` ที่ `jobcode` (normalize `prNormCode` ตัด suffix รุ่น) = รหัสโครงการ** — ★ **ผู้ใช้ยืนยัน payables/pvVouchers มี `jobcode`/`jobname` จริง** (ผมเคยเข้าใจผิดว่าไม่มี — ตรวจ `Code.standalone.gs` header ยืนยัน: payables col `jobcode`+`jobname`, pvVouchers col `jobcode`) + PV-only (`pvVouchers.jobcode` ที่ไม่ match AP ใบไหน). paid = `pvVouchers.AP_No === payable.vchno`. ลูกหนี้ (customer) จาก `invoices.customerName`; ผู้รับเหมาหลัก = เจ้าหนี้ยอดรวมมากสุด.
- **Gate (ผู้ใช้เลือก "AR สะสม ≥ AP สะสม"):** `prComputeAp` เรียง AP ตามวัน (vchdate→due) running cumulative เทียบยอดรับจริงสะสม. สถานะรายใบ: `paid`(มี PV) > `planned`(มี forecast REF_DOC) > `ready`(covered cum≤รับจริง + ปลดล็อก≤60วัน) > `overdue`(covered แต่ปลดล็อก **>60วัน** = "ค้างนาน") > `waiting`(ยังไม่ covered, โชว์ "ขาดอีก ฿X"). **KPI 5 ใบ + แท็บ + outstanding scope ตามปีที่เลือก** (FY dropdown default ปีล่าสุด; `agg` คำนวณใน component หลังกรอง fyProjects ไม่ใช่ใน prBuildAll — กัน KPI รวมทุกปีแต่ป้ายบอกปีเดียว). มี date-search "จ่ายได้ภายในวันที่" + Export Excel.
- **★ ปุ่ม "วางแผนจ่าย" เขียนจริง — รูปแบบเดียวกับ Bank Diary `BDApPanel.handleBulkPlanAP` เป๊ะ (18 field):** เขียน `forecastEntries` row `{EXPENSE_TYPE:'AP', REF_DOC:vchno, STATUS:'PLANNED', AMOUNT:'-<net>', JOB_NO:รหัสโครงการ, PROJECT_NAME, CATEGORY:'2'(โครงการ), Bank_AC, id:WTPData.newId(), …}` → ไปโผล่หน้า **Bank Daily** (BDApPanel อ่าน `plannedRefs`/`plannedDateByRef` จาก REF_DOC) + หน้านี้ detect planned ผ่าน `forecast REF_DOC===vchno && !ACTUAL` → โชว์ "วางแผนแล้ว" + ปุ่ม **"ยกเลิกแผน"** (ลบ forecast ที่ผูก REF_DOC ที่ยังไม่ ACTUAL — mirror `handleBulkUnplanAP`). gate `canEdit` (staff/manager). modal ถาม วันจ่าย + บัญชี (จาก `data.bankAccounts`). ตรงโจทย์ผู้ใช้: "กดวางแผนจ่ายแล้วขึ้นที่ BANK DAIRY + หน้านี้ดูรายการที่วางแผนได้ด้วย".
- **verify (preview, login-gate+RLS → isolated render `<PaymentReconPage>` ลง overlay + mock ครอบทุกสถานะ + inject session manager, ตรวจ DOM/eval):** Babel ผ่านทุกไฟล์ · 0 console error · `prBuildAll` คำนวณถูก (running coverage: เจ้าหนี้ B covered→ready, C ไม่ covered→waiting "ขาดอีก 500,000", E ปลดล็อก ~250 วัน→overdue, A/F มี PV→paid, D มี forecast→planned) · KPI/outstanding/แท็บ scope ตามปี (FY69 outstanding 3.1M) · FY dropdown สลับ 69↔68 · expand โชว์ AR (INV + วันรับจริง/รอรับ) + AP (เจ้าหนี้/PV/เงื่อนไข/สถานะ) · **plan write row ตรง Bank Diary 18 field** (REF_DOC='WPA-002', EXPENSE_TYPE='AP', STATUS='PLANNED', AMOUNT='-1500000', JOB_NO, CATEGORY='2', id_…) + toast + modal ปิด · หลังวางแผน B→planned (ปุ่มวางแผนหาย, ยกเลิกแผนโผล่) · viewer (ไม่ล็อกอิน) = ไม่มีปุ่ม (canEdit gate). *(ปุ่ม + ข้อมูลจริง verify ใน prod ไม่ได้เพราะ RLS — ใช้ mock + DOM/eval ตาม [[preview-login-gate-verify]])*
- **robust + diagnostic (build `c`, ตอบ "เปิดมาไม่มีข้อมูล ต้องเติมอะไร"):** เพิ่มตัวเลือก **"ทุกปี"** ใน FY dropdown (`fy==='all'` — กันโครงการที่ `deriveFy=0` หาย เพราะ fySet นับเฉพาะ fy truthy) + บรรทัดสรุป **"เซ็นแล้วรวม N · มี AP M"** (`diag`) + empty-state บอกสาเหตุ (signed=0 → ตรวจ login/sync · fyProjects=0 → ลอง "ทุกปี" · ตัวกรอง → กด "ทุกโครงการ") + **banner เตือนเมื่อมีโครงการแต่ AP ว่างหมด** (`fyHasAp=false` → "payables/PV ต้องมี jobcode = รหัสโครงการ"). ใช้หน้านี้ self-diagnose ได้ว่าขาดข้อมูลตรงไหน (อ่านข้อมูลจริงผ่าน RLS ไม่ได้). **ของที่หน้าต้องมีถึงจะครบ:** project มี `Start`/มูลค่าสัญญา (→ fy + signed) · invoices `jobNo`=รหัสโครงการ (AR) + receipts ผูก `invoiceNo` (วันรับ) · payables/PV มี **`jobcode`=รหัสโครงการ** (AP) · PV `AP_No`=payable `vchno` (paid).
- **wiring:** `index.html` (script `?v=20260624m` หลัง page_bank_recon + `app.jsx`→`20260624c`), `app.jsx` 4 จุด (routes config + render switch + sidebar nav; owner **ไม่** อยู่ใน excludePages แล้ว). **ข้อจำกัด:** AP ใบที่ `jobcode` ว่าง = ไม่เข้าโครงการ (banner เตือน) — ตรงที่ผู้ใช้บอก "เดี๋ยวขาดข้อมูลเอา PV มาเติม".

## 2026-06-24 — Bank Recon: แบ่ง 2 หน้าย่อย (sub-nav) + หน้าใหม่ "เทียบ Mango ERP ↔ STM" (build `page_bank_recon 20260624f`)
- **คำขอผู้ใช้ (เตย):** แบ่งหน้า `#bank_recon` เป็น 2 หน้าย่อย — (1) เดิม สำหรับ Weekly Forecast (เทียบ PV/ใบรับ ในระบบ WTP) (2) ใหม่ เทียบ**งบกระทบยอดจากรายงาน Mango ERP** กับ STM จริง เพื่อหารายการลงบัญชี **ขาด/เกิน**. เลือกผ่าน AskUserQuestion: **แท็บย่อยในหน้าเดียว** + **ใช้ STM ที่ import หน้าเดิมร่วมกัน**.
- **sub-nav (`mainTab` state: 'forecast'|'mango')** — ปุ่ม 2 อันใต้ account selector (account/เดือน/STM ใช้ร่วม). content forecast เดิม (ฝั่งจ่าย 5 tabs + ฝั่งรับ AR + statement) ห่อใน `{mainTab==='forecast' && <React.Fragment>…}`; แท็บ mango = `<BRMangoTab>`.
- **โครงสร้างไฟล์ Mango จริง (จาก `05.2026 งบกระทบยอด Mango-4863.xlsx` ที่ผู้ใช้ส่ง — ต่างจาก skill `mango-merge-recon` เล็กน้อย):** sheet เดียว, R1 header (BK.Date·Voucher No.·Voucher Date·Vendor·Cheque No.·Cheque Date·Remark·Debit·Credit·Balance), R2 **Balance Forward**, R3..=รายการเดือนนี้, แถว **"Bank as Date DD/MM/YYYY"**=ยอด book ปลายเดือน (จบส่วน 1), แถว **"Outstanding Cheque"**=หัวส่วน 2, ส่วน 2=เช็คค้างยังไม่ขึ้นธนาคาร (PV เก่าปี 22-25). **Debit=เงินเข้า(+) · Credit=เงินออก(ค่าลบ) → amount=Debit+Credit (sign ตรงกับ STM +เข้า/−ออก พอดี)**. วันที่เป็น string DD/MM/YYYY. **หลายแถว Debit=Credit=0** (ตัดมัดจำ/รับสินค้า=JV ไม่กระทบเงินสด) → engine ตัดทิ้ง.
- **`brParseMango(aoa, fileName)`** (engine ใหม่) — header-driven (หาแถว header จาก "voucher"+"debit"/"credit"), เลขบัญชีจากชื่อไฟล์ (`(\d{4})(?!.*\d)` = 4 หลักท้าย), แยก phase ด้วย marker ใน Vendor column (Balance Forward→opening · Bank as Date→bankAsDate/Bal · Outstanding→ส่วน 2) → `{account, opening, bankAsDate, bankAsDateBal, movements[], outstanding[]}`. ใช้ `brToISO`/`brNum` เดิม (รองรับ Date/DD-MM-YYYY/พ.ศ./comma).
- **`brReconcileMango({movements, stmLines, amtTol, dateWindow=5})`** — เทียบ Mango ส่วน 1 (เฉพาะ `|amount|>tol`) ↔ STM (`lines` ของ acct+เดือน): match amount รวม sign (±0.01) + วัน (±5) + เลขเช็ค → **3 ถัง: `matched` · `stmOnly`** (มีใน STM ไม่มีใน Mango = ธนาคารหักแล้วบัญชียังไม่ลง = **ขาด**) **· `bookOnly`** (มีใน Mango ไม่มีใน STM = บัญชีลงแล้วธนาคารยังไม่มี = **เกิน/เช็คค้าง**).
- **`BRMangoTab` (component ใหม่)** — อัปโหลดไฟล์ Mango (`brParseFile`→`brParseMango`, ลองทุก sheet) เก็บใน state (ไม่ persist — ตรวจครั้งคราว) → KPI (ยอด book/ตรงกัน/ขาด/เกิน) + 4 tabs (ขาด default·เกิน·ตรงกัน·Outstanding) read-only. เตือนถ้าบัญชีไฟล์ ≠ บัญชีที่เลือก (`bdAcctMatchesCheck`) หรือยังไม่มี STM เดือนนั้น.
- **verify (preview, isolated render + mock โครงสร้างไฟล์จริง — login-gate+RLS):** Babel ผ่าน 0 error, fragment balance 1/1 · parser: account `4863`/opening 3.17M/Bank as Date 2026-05-31=2.29M/movements 4/outstanding 2 · engine: ตัด JV→3 moves · matched 2 · **bookOnly(เกิน) PV10 −3M · stmOnly(ขาด) S3 −500k** · BRMangoTab: empty→upload (mock xlsx ผ่าน DataTransfer+dispatch change)→4 tabs counts ตรง engine + KPI 2,286,151 + ทุก tab เนื้อหาถูก · BankReconPage เต็ม: sub-nav 2 แท็บ สลับ forecast↔mango ไม่ crash.
- **ค้าง/อนาคต:** Mango ไม่ persist (อัปโหลดใหม่ทุกครั้งเปิดแท็บ) · ยังไม่ cross-check outstanding ที่ "เพิ่งขึ้น STM" · ยังไม่มีปุ่ม action (ลงบัญชี/จับคู่เอง) — เฟสแรกเป็น diagnostic ดูอย่างเดียว (เหมือน AR section).

## 2026-06-24 — Bank Recon: เพิ่มฝั่งรับ (AR) — กระทบเงินเข้าจริง ↔ ใบรับเงิน (build `page_bank_recon 20260624e`)
- **คำขอผู้ใช้ (เตย, ต่อจากการ์ด recon Section 03):** การ์ด recon ตรวจระดับ "ยอดรวม" → ถ้า "เงินเข้าที่ดึงมา" (IV/loan actual = มาจากการคีย์/มาร์คในระบบ ไม่ใช่ statement) ≠ เงินเข้าธนาคารจริง จะ**ดิฟเสมอ + แยกไม่ออกว่าฝั่งไหนผิด**. ต้องตรวจ "เงินเข้ามีรายการเกิดขึ้นจริงตามยอดที่ดึงมาไหม" **รายตัวกับ statement ธนาคารจริง** (เลือก approach นี้ผ่าน AskUserQuestion = "แม่นสุด").
- **ใช้ของที่มีอยู่แล้ว:** หน้า Bank Reconciliation import statement จริง + `brReconcile` คืน **`inLines`** (เงินเข้า = credit, `amount > 0`) อยู่แล้ว — เดิมเทียบเฉพาะฝั่งจ่าย (PV). รอบนี้เพิ่ม**ฝั่งรับ** = เทียบ `inLines` ↔ **receipts (ใบรับเงิน)**.
- **`brReconcileAr({ inLines, receipts, amtTol, dateWindow })` (engine ใหม่ module-level, หลัง `brFindSubset`)** — 1:1 amount+date matching (ไม่มี pass ref/subset แบบฝั่งจ่าย). คืน 3 ถัง: **`matched`** (เงินเข้าจริง ↔ ใบรับ) · **`receiptNoDeposit`** (ระบบบันทึกรับแล้ว แต่ไม่มีเงินเข้าจริง = "ยอดที่ดึงมาไม่จริง" ★ ตอบโจทย์เป๊ะ) · **`depositNoReceipt`** (เงินเข้าจริง แต่ยังไม่มีใบรับ = ดอกเบี้ย/เงินกู้/โอนเข้า/IV ยังไม่บันทึก). **★ ลองทั้ง `netReceived` (เงินเข้าหลังหักโอนสิทธิ — ปกติเข้าบัญชีจริง) และ `grossAmount`** (เผื่อเข้าเต็มแล้วโอนออกแยก) — ตัวใดตรง statement ก็เจอ. `dateWindow=7` (เงินเข้า clear ช้ากว่าวันออกใบรับ).
- **receipt accessors (robust seed/synced UPPER):** `brRcDate` (receiptDate→RECEIPT_DATE) · `brRcNet` (netReceived→grossAmount) · `brRcGross` · `brRcBank` (bankAccount มีเลขบัญชี → `bdAcctMatchesCheck` เลขท้าย 4).
- **`BRArSection` (component ใหม่, read-only)** — render ใต้ section ฝั่งจ่าย ก่อน `BRStatementTable`. หัวการ์ด "💰 กระทบยอดเงินเข้า (รับจริง)" + 3 tabs (default = `receiptNoDeposit` สีแดง สำคัญสุด): บันทึกรับ·ยังไม่เจอเงินเข้า / ตรงกัน / เงินเข้า·ยังไม่มีใบรับ. แต่ละ tab = ตาราง + footer รวม. **ยังไม่มีปุ่ม action (ดูอย่างเดียว)** — เฟสแรกเป็น diagnostic; manual-match ค่อยเพิ่มถ้าจำเป็น.
- **wiring (`BankReconPage`):** `receiptsForAcct` memo (receipts ที่ `receiptDate` เดือนนี้ + บัญชีตรง; ไม่ระบุบัญชี → รวมไว้เทียบยอด/วัน) + `arRecon = brReconcileAr({ inLines: recon.inLines, receipts: receiptsForAcct })` → `<BRArSection arRecon acct />`. subtitle หน้าเปลี่ยนเป็น "จ่าย: PV ↔ statement · รับ: เงินเข้า ↔ ใบรับเงิน". ฝั่งจ่ายเดิม (5 tabs) **ไม่แตะ**.
- **verify (preview, isolated render + mock ครอบทุกเคส — login-gate+RLS):** Babel ผ่าน 0 error · engine: L1/L2↔RC1/RC2 matched (RC2 หักโอน net 4.5M วันต่าง 2 → via net) · **RC-003 = receiptNoDeposit** (บันทึกรับ 3.2M ไม่มีเงินเข้าจริง) · ดอกเบี้ย 500k = depositNoReceipt · เงินเข้าเต็ม 6.5M ↔ ใบรับ net4.5M/gross6.5M → matched **via gross** · UI: 3 tabs counts ตรง engine, default tab แดง (oklch hue 22) โชว์ RC-003 + รวม 3,200,000 + คำเตือน, สลับ tab matched/extra เนื้อหาถูก.

## 2026-06-24 — Weekly Forecast: การ์ด "ตรวจสอบยอดดิบ" Section 03 (Cash Reconciliation, manager-only) (build `page_cashflow 20260624f`)
- **คำขอผู้ใช้ (เตย):** พอร์ตวิธีตรวจสอบยอดที่เคยทำใน Excel มาไว้บนหน้า Weekly Forecast — **ยกมา (B/F) + รับโครงการ (IV) + เงินกู้ − ค่าใช้จ่าย = ยอดสุทธิใช้ได้** ซึ่ง "ทำจริงต้องตรงกับเงินในธนาคารจริง". ตรง = เขียว ✓ / ไม่ตรง = แดง ⚠ + บอกส่วนต่าง + ตารางรายสัปดาห์ชี้ว่ายอดเริ่มขาดสัปดาห์ไหน. **เห็นเฉพาะ role `manager`** ("คนอื่นไม่จำเป็นต้องมาตรวจ").
- **`CfReconCard` (component ใหม่ก่อน `CashFlowDashboard` ใน [page_cashflow.jsx](app/page_cashflow.jsx))** — render เป็น "Section 03" ใต้ Section 02 (ก่อน drill-down modals). gate ด้วย `{cfIsManager() && <CfReconCard .../>}`. **`cfIsManager()`** (helper ใหม่ข้าง `cfIsReadOnly`) = `wtp-session.role === 'manager'`.
- **Grand recon:** `calculated = monthBFAvailable + ivActual + loanActual − outflowActual` (ใช้ค่าที่หน้าคำนวณอยู่แล้ว: B/F=available หลังหัก HOLD, ivActual/loanActual=`sumArr(...actual)`, outflowActual=Σ `pvActualByWeekCat`) เทียบ `liveAvailable` (ยอดสดวันนี้ − HOLD). `grandDiff = calculated − liveAvailable`; ตรง = `|diff| ≤ 1` บาท (กันเศษสตางค์). โชว์ banner + กล่องสูตร + 3 ช่อง (คำนวณได้ / ธนาคารจริง / ส่วนต่าง).
- **★ ตารางตรวจรายสัปดาห์ (telescoping):** แต่ละสัปดาห์ **เปิดด้วยยอดจริงสิ้นสัปดาห์ก่อน** (`prevClose` = actual snapshot ของ `weeks[i-1].toISO` ผ่าน `getBalanceAtDate − getHoldAtDate`; สัปดาห์ปัจจุบัน = `liveAvailable`; อนาคต = null) → `calc[i] = opening + inflowActual[i] − outflowActual[i]`, `diff[i] = calc − actualEnd`. เพราะเปิดด้วย "ยอดจริง" ไม่ใช่ "ยอดคำนวณ" → **ส่วนต่างจะโผล่เฉพาะสัปดาห์ที่รายการตกหล่นจริง** (พิสูจน์: Σ weekDiff = grandDiff). แถวแรกที่ `|diff|>1` = `firstBreak` ไฮไลต์แดง + left-border + footer "🔎 ยอดเริ่มขาดที่ Wn". สัปดาห์อนาคต = "รออนาคต" (ไม่เทียบ).
- **verify (preview, isolated render `CfReconCard` 2 เคสตามรูปผู้ใช้, DOM/eval — login-gate+RLS):** Babel ผ่าน 0 error · **PASS:** ✓ ยอดตรงกัน, สูตร 2,924,226.17 + 4,805,469.00 + 1,473,275.00 − 7,597,582.34 = 1,605,387.83, 3 ช่อง diff 0.00, ทุกสัปดาห์ ✓ · **FAIL:** ⚠ ยอดไม่ตรง diff −1,318,838.34 (ธนาคารจริง 2,924,226.17), **W2 = break row** (แดง + left-border) ส่วน W1/W3/W4 ✓ — ตรงรูปเป๊ะ · สี oklch hue 152=เขียว(pass)/22=แดง(fail) ครบ banner/icon/diff-box/break-row.

## 2026-06-25 — Weekly Forecast Section 03: ย่อ default + ซ่อนตอนปริ๊น/นำเสนอ (build `page_cashflow 20260625e`)
- **คำขอผู้ใช้:** Section 03 "ตรวจสอบยอดดิบ" (`CfReconCard`) ไม่อยากให้โชว์รายการจนกว่าจะกดเปิด (แม้เป็น manager) + ไม่โชว์ตอนปริ๊น PDF + ไม่โชว์ในโหมดนำเสนอ.
- **FIX (UI ล้วน):** (1) **ย่อ default** — เพิ่ม `const [collapsed, setCollapsed] = cfState(true)`; แทน `<SectionTitle>` ด้วยหัวข้อ clickable (toggle collapse, ป้าย "กดเพื่อดู ▶ / ย่อ" + ลูกศรหมุน); ห่อเนื้อหา 2 การ์ด (banner+สูตร+3ช่อง + ตารางรายสัปดาห์) ใน `{!collapsed && (<React.Fragment>…</React.Fragment>)}` → เห็นแค่หัวข้อจนกว่าจะคลิก. (2) **ซ่อนปริ๊น+นำเสนอ** — เพิ่ม class **`no-present`** ที่ root `cf-section-03` → CSS เดิม `body.present-mode .no-present { display:none }` ([styles.css:627](app/styles.css)) ซ่อนในโหมดนำเสนอ **และตอนปริ๊น** (ปุ่มพิมพ์ cashflow เพิ่ม `present-mode` + `cf-print-mode` ทั้งคู่ — [page_cashflow.jsx:1639](app/page_cashflow.jsx)) → ครอบทั้ง 2 กรณีด้วย class เดียว ไม่ต้องเพิ่ม CSS.
- **verify (preview):** Babel transform ผ่าน 0 error, fragment balance 1/1, มี `cf-section-03 no-present` + `collapsed` state.

## 2026-06-25 — Weekly Forecast Section 01: หัวข้อ "กระแสเงินสดออก" + ปุ่มสลับโหมด ทำให้เหมือน BIO (build `page_cashflow 20260625f`)
- **คำขอผู้ใช้:** ปุ่มสลับ "ประมาณการคงเหลือ / แผนจ่ายจริง (AP)" + ขอบเขต "ทั้งเดือน / เฉพาะสัปดาห์นี้" ที่หัวแถว outflow (Section 01) อยากได้สวยแบบ BIO.
- **FIX (copy โครงสร้างจาก BIO 1:1):** เปลี่ยนหัว `<td>` outflow จาก "title + ปุ่ม pill ลอย 2 อันแยกกัน (borderRadius 99 รายปุ่ม)" → **flex `justify-content:space-between`** (title ซ้าย / ปุ่มขวา) + ปุ่มเป็น **segmented control** (กล่อง 1 อัน `borderRadius:8 overflow:hidden border:1px` ปุ่มข้างใน `border:none` ติดกันเป็นแถบเดียว). โหมดจ่าย = ขอบ/active สี `var(--bad)` (แดง, เข้ากับ outflow); ขอบเขต = สี `var(--ink-*)`. subtitle `fontWeight:500` + bold คำสำคัญ.
- **★ แก้บั๊ก CSS ติดมา:** ปุ่มเดิม `padding: \`1px ${cfScale(7)}px\`` → `cfScale(7)` คืน `calc(7px * var(--cf-k,1))` แล้ว `+ 'px'` = `calc(...)px` (invalid). โครงใหม่ใช้ `padding: \`${cfScale(4)} ${cfScale(9)}\`` (ไม่ต่อ `px`) → หาย. verify: `${cfScale(N)}px` เหลือ 0 ที่.
- **verify (preview):** Babel ผ่าน 0 error, segmented border + flex space-between + ink-400 scope border ครบ, buggy `}px` = 0. (ข้อมูลจริง verify ไม่ได้เพราะ login+RLS — โครงสร้างเหมือน BIO ที่ verify แล้ว.)

## 2026-06-24 — แผนที่จังหวัด: ไม่มีขอบเวลาซูม + คลิกจังหวัด → ดูโครงการ POG (build `thaimap 20260624c` / `page_investor 20260624d`)
- **Fix "ขอบๆ" เวลาซูม:** SVG เดิมมี `overflow:'visible'` → จังหวัดขอบๆ ล้นออกนอก viewport เห็นเป็น "กรอบ". แก้: ลบ `overflow:'visible'` จาก SVG + เพิ่ม `overflow:'hidden'` ที่ wrapper div ของ inline map.
- **Fullscreen เต็มจอจริง:** เดิม `display:'inline-block' + height:'82vh'` ทำให้แผนที่ไม่ fill ความกว้าง. แก้: wrapper fullscreen = `height:'100%'` (flex child ของ parent ที่ `flex:1`) + SVG = `height:'100%', width:'auto'`.
- **Fullscreen overlay มี sidebar โครงการ:** เปลี่ยน center area เป็น `display:'flex', gap:12` — แผนที่ `flex:1` ซ้าย + `provPanel()` (290px) ขวา เมื่อมีจังหวัดถูกเลือก. ไม่มีการเลือก = แผนที่เต็มกว้าง.
- **คลิกจังหวัด → panel โครงการ (`provPanel`):** state ใหม่ `selProv` (ชื่อจังหวัดไทย, toggle ได้) เพิ่มใน `ThaiMap` ก่อน early-return. กดจังหวัดซ้ำ = ปิด. panel โชว์: ชื่อจังหวัด + ภาค + จำนวนโครงการ + ปุ่ม ✕; ถ้ามี `props.provProjects[selProv]` = รายการโครงการ (code/site/type/status/amount ต่อใบ); ถ้าไม่มี = "ไม่มีรายละเอียดโครงการ". inline map: panel โชว์ใต้แผนที่ (`marginTop:10`) เมื่อ `selProv && !full`.
- **`provProjects` prop ใหม่ใน `ThaiMap`:** `{ '<ชื่อจังหวัดไทยปกติ>': [{code, site, type, status, amount}] }` — key = ชื่อหลัง normalize (ตัด จ./ + ALIAS).
- **`byProvProjects` ใน `invMetrics` (`page_investor.jsx`):** helper `invNormProv` (inline) normalize ชื่อจังหวัดจาก `r.province` → key ตรงกับ `selProv` ของ ThaiMap. build ไปพร้อม `byProv` loop. `r.site` = ชื่อโครงการ, `r.contractNo` = เลขสัญญา. return ใหม่รวม `byProvProjects`.
- **Pass through `InvThaiMap` → `ThaiMap`:** `InvThaiMap` รับ `provProjects` prop ใหม่ + forward ไป `window.ThaiMap`. call site ใน `InvCustomers` ส่ง `provProjects: m.byProvProjects`.

## 2026-06-25 — แผนที่จังหวัด: panel โครงการลอยบนแผนที่ + จัดกลุ่มตามประเภทสินค้า (build `thaimap 20260625b`)
- **คำขอผู้ใช้ 3 ข้อ:** (1) panel ลอยอยู่บนแผนที่แทนเป็น sidebar ข้าง (2) จัดกลุ่มตามประเภทสินค้าก่อน คลิกกางดูโครงการ (3) ขยายกว้างขึ้น.
- **FIX 1 — floating panel (`position:absolute`):** `provPanel(isFull)` เปลี่ยน `outerStyle` จาก column flex layout ข้าง → `position:'absolute', top:8, right:8, bottom:8, width:380, zIndex:10` เฉพาะ `isFull===true`. Inline mode (แผนที่เล็กใน §7) ยังแสดงด้านล่างแผนที่ตามเดิม (`position` ปกติ). ต้อง wrap `mapArea` ใน `position:relative` อยู่แล้ว → panel ลอยได้เลย.
- **FIX 2 — กลุ่มประเภทสินค้า + expand/collapse:** state ใหม่ `grpExp` (`{}`) หลัง `selProv`. reset effect (`setGrpExp({})`) เมื่อเปลี่ยนจังหวัด. `provPanel` group projects ด้วย `prj.type` → เรียง count desc → แต่ละกลุ่มมีหัว (▸/▾ + ชื่อ + badge จำนวน + ยอดรวม) + กดกาง → list รายโครงการ (code/site/status/amount) ใน div padding. ยุบทั้งหมดเป็น default (expand เฉพาะที่กด). `setGrpExp(prev => {...prev, [t]: !prev[t]})`.
- **FIX 3 — กว้าง 380px** (จาก 290px) เฉพาะ fullscreen floating panel.
- **Controls ย้ายซ้าย:** เดิม controls อยู่ขวาบน → ชนกับ panel (ขวาบน). fullscreen: `ctrlStyle.left=8` แทน `right=8`, zIndex=11 > panel 10.
- **Overlay simplify:** เดิม overlay = flex row (mapArea + sidebar column). ตอนนี้ panel อยู่ใน `mapArea` แล้ว → overlay middle = `el('div', {style:{flex:1, minHeight:0, overflow:'hidden'}}, mapArea(true))` เท่านั้น (ลบ flex-row wrapper + `selProv ? sidebar : null`).
- **`mapArea` ส่ง `provPanel(true)` ข้างใน** หลัง `tipFor(isFull)` (เฉพาะ `isFull`).

## 2026-06-25 — หน้า #users รวมทุกแหล่ง + จัดการสิทธิ์รายคน + admin ผ่าน service_role key (build `page_users 20260625u` / `data_supabase 20260625u` / `app.jsx 20260625u`)
- **เหตุผล:** ก่อนหน้านี้ user มีอยู่ 3 ที่แยกกัน (config.js USERS / Supabase Auth accounts / data.users Sheet entity) → ต้องใช้ 3 tool คนละที่เพื่อจัดการ. รอบนี้รวมเป็นหน้า Users หน้าเดียว: เห็นทุกรายชื่อ, แก้รหัสผ่านได้, เพิ่ม/ลบได้, กำหนดสิทธิ์รายหน้าได้.
- **`WTPData.adminInit(serviceRoleKey)` + `WTPData.adminReady()` + `WTPData.admin.*` (`data_supabase.js`):** สร้าง `adminSb` (Supabase client แยก ใช้ service_role) — **แยกออกจาก `sb` client ปกติ (anon+JWT) เพื่อกัน key ซ้ำ**. เมธอด admin: `listUsers()` (paginate 200/page), `createUser({username,displayName,role,password,pages})`, `updateUser(id, patch)`, `setPassword(id, password)`, `deleteUser(id)`. service_role key **เก็บใน `sessionStorage 'wtp-srk'` เท่านั้น** (หายเมื่อปิดแท็บ, ไม่เก็บ localStorage, ไม่ในรีโป).
- **`WTPAuth.canViewPage(route)` ใหม่ (`app.jsx`):** ตรวจ `session.pages` (array = per-user override) ก่อน → ถ้า null ถอย fallback role default (`ROLE_PERMS`). `WTPAuth.defaultPagesFor(role, allRoutes)` คืน list ของหน้าที่ role นั้นเห็น default.
- **`authSignIn` คืน `pages` (`data_supabase.js`):** อ่าน `app_metadata.pages` จาก JWT → session.pages (null ถ้าไม่ใช่ array, หรือ array ของ route keys). ค่า null = ใช้ role default, array = override เป๊ะตามที่กำหนดให้คนนั้น.
- **`PAGE_GROUPS` + `ALL_PAGES` (`page_users.jsx`):** 4 กลุ่ม (แดชบอร์ด 9 / รายงาน/วิเคราะห์ 10 / จัดการข้อมูล 8 / ระบบ 2 = รวม 29 route keys). ใช้ใน checkbox grid เลือกสิทธิ์รายหน้า.
- **`ServiceRoleCard` (line ~499):** กล่องวาง service_role key ที่ด้านบนหน้า Users. key จะถูก `adminInit()` แล้วเก็บใน `sessionStorage` — โชว์แค่ 8 ตัวอักษรแรก + ปุ่ม "ล้างคีย์". เมื่อวาง key → โหลด Supabase Auth users อัตโนมัติ (`loadAuthUsers`).
- **`UserEditModal` (line ~559):** ฟอร์มแก้ username/role/displayName + **per-page access section**: radio "ใช้ default ของ role" vs "กำหนดเอง". กรณี "กำหนดเอง" = checkbox grid 4 กลุ่ม, มี group-level select-all (indeterminate state), shortcuts "เลือกทั้งหมด / ไม่เลือกเลย / ← default ของ role". บันทึก = `admin.updateUser` เขียน `app_metadata.{role, displayName, pages}`.
- **`PasswordResetModal` (line ~737):** input รหัสแบบ show/hide + ปุ่ม "🎲 สุ่มรหัส" (10 ตัว alphanumeric) + คำเตือน "คัดลอกไว้ก่อนปิด (ดูภายหลังไม่ได้)". บันทึก = `admin.setPassword`.
- **`combinedRows` memo (UsersPage):** merge 3 แหล่ง → Supabase Auth เป็นหลัก + ติดแท็ก `_sources` Set (`'auth'/'sheet'/'config'`). ตารางโชว์ badge สี (Auth เขียว / Sheet น้ำเงิน / config.js อำพัน) + Username / ชื่อแสดง / Role / "สิทธิ์เข้าหน้า" (badge จำนวนหน้า หรือ "role default") / lastSeen / ปุ่ม (เปิดเฉพาะ inAuth). เพิ่มผู้ใช้ใหม่ = สร้าง Supabase Auth user ทันที (ต้องมี service_role).
- **Security model:** service_role ไม่เคยอยู่ใน repo/localStorage — วางต่อ session เท่านั้น. `adminSb` client แยกจาก `sb` → JWT ของผู้ใช้ปัจจุบันไม่ถูกผสม. per-user pages มีผลตอน login ครั้งถัดไป (JWT-based).
- **⚠️ pages override มีผลตอน "login ใหม่" เท่านั้น** — session เดิมที่ค้างอยู่ยังเห็นหน้าตาม session เก่า. ต้องล็อกเอาท์แล้ว login ใหม่ถึงรับ pages ที่เพิ่งแก้.

## 2026-06-25 — Bank Diary การ์ดบัญชี: รวมรายการ "เจ้าหนี้เดียวกัน" ในวันเดียว (แม้มาจากคนละหน้า/คนละชนิด) (build `page_bank_diary 20260625b`)
- **(build `b`) แก้ตาม feedback "บางอันไม่ยอมยุบ":** build `a` เปลี่ยน single-item creditor → `BDItemRow` (แถวกางเต็ม โชว์ชื่อยาว+AP+remark) ทำให้รายการใบเดียวดู "ไม่ยุบ" ไม่เสมอกับกลุ่มหลายใบ. คืนเป็น **render `BDDayItemGroup` ทุกรายการที่จับชื่อเจ้าหนี้ได้ (แม้ใบเดียว)** → แถวยุบกะทัดรัด (▶ + "· N รายการ" + ชื่อ canonical) เสมอกันทั้งลิสต์ เหมือนพฤติกรรมเดิมก่อนแก้. เงื่อนไข: `(g.items.length > 1 || g.items[0].creditor)` → group; เฉพาะรายการไม่มีเจ้าหนี้ (โอน ฯลฯ) → `BDItemRow` เดี่ยว. verify: single forecast = "▶ ประมาณการ · บริษัท อินโนวาเทค… · 1 รายการ", โอน = แถวเดี่ยวไม่มี ▶.
- **อาการ (ผู้ใช้):** ในการ์ดบัญชี (วันที่กางออก) เจ้าหนี้รายเดียวกันโผล่เป็นหลายบรรทัดแยกกัน เพราะ forecast มาจากคนละหน้า (BDApPanel / page_payment_recon / ฯลฯ) → `DESCRIPTION` คนละรูปแบบ (คำนำ "จ่าย", เลขที่ AP/งวด ต่อท้าย) → เดิมจับกลุ่มด้วย `it.group = desc.replace(/\(...\)$/)` ที่ตัดได้แค่วงเล็บ "ท้ายสุด" → เพี้ยนนิดเดียวก็ไม่ merge. อยากให้ **รวมเป็นบรรทัดเดียว** แต่กางดูแล้ว **เห็นว่าแต่ละรายการมาจากแหล่งไหน**.
- **FIX (ฝั่งแสดงผลล้วน ไม่แตะ compute/sync — net/running คำนวณจาก raw items เหมือนเดิม):**
  - **`bdVendorCanon(name)` (helper ใหม่ หลัง `bdItemTag`)** — ดึง "ชื่อนิติบุคคล" มาตรฐาน: regex จับช่วง `บริษัท|บมจ.|บจก. … จำกัด (มหาชน)?` (non-greedy, ทนคำนำ-ท้าย); ไม่ match → fallback ตัดกริยานำหน้า (จ่าย/ชำระ/รับ) + วงเล็บ/หางอ้างอิงท้าย. coerce `String(x==null?'':x)` ก่อน (กัน synced number → `.replace` พัง ตาม [[global-scope-name-collision]] pattern). คืน '' ถ้าจับไม่ได้.
  - **`bdBuildAccountView` แนบ `creditor: bdVendorCanon(...)`** ให้ items: check (`c.payee`), forecast (`f.desc`), pv (`g.payee`). transfer ไม่แนบ (counterparty เป็นบัญชี ไม่ใช่เจ้าหนี้).
  - **`BDDayGroup` reducer เปลี่ยน key** จาก `'g:'+kind+':'+group` → **`'c:'+(signed<0?'o':'i')+':'+creditor.toLowerCase()`** (จับกลุ่มข้ามชนิด, แยกทิศรับ/จ่าย). ไม่มี creditor → เดี่ยว (`'i:'+i`). กลุ่ม >1 รายการ → `BDDayItemGroup`; =1 → `BDItemRow` เดี่ยว.
  - **`BDDayItemGroup`** คำนวณ `kinds` เอง: ชนิดเดียว → ป้ายชนิดนั้น; หลายชนิด → ป้าย **"รวม"** (`#eef2ff`). หัวโชว์ "· N รายการ". กาง = `BDItemRow` แต่ละรายการ **โชว์ป้ายชนิด (ไม่ hideTag)** + title เต็ม + sub (เลขที่ AP/PV/เช็ค) → เห็นว่ามาจากแหล่งไหน. (เดิม hideTag + label=refDoc → เลิก เพราะตอนนี้ต้องเห็นแหล่งที่มา.)
- **verify (preview, isolated — login-gate+RLS):** ทุกฟังก์ชัน compile เป็น `function` · `bdVendorCanon` รวม 2 ธารา + 2 เอ็กเซีย (คำนำ/AP/งวด ต่างกัน) เป็นชื่อเดียว, มหาชน/null/number ปลอดภัย · reducer 10 raw → 7 rows (ธารา −378,166.36 = 2×189k, เอ็กเซีย −181,364.48 = 2×90.7k, เอน คอนแพค รวม forecast+PV ป้าย "รวม", transfer เดี่ยว) · render `BDDayGroup` จริง: ย่อ = "ประมาณการ · บริษัท ธารา… · 2 รายการ · −378,166.36" / "รวม · บริษัท เอ็กเซีย… · 2 รายการ", กาง = sub-item โชว์ป้าย ประมาณการ/PV + เลขที่อ้างอิงต่อรายการ · ไม่มี render error.

## 2026-06-25 — ลบ SHEET CONFIG ออกจาก config.js + `pushProjectsToSheet` ย้ายมาใช้ Supabase (build `config 20260625a` / `page_project_control 20260625a`)
- **ลบ 4 key ที่เหลือจาก Sheets era ออกจาก `app/config.js`:** `SHEET_ID`, `APPS_SCRIPT_URL`, `AUTO_REFRESH_MS`, `ROW_LEVEL_SYNC` — ไม่ถูกใช้งานตั้งแต่ cutover Supabase. `data_sync.js` early-return ก่อนอ่าน Sheets keys (line 33-36); `data_supabase.js` ใช้ `cfg.SHEET_ID` แบบ `cfg.SHEET_ID ? ... : ''` (กลายเป็น '' โดยไม่มีผล); `data.js` `isOnline()` เส้นทาง Supabase ไม่แตะ key เหล่านี้ → ลบปลอดภัย.
- **`pushProjectsToSheet` ใน `page_project_control.jsx` เปลี่ยนจาก direct Apps Script fetch → `WTPData.writeTable('projects', rows, r => String(r.id || ''))`** (เหมือน P&L/Budget import ที่ทำใน Phase 5). ข้อความ UI เปลี่ยนจาก "Google Sheet" → "Supabase"; ปุ่ม forcePushFull เปลี่ยน label/title/confirm dialog ตาม. ลบโค้ด `if (!url)` + column-count check (ไม่มีความหมายใน Supabase context).
- **`config.js` ยังเก็บ `SHEET_ID` ไว้ไม่ได้** (ตัดทิ้งจริง) — ถ้าต้องการ gviz fallback ในอนาคต เพิ่ม key กลับมาได้ตอนนั้น.

## 2026-06-25 — ลบ `USERS` array ออกจาก config.js + รวมเจ้าหนี้เดียวกัน Bank Diary (build `config 20260625b` / `page_bank_diary 20260625a`)
- **ลบ `USERS: [...]` block ออกจาก `app/config.js`** — ตั้งแต่ cutover Supabase Auth (`USE_SUPABASE_AUTH:true`) login ผ่าน `WTPData.authSignIn` (Supabase) ไม่แตะ USERS อีก; `page_users.jsx` อ่าน USERS เพื่อโชว์ badge "config.js" (สีอำพัน) ที่ทำให้สับสน → ลบทิ้ง: badge หายไป, `configUsers` fallback `|| []` ทำงานถูกต้อง, `tools/supabase-auth-setup.html` โหลดผู้ใช้ผ่านปุ่ม "โหลดจาก Supabase" อยู่แล้ว ไม่ต้องการ config array อีก. (app.jsx early-return เมื่อ `USE_SUPABASE_AUTH` = true → old login path ไม่ถึง `configUsers` เลย.)
- **Bank Diary: รวมรายการ "เจ้าหนี้เดียวกัน" ในวันเดียว** (`page_bank_diary.jsx 20260625a`): เจ้าหนี้รายเดียวกันที่โผล่หลายบรรทัด (forecast มาจากคนละหน้า, เลขที่ต่างกัน) ถูก merge เป็นบรรทัดเดียว — helper กลาง **`bdVendorCanon(name)`** ดึงชื่อนิติบุคคล (regex จับ บริษัท/บมจ./บจก. … จำกัด(มหาชน)), `bdBuildAccountView` แนบ `creditor` ให้ items, `BDDayGroup` reducer ใช้ key `'c:'+direction+':'+creditor.toLowerCase()`. กลุ่ม >1 รายการ → `BDDayItemGroup` (ป้าย "รวม" เมื่อหลายชนิด, กางแล้วเห็นป้ายชนิด+เลขอ้างอิงต่อรายการ).

## 2026-06-25 — PV "ยอดจ่ายจริง" ผิด (Cash Flow โชว์ยอดก่อนหัก) + ปุ่มปรับยอดจาก e13 (build `page_data_extras 20260625a`)
- **อาการ (เตย):** ยอด **Actual** บนหน้า Cash Flow (`Σ pvVouchers.Net_Amount` ตาม `Pmt_Date`) โชว์ **ยอดก่อนหัก** ไม่ใช่ยอดจ่ายจริงหลังหัก (WHT/เงินประกัน/มัดจำ-เคลียร์เงินทดรอง).
- **ROOT CAUSE = ข้อมูลต้นทางขาดช่อง (ไม่ใช่บั๊กโค้ด):** PV import มาจาก **"AP รายงาน 4.3"** (`importSourceNote` ใน `DataPVPage`) ซึ่ง **ไม่มีคอลัมน์ "จ่ายจริง"** → ช่อง `Net_Amount` เลยได้ยอด net ก่อนหัก. โค้ด cashflow (`pv.Net_Amount || pv.Amount`) ถูกต้องแล้ว แต่ค่าที่ feed เข้ามาผิด.
- **ไฟล์ที่มีคำตอบ = `v_ap_rpte13` (e13)** — เป็น AP detail report 54 คอลัมน์ (raw db column names: `vchno`=AP, `payh_vchno`/`plno`=PV, `paydate`, `due`, `amt`(ก่อน VAT), `vatamt`, `netamount`(=amt+vat, ก่อนหัก), `whamt`(WHT), `downamt`(ADV/มัดจำ — **บวก**=วางมัดจำเงินออกจริง / **ลบ**=เคลียร์/ตัดมัดจำ → cf_net=0,payamt=0), `retamt`(ประกัน), `cf_net`(สุทธิต้องจ่าย), **`payamt`(จ่ายจริง)** ★, `balanceamt`(คงค้าง AP), `jobcode`/`jobname`(โครงการ), `dpt_code`/`dpt_name`(แผนก), `cust_name`, `remark`). **สูตร:** `cf_net = amt + vat + downamt − wht − ret − lessoth`; paid → `payamt = cf_net`. **`payamt`/`cf_net` คือ "เงินออกจากบัญชีจริง" — จัดการเคสมัดจำ/เคลียร์เงินทดรองอัตโนมัติ (แถวเคลียร์ = 0).** ออดิตไฟล์จริง: PV 11,551 ตัว — 6,492 (56%) มีหัก, **Σnetamount 1,520ล้าน vs Σpayamt 1,448ล้าน → เกินจริง 71.5 ล้าน** (เช่น PV20260600047 ก่อนหัก 336,736 → จ่ายจริง 129,300).
- **FIX (เลือกวิธี "overlay ปลอดภัยสุด" — ไม่แตะ import 4.3, ไม่กระทบหน้าอื่น):** เพิ่มแบนเนอร์ **`PvActualPaidFixer`** (`config.banner` ของ `DataPVPage`, gate `canEdit`): อัปไฟล์ e13 → จับคู่ด้วย key **`PL_PV_No + '||' + AP_No` = `payh_vchno + '||' + vchno`** → preview (จับคู่ได้/จะแก้/ตรงแล้ว/e13-only + Δยอด + ตารางเดิม→จ่ายจริง) → กดปรับ = **update เฉพาะ `Net_Amount` ของ PV ที่มีอยู่แล้ว** (`payamt`; cell ว่าง→`cf_net`) → `forceSyncNow`. **ไม่เพิ่ม/ไม่ลบแถว · ไม่แตะฟิลด์อื่น** → ทุกหน้าที่อ่าน `Net_Amount` (Cash Flow/Bank Diary/Home/payment_recon) ถูกตามทันที. helper `pvFixNum` (parse "1,068.93"/"(2,997)"/number). **GOTCHA:** e13 เก็บ payamt/netamount เป็น **shared-string ข้อความ** (เช่น "640.93", "1,068.93" มี comma) ไม่ใช่ number → ต้อง coerce comma/paren เสมอ.
- **วิธีใช้ (เตย):** อัป 4.3 เข้า PV เหมือนเดิม → จากนั้นอัป e13 (export เฉพาะช่วงเดือนที่ต้องการ ไม่ต้องทั้ง 18 เดือน — ไฟล์เต็ม = 12,745 แถว/3MB) ที่แบนเนอร์ "🔧 ปรับยอดจ่ายจริงจาก e13" → ยอด Cash Flow ถูก. verify ใน preview (isolated render เพราะ login+RLS): comma-string→129,300.29, เคลียร์เงินทดรอง 8,420→0, ของที่ตรง/ไม่อยู่ในไฟล์ไม่ถูกแตะ, ไม่เพิ่ม/ลบแถว, sync ทำงาน.
- **ค้าง (ยังไม่ทำ):** AP/PV Explorer หน้าใหม่ (ดู/ค้น/วิเคราะห์ราย AP เต็ม 18 เดือน — 9 อย่างที่เตยอยากดู: จ่ายกี่ครั้ง/PV/วันจ่าย/ครบกำหนด/โครงการ/แผนก/remark/คงค้าง/ยอดครบทุกช่อง) เป็นถังแยก (ไม่บวม synced entities) — เสนอไว้ ยังไม่เลือกทำ.
- **2026-06-25 (build `page_cashflow 20260625b`) — บั๊กตามมา: `Net_Amount=0` ตกหล่นใน Cash Flow.** หลัง e13 fix → PV ตัดมัดจำ/เคลียร์เงินทดรอง `Net_Amount` = 0 แล้วถูกต้อง แต่ Cash Flow ยังโชว์ยอดเต็ม. **ROOT:** `page_cashflow.jsx` มีโค้ด `Number(pv.Net_Amount || pv.Amount || 0)` 2 จุด (line 728 = `pvActualByWeekCat`, line 1362 = `openActualDrill`) — JS treat `0` เป็น falsy → fallback ไป `pv.Amount` (ยอดก่อนหัก 1.86M) → **นับซ้ำเป็นเงินออกเต็มก้อน**. เช่น PV20260600113/APS2026050085 ตัดมัดจำเอเซีย วอเตอร์ 24/06/2026 → modal โชว์ ยอดสุทธิ 0.00 / Amount 1,867,289.71 แต่ Cash Flow ยังนับ 1.86M. **FIX:** เปลี่ยน `||` เป็น `pv.Net_Amount != null && pv.Net_Amount !== '' ? pv.Net_Amount : (pv.Amount || 0)` — แยกระหว่าง "0 = จงใจ" กับ "null/empty = ไม่มีค่า → fallback Amount". verify 7 edge cases: Net=0→0✓, Net=null→Amount✓, Net=''→Amount✓, Net=positive→Net✓, Net='0'→0✓, Net=undefined→Amount✓. **Pattern เตือน:** `field || fallback` อันตรายเมื่อ field มีค่า `0` ที่แปลว่า "จงใจตั้งเป็น 0" (เช่น ยอดจ่ายจริง=0 ของแถวเคลียร์). Bank Diary `bdNormPV` (`bdNum(t.Net_Amount)` ไม่ fallback) — ปลอดภัยอยู่แล้ว ไม่กระทบ.
- **2026-06-25 (build `page_cashflow 20260625d` / `page_bank_recon 20260625a`) — STM (BANK_RECON) "แถวผี" นับเบิ้ลใน Cash Flow Actual.** drill "จ่ายจริง" โชว์ STM ซ้ำ (เช่น 153,273.66 โผล่ 2 แถว ทั้งที่ statement จริงมี 1) → Actual โป่ง. **ROOT:** `recordActual` ([page_bank_recon.jsx:821](app/page_bank_recon.jsx:821)) สร้าง forecastEntries ด้วย `WTPData.newId()` (**id สุ่มทุกครั้ง**) + `setData` **append** → ถ้า statement line ถูก record 2 ครั้ง (re-import+re-record / 2 แท็บ / glitch) ได้ 2 แถว content เดียวกันคนละ id; reconState ชี้แค่ id ล่าสุด → แถวเก่า = **orphan ค้างถาวร** (self-heal เพิ่มเท่านั้น ไม่เคยลบ). `pvActualByWeekCat` + `openActualDrill` ไล่ `forecastEntries` รวม BANK_RECON ทุกแถว ไม่กรอง → นับเบิ้ล.
  - **★ บทเรียนสำคัญ (เคยพลาด build `c`):** เวอร์ชันแรก dedup ด้วย **content key** (`Bank_AC|date|amt|DESCRIPTION`) — **ผิด!** เพราะ **ค่าธรรมเนียม LG/รายการจ่ายซ้ำจริง มี ยอด+วัน+desc เหมือนกันเป๊ะหลายครั้ง** (ผู้ใช้ยืนยัน) → content-dedup ยุบเหลือ 1 = **ยอดขาด**. ห้าม dedup BANK_RECON ด้วยเนื้อหา.
  - **FIX ที่ถูก = ใช้ `bankReconState` เป็นตัวตัดสิน** (1 entry ต่อ 1 บรรทัด statement ที่ record → `forecastId`). LG จ่าย 2 ครั้ง = 2 บรรทัด statement = **2 reconState entry** → 2 forecastId → **ของจริงทั้งคู่**. "แถวผี" = forecastEntries BANK_RECON ที่ **id ไม่ใช่ forecastId ปัจจุบันของ reconState entry ไหนเลย** + มี **live twin** (ตัวจริง content เดียวกัน) อยู่ → ตัด. `reconValidity` memo + `isReconDupOrphan(fe,date,amt)` ใน `page_cashflow.jsx` (ใช้ทั้ง `pvActualByWeekCat` + `openActualDrill`): `validIds`=Set ของ forecastId ที่ `decision='recorded'`; ถ้า `validIds.size===0` (reconState ไม่โหลด/ไม่ใช้) → ไม่กรอง (กันยอดหาย); orphan ที่**ไม่มี twin** ยังนับ (เผื่อ reconState หาย). verify: phantom 153,273.66 ตัด · **LG x2 (content เหมือน) เก็บทั้งคู่** · lone orphan เก็บ · ตัวจริงครบ.
  - **+ ลบแถวผีออกจริง (ปุ่มในหน้ากระทบยอด):** banner "🧹 ลบแถวผี (N)" ([page_bank_recon.jsx](app/page_bank_recon.jsx) `brOrphanRows`/`purgeReconOrphans`, gate `!readOnly`) — หา orphan-with-live-twin เดียวกับ cashflow → `WTPData.forceDeleteRows('forecastEntries', ids)` (ข้ามเกราะ protect-ACTUAL). **ตัวจริง + LG จ่ายซ้ำจริง ไม่โดน** (มี reconState entry → ไม่ใช่ orphan). verify: mock LG x2 + phantom + lone → flag เฉพาะ phantom. (cashflow filter ซ่อนไม่ให้นับ · ปุ่มนี้ลบออกจาก data จริง — เตยกดเองครั้งเดียว).
  - **ค้าง:** root hardening (deterministic forecastId จาก line.id + upsert ใน recordActual กัน orphan เกิดใหม่) ยังไม่ทำ — ตอนนี้กันที่ปลายทาง (filter + ปุ่มลบ). ดู [[bankrecon-actual-clobber]].

## 2026-06-26 — Project Control: sub-nav "ข้อมูลโครงการ / ข้อมูลการเงิน" + manual fcDates (build `pc_engine 20260626b` / `page_project_control 20260626b`)
- **คำขอผู้ใช้:** เพิ่มแท็บข้างปุ่ม Upload Excel หน้า #projects: (1) ข้อมูลโครงการ (เดิม) (2) **ข้อมูลการเงิน** (ใหม่) ที่โฟกัสการเงินรายโครงการ.
- **`ProjectFinanceGrid` (ใหม่ ใน page_project_control.jsx) — table 1 แถว/โครงการ ลงนามแล้ว** (filter `status !== ยกเลิก/ยังไม่ลงนาม && contractAmt > 0`). คอลัมน์: sticky-left # + โครงการ · **ผู้รับโอนสิทธิ์** chip คลิกได้ (`PcAssigneeChip`: empty=ปุ่ม "+ ระบุ" / hasLink=ฟ้า / no link=เหลือง) · **ภาระหนี้ปัจจุบัน** auto-link จาก `PCU.debtForProject` · **ราย งวด dynamic** (`maxInst = Math.max(2, longest installments)`): ส่งมอบ / มูลค่า / ตรวจรับ / **📅 คาดการณ์รับเงิน** = `PcFcDateInput` (input type=date) value = `fin.fcDates[i] || it.forecastDate` (manual ทับ auto, ฟ้า bold). paid งวด โชว์ "รับแล้ว" แทน input.
- **engine ([pc_engine.jsx](app/pc_engine.jsx) `buildInstallments`):** อ่าน `fin.fcDates[n-1]` ถ้ามี → `forecastDate = manualFc` (override auto `acceptDate||deliveryDate + creditTerm`) + ตั้ง `forecastManual=true`. cascade: `splitForecast` ใช้ `i.forecastDate` ตามเดิม → `fc1Date`/`fc2Date`/`forecastLines[]` อัปเดต → **Cashflow Forecast (section 03) + War Room P1 "รายรับสะสมประจำปี" bucket รายเดือนเปลี่ยนตามทันที** (cascade ผ่าน engine ไม่ต้องแก้หน้าอื่น).
- **`saveFcDate(r, i, dateStr)`** — เขียน `PCU.setFinanceField(contractNo, { fcDates: [...] })` → sync ผ่าน `pcfin.<ContractNo>` (manualOverrides ทีมเห็นเหมือนกัน) + ตัด trailing nulls.
- **คลิก assignee chip → `DebtFormModal` (reuse จาก [page_debt.jsx](app/page_debt.jsx))** pre-filled `{projectCode, projectName, principalAmount: outstandingAR||contractAmt, balance: same}`. existing debt → edit mode / no debt → new mode. onSave → append/update `data.debtMaster` + `PCU.setFinanceField(contractNo, {linkedDebtContract, assignee})` → **sync ไปหน้าภาระหนี้ + หน้าใบแจ้งหนี้ทันที** (page_invoices `resolveAssignee` อ่าน `pcfin.assignee` อยู่แล้ว). DebtFormModal เป็น global จาก page_debt.jsx ที่โหลดทีหลัง — เรียกใน render handler (call-time resolve) จึง ok.
- **`PCU.debtForProject(projectCode, debtMaster)` (helper ใหม่)** — filter `projectCode===code` (exact หรือ tokens คั่น `,;/`) + `status==='Active'` → คืน `{total: Σ balance/principalAmount, debts[], assignee: first borrowerName, contracts[]}`. exposed บน `window.PCU`.
- **sub-nav UI (ใต้ topbar)** — 2-button toggle: [📋 ข้อมูลโครงการ] [💰 ข้อมูลการเงิน]. state `tab` ('overview'|'finance'). overview = 5 sections เดิม; finance = `<ProjectFinanceGrid>` ภายใต้ band "F1".
- **verify (preview, isolated render mock 2 rows + 1 unsigned + 1 debt linked):** Babel ผ่าน 0 error · table 2 rows (unsigned filtered) · headers ครบ (#/โครงการ/ผู้รับ/หนี้/งวด 1-3 dynamic) · chip empty/linked ถูก · debt 1,500,000 (Σ balance LN-001 จาก projectCode=ENC101) · 4 fc inputs (paid งวด skip) · engine end-to-end: manual fcDate 2026-06-15 ทับ auto 2026-04-09 → `forecastLines[0].date='2026-06-15'` ✓ · `debtForProject` filter status=Close/projectCode ผิด ถูกตัด.

## 2026-06-27 — แท็บ "ตารางคำนวณดอกเบี้ยลีซอิท" ในหน้า #debt + 3 entities ใหม่ + Excel export มีสูตร (build `leasit_engine 20260627c` / `page_debt_leasit 20260627b` / `page_debt 20260627a` / `data.js 20260627a` / `data_supabase 20260627a`)
- **โจทย์ (เตย):** นำเข้าไฟล์ `ตารางคำนวณดอกเบี้ยเงินกู้-ลีซอิท.xlsx` (94 ชีต = 88 สัญญา) เข้าระบบ — ติดตามดอกจ่ายล่วงหน้า/เกิดจริง/ค้างรับคืน รายสัญญา + **export Excel ทุกเดือนให้บัญชีตรวจ "ต้องเห็นสูตรการคำนวณดอก"** (ไม่ใช่แค่ value).
- **โครงสร้างไฟล์ต้นทาง (verify ด้วย Excel COM):** ชีต `รายชื่อโครงการ` (ทะเบียน loanId→jobNo/projectName/product) + `สรุปรวม` (master + cross-sheet SUM formulas) + ชีต `01`–`88` (1 สัญญา/ชีต). **layout มี 3 แบบ:** (1) ใหม่ — R10 header `Type|Month|Year|เริ่ม|สิ้นสุด|Principal|Int.rate|Days|Int.amount|…` + Prepaid block + R24 marker "ดอกเบี้ยที่เกิดขึ้นจริง" + Actual block + "สรุป"/"ส่วนต่าง" + free-text refund (col 3 text + col 5 amount); (2) เก่า (ชีต 01-29 hidden) — **ไม่มี col Type** (Month อยู่ col 0) → ทุกแถว = Prepaid implicit, ไม่มี Actual block; (3) variant — col 0 มี "Prepaid" (ไม่ใช่ "Type") เป็นหัวคอลัมน์. **สูตรในต้นทางคงที่ทุกชีต:** `=F*G*H/365` (Int.amount) · `=DATEDIF(D,E,"d")+1` (Days) · `=SUM(I26:I32)` (subtotal) · `=H9`/`=I23`/`=E34-E35` (สรุป).
- **Data model — ผูกกับ `debtMaster` เดิม (ตามคำขอ "ภาระหนี้คือศูนย์กลาง"):** เพิ่ม debtMaster row ต่อสัญญาด้วย `id='lit_<loanId>'` (deterministic, namespaced) + `leasitSource:true` flag + ฟิลด์ Leasit-specific (`leasitLoanId/leasitTicketType/leasitProductType/leasitDateReceived/leasitDateDue/leasitDateDueRoll/leasitDateRepaid/leasitTermDays/leasitPrincipalChequeNo/leasitTotalPrepaid/leasitTotalActual/leasitVariance/leasitRefunded/leasitRefundOutstanding`) + map กับฟิลด์เดิม (`debtCategory='LIT'`, `borrowerName='บจก. ลีซอิท'`, `principalAmount`, `interestRate`, `status='Active'|'Close'`, `balance=principal if Active else 0`, `projectCode=jobNo`, `projectName`). → **หน้า #debt ภาระหนี้รวมเห็นสัญญาลีซอิทรวมกับหนี้อื่นทันที** (ไม่ต้องดูแลตารางคู่ขนาน) + **#debt_ledger maturity alert** ใช้ `maturityDate` ที่ engine ตั้งให้.
- **3 child entities ใหม่ใน Supabase** (key by `loanId`, **ไม่ใช่** debtMaster.id เพราะ id อาจ rebrand): `interestSchedulePrepaid` · `interestScheduleActual` · `interestRefund` — JSONB pattern เดียวกับตารางอื่น. **⚠️ ต้องรัน [supabase/leasit-loans.sql](supabase/leasit-loans.sql) ใน SQL Editor ครั้งเดียว ก่อนใช้งาน** (สร้าง 3 ตาราง + trigger updated_at + realtime publication + RLS read=authenticated/write=staff·manager). ก่อนรัน: หน้าเปิดได้ (degrade — โหลด entities คืน [] + warn console "table not found in schema cache"); หลังรัน: เปิดใช้ได้เต็ม. wire ใน `data.js CRUD_KEYS` + `data_supabase.js CRUD_ENTITIES` (เพิ่ม 3 ชื่อต่อท้าย).
- **Engine `app/leasit_engine.jsx`** (global `window.LeasitEngine`): `parseLeasitWorkbook(wb)` → `{loans, prepaid, actual, refund, registry, errors[]}` · `litLoanToDebtRow(loan)` → debtMaster row · `litComputeTotals(loanId, …)` → recompute หลัง edit · `litBuildExportWorkbook(loans, prepaid, actual, refund)` → XLSX workbook พร้อมส่งบัญชี.
  - **Header detection** (`litFindScheduleHeader`): label-driven — รองรับ 3 layouts ผ่านเงื่อนไข `(c0∈{type,prepaid,actual} && c1=month && c2=year) → hasType:true` หรือ `(c0=month && c1=year && c2=เริ่ม && c3=สิ้นสุด) → hasType:false`. `litReadScheduleRows(aoa, headerRow, expectType, hasType)` shift offset −1 เมื่อ no-Type layout.
  - **Date handling:** `litToISO` รองรับ Date object / Excel serial (45995) / DD/MM/YYYY (CE) / DD/MM/พ.ศ. (`y>2400 → −543`); `litDaysBetween` inclusive +1 (ตรงสูตร DATEDIF+1).
  - **Days bug guard:** ถ้า days > 366 หรือเป็น date serial → recompute จาก start/end. ไม่งั้นใช้ค่าจากไฟล์.
  - **Refund parser** (`litReadRefundRows`): เริ่มหลังแถว "ส่วนต่าง" (col 3), กรองแถว `^(รวม|ส่วนต่าง|Total|ยอดดอกเบี้ย)` (กัน summary leak), ดึงประเภท RV/Transfer จาก keyword, parse วันที่ DD/MM/YYYY + ref doc `RVxxxx-xxx` จาก free text.
  - **GOTCHA comment bug:** `Leasit*/lit*` ใน block comment ปิด comment เร็วเกิน → Babel parse code เปล่า → `LeasitPanel` ไม่ register. ตระกูล [[global-scope-name-collision]] เวอร์ชัน comment syntax — เลี่ยง `*/` ใน comment text ตลอด.
- **UI `app/page_debt_leasit.jsx`** (global `window.LeasitPanel`): KPI 4 ใบ (สัญญา/ดอกล่วงหน้า/ส่วนต่าง/ค้างรับคืน) · toolbar (search/status/upload/export-all) · ตารางสรุปรายสัญญา 14 col คลิกได้ · `LeasitImportModal` (drop-zone + preview diff 4 KPI + 50 แถวแรก + errors + confirm) · `LeasitLoanDrawer` (header info + summary 5 ใบ + schedule prepaid/actual/refund + ปุ่ม export ราย loan). `handleImport`: upsert debtMaster (leasit rows ทับด้วย deterministic id) + replace 3 child tables เฉพาะ loanIds ที่ import. wire ใน [page_debt.jsx](app/page_debt.jsx) ผ่าน **sub-nav** `mainTab` (`'overview'|'leasit'`) — `overview` = ภาระหนี้รวมเดิม + `leasit` = `<LeasitPanel>`.
- **Excel export มีสูตรคำนวณ** (`litBuildExportWorkbook`): (1) ชีต **`สรุปรวม`** = ลำดับที่/contractNo/JOB/โครงการ/ประเภท/สถานะ/อัตรา/วันรับ/วันครบ/ระยะเวลา/วงเงิน + cross-sheet formulas (`='30'!E34` ลิงก์ ยอดล่วงหน้า/เกิดจริง/ส่วนต่าง จากแต่ละชีต) + `SUM(K2:K<n>)` grand total + วันคืนเงิน. (2) ชีตต่อสัญญา 1 ชีต/loanId = header R1-9 (master) → R10 header `Type|Month|Year|...` → Prepaid rows ที่ Days column = `{f:'DATEDIF(D11,E11,"d")+1'}` + Int.amount = `{f:'F11*G11*H11/365'}` (cellobject `{f:string}` ของ SheetJS) → subtotal = `{f:'SUM(I11:I19)'}` → marker "ดอกเบี้ยที่เกิดขึ้นจริง" → Actual rows (สูตรเดียวกัน) → subtotal → "สรุป" block (ยอดล่วงหน้า/เกิดจริง/ส่วนต่าง = ลิงก์เข้า subtotals) → refund list + `SUM(E…:E…)` + ค้างรับคืน = `E<varRow>-E<refTotalRow>`. → **บัญชีเปิดไฟล์ใน Excel แล้ว recalc ได้ ตรวจสูตรการคำนวณดอกได้ทั้งหมด**.
- **verify (preview, isolated parse ไฟล์จริงผ่าน fetch + เทียบ source):** 94 ชีตเข้า 94 loans ครบ (sheetsLikeLoan === loans), 750 prepaid + 159 actual + 123 refund, **0 errors**. ตัวอย่าง loan 30 (closed): tot_pre=27,123.29 ✓ / tot_act=23,506.85 ✓ / variance=3,616.44 ✓ / refunded=3,616.44 ✓ (RV 678.09 + โอนคืน 25/03/2026 2,938.35) / outstanding=0.00 ✓. loan 1 (old format ที่ก่อนแก้ fail): contractNo `LITSM/PBF/67-11103` + tot_pre=130,931.51 ✓.
- **Push checklist:**
  1. `git push origin master`
  2. รัน [supabase/leasit-loans.sql](supabase/leasit-loans.sql) ใน Supabase SQL Editor (สร้าง 3 ตาราง + RLS)
  3. ทีม hard-refresh (Ctrl+Shift+R) — โค้ดเก่าใน cache ยังไม่รู้จัก 3 entities
  4. เปิด #debt → tab "📑 ตารางคำนวณดอกเบี้ยลีซอิท" → กด "📥 นำเข้า Excel" → upload ไฟล์ต้นทาง (รอบแรกใช้เวลา ~3-5 วินาที สำหรับ 94 ชีต)

## 2026-06-27 — Leasit panel: ขยาย drawer + สรุปเงินต้น PRE/POS + alert ครบกำหนด 30 วัน (build `page_debt_leasit 20260627d`)
- **อาการ (เตย):** drawer ราย loan แคบเกินไป + อยากเห็น เบิก/จ่ายคืน/คงเหลือเงินต้น แยก PRE vs POS + แจ้งเตือนสัญญา Active ที่ใกล้ครบกำหนดภายใน 30 วัน.
- **ROOT (drawer แคบ):** `<Modal size="xl">` ที่ใช้ก่อนหน้า — Modal component ([components.jsx:502](app/components.jsx:502)) **ไม่รองรับ `size` prop** (รับเฉพาะ `wide` boolean + `maxWidth` number) → prop ถูก ignore → ตก default 540px. แก้ใช้ `maxWidth={1400}` (drawer) / `{1100}` (import preview) → `width: min(N, calc(100vw - 32px))` ขยายเต็มจอ.
- **PRE/POS classification:** `isPRE = String(leasitTicketType).toUpperCase() === 'PRE'` → fallback ทุกอย่างที่ไม่ใช่ 'PRE' (NON / 0 / empty / 'POS') = POS. ใน main panel เพิ่ม **2 การ์ดเทียบ** (PrincipalCard, สี oklch 250 ฟ้า PRE / 25 ส้ม POS) บอก **count · Active · เบิก · คืน · คงเหลือ** ราย ticket — คำนวณ:
  - drawn = Σ principalAmount ของทุก loan
  - repaid = Σ principalAmount ของ status==='Close' (= ปิดหนี้ ปิดเงินต้นครบ)
  - outstanding = Σ principalAmount ของ status==='Active'
- **Drawer ราย loan:** เพิ่ม **💰 สรุปเงินต้น card** (PRE/POS badge + Pre/Post-financing label + 3 ช่อง เบิก/จ่ายคืน/คงเหลือ พร้อม sub-line "รับเงิน {date}" / "ปิดหนี้ {date}" / "ครบกำหนด {date}") เหนือการ์ด header เดิม.
- **Maturity alert (main panel banner):** `maturityAlerts` memo — Active loans ที่ `leasitDateDueRoll || leasitDateDue` ≤ 30 วันจากวันนี้ (รวม overdue = days < 0). banner ส้มอ่อน + grid auto-fill 280px/col แสดงราย loan (id/contract/projectName/ครบกำหนด/เงินต้น) + **badge สีตามความเร่งด่วน** (days<0 แดง / days≤7 ส้ม / อื่น เทา). **คลิกการ์ดเปิด drawer** (setViewLoan). จำกัด 12 รายการแรก + แจ้ง "อีก N".
- **Drawer ส่วน maturity:** banner ในตัว drawer แยก 2 สถานะ — `maturityNear` (0-30 วัน สีเหลือง "⏰ ครบกำหนดในอีก N วัน") / `maturityOverdue` (days<0 สีแดง "⚠️ เกินกำหนดแล้ว N วัน") + sub-line "ครบกำหนด {date} · เงินต้นคงเหลือ {amt}". ไม่โชว์ถ้า Close หรือไม่มี dueDate.
- **verify (preview, isolated render `<LeasitPanel>` ลง overlay + mock 6 rows ครอบทุกเคส — login-gate+RLS → DOM/eval):** maturity banner = **2 สัญญา** (#4 POS overdue 3 วัน + #1 PRE near 5 วัน) ✓ — #2 (90d), #5 (60d), #3/#6 (Closed) ถูกกรองออก. PRE card: 3 สัญญา / Active 2 / เบิก 4.75M / คืน 0.55M / คงเหลือ 4.2M ตรงทุกตัว. POS card: เบิก 5M / คืน 1.2M / คงเหลือ 3.8M ตรง. Drawer (#1 near 5d): "⏰ ครบกำหนดในอีก 5 วัน" + PRE Pre-financing badge + เบิก 2.7M / คืน 0 "— (ยังไม่ปิดหนี้)" / คงเหลือ 2.7M ✓. Drawer (#3 Close): ไม่มี maturity banner + เบิก 0.55M / คืน 0.55M "ปิดหนี้ 15/03/2026" / คงเหลือ 0 ✓. Drawer (#4 overdue): "⚠️ เกินกำหนดแล้ว 3 วัน" สีแดง + POS Post-financing ✓. ไม่มี console error.

## 2026-06-27 — Leasit: เพิ่ม/แก้ไขสัญญาเอง + autocomplete โครงการ + คำนวณ schedule อัตโนมัติ + จ่ายคืนเงินต้นหลายครั้ง (build `leasit_engine 20260627i` / `page_debt_leasit 20260627e`)
- **โจทย์ (เตย):** เปลี่ยน Leasit panel จาก "import-only" → **full CRUD tool** — ทำงานต่อจาก Excel ในเว็บได้เลย: เพิ่ม/แก้ไขสัญญา + ค้นหาโครงการเด้ง autocomplete + คำนวณดอกเบี้ยจากเว็บ + จ่ายคืนเงินต้นหลายครั้ง + เลขเช็คทั้งเงินต้น+ดอก รายงวด.
- **Engine helpers ใหม่ (`leasit_engine.jsx`, exposed บน `window.LeasitEngine`):**
  - **`litGenerateMonthlySchedule(principal, rate, startISO, endISO)`** — สำคัญสุด: split ตามรูปแบบ **Leasit charge pattern จริง** = แบ่งที่ **วันที่ 20 ของเดือน + วันสิ้นเดือน** (ไม่ใช่แค่สิ้นเดือนอย่างเดียว). คำนวณ days แบบ inclusive ทั้ง 2 ขอบ (+1) สำหรับ mid-segments + **exclusive ขอบสุดท้าย (วันครบกำหนดไม่นับ, เป็นวันคืนเงิน)**. ใช้สูตร `principal × rate × days / 365` ตรงกับต้นทาง. **★ GOTCHA: เลี่ยง `Date.toISOString()`** ใน TZ +07 จะ shift 1 วันก่อน (`new Date(2025,11,20).toISOString().slice(0,10)` = "2025-12-19" ไม่ใช่ "2025-12-20") → ใช้ local components `y + pad2(m) + pad2(d)` ตรงๆ. verify: loan 30 (550K, 0.15, Dec 4 → Apr 3) → **9 rows · 120 วัน · 27,123.29 ตรง source เป๊ะ** + days [17,11,20,11,20,8,20,11,2] ตรงทุก row.
  - **`litCalcTermDays(s,e)`** — `(end-start)` ไม่ +1 (วันครบกำหนดไม่นับ; loan 30 → 120 วัน)
  - **`litNextLoanId(debtMasterRows)`** — max(leasitLoanId) + 1
  - **`litBlankLoanDraft(debtMasterRows)`** — template สำหรับสร้างใหม่ (auto loanId, default rate 0.15, ticketType PRE, status Active)
  - **`litSearchProjects(projects, query, limit)`** — autocomplete: substring match บน `Contract No.` / `Site Name` / code / projectName (ผูกกับ `data.projects` ที่ sync จาก Supabase)
  - **`litGenerateActualFromClose(p, r, startISO, repaidISO)`** — delegate ไป `litGenerateMonthlySchedule` (จาก dateReceived → dateRepaid)
- **`LeasitProjectAutocomplete` (component ใหม่):** input cascade [รหัสโครงการ 120px | ชื่อโครงการ flex] + dropdown absolute เมื่อพิมพ์ (top 10 matches, hover bg, click → fill ทั้งคู่). กรอกเองได้ด้วย (ไม่บังคับเลือกจากลิสต์) — footer "หรือพิมพ์เองได้เลย". close ผ่าน mousedown นอก wrapper.
- **`LeasitLoanForm` (component ใหม่, modal maxWidth=1300):** 4 tabs:
  - **📋 ข้อมูลสัญญา** — 12 ฟิลด์ master: contractNo · projectAutocomplete · ticketType (PRE/POS/NON dropdown) · principal · rate · 5 dates (รับ/ครบ/โรล/คืน + termDays auto) · principalChequeNo · interestChequeNo (เผื่อใช้ทั้งสัญญา) · status auto จาก dateRepaid · note
  - **💰 ดอกล่วงหน้า** — ปุ่ม ⚡ "คำนวณตารางดอกเบี้ยอัตโนมัติ (รายเดือน)" → call generator + ปุ่ม + เพิ่มงวด (manual) + ตารางแก้ inline (date/principal/rate/days/intAmount/**chequeNo รายงวด**/paymentDate) + ปุ่ม ✕ ลบรายแถว + footer "รวม" + hint สูตร
  - **✅ ดอกเกิดจริง** — ปุ่ม ⚡ "คำนวณจากวันรับ → วันคืนเงิน" (disabled ถ้าไม่มี dateRepaid) + ตารางแก้ inline เหมือนกัน
  - **💸 รับคืน/จ่ายคืน** — 2 ปุ่มเพิ่ม `+ รับคืนดอก (RV/โอน)` กับ `+ จ่ายคืนเงินต้น` → row ใหม่ใน `interestRefund` แตกต่างกันด้วย field **`kind: 'interest' | 'principal'`** (มีคอลัมน์ select แก้ทีหลังได้) + date · amount · refDoc (RV20XX/PV) · note
  - **Footer**: ปุ่ม "🗑️ ลบสัญญา" (สีแดง, เฉพาะ edit mode + canEdit + confirm 1 รอบ) ซ้าย / ยกเลิก + 💾 บันทึก ขวา
- **Wire ใน `LeasitPanel`:**
  - toolbar เพิ่ม **➕ เพิ่มสัญญา** (สีน้ำเงิน, ก่อน 📥 นำเข้า Excel) → `setFormState({mode:'new'})` + state `formState`
  - drawer เพิ่ม **✏️ แก้ไข / เพิ่มจ่ายคืน** (สี oklch 250 น้ำเงิน, ซ้ายล่าง) → ปิด drawer + เปิด form mode='edit' + pre-filled
  - `handleSaveForm(draft, preRows, actRows, refRows)`: deterministic id `'lit_'+loanId` + recompute totals (totalPrepaid/Actual/refunded/refundOutstanding) + แยก `principalRepaid` = Σ refund.kind='principal' + balance = principal − principalRepaid (Active) หรือ 0 (Close) + replace 3 child tables เฉพาะ loanId นั้น + forceSyncNow + toast
  - `handleDeleteForm(draft)`: ลบ debtMaster row + 3 child tables เฉพาะ loanId + forceSyncNow + ปิด drawer + toast
- **verify (preview, isolated render `<LeasitPanel>` + mock projects, full E2E):**
  - autocomplete: พิมพ์ "ENC" → 2 matches (ENC165 + ENC301) → click → projectCode + projectName auto-fill ✓
  - กรอก contract + dates + principal=550000 + rate=0.15 → termDays auto = **120** (ไม่ใช่ 121) ✓
  - กดคำนวณ schedule → **9 rows · total 27,123.29 ตรง source** ✓
  - กดบันทึก → modal ปิด + toast "บันทึก LITSM/PBF/68-TEST สำเร็จ" + debtMaster มี 1 row `id=lit_1` (deterministic) + prepaid 9 rows + master snapshot ครบ (`leasitTotalPrepaid=27123.29`, `leasitTermDays=120`, `status=Active`) ✓
  - prepaid row 1: dateStart=2025-12-04 dateEnd=2025-12-20 days=17 intAmount=3,842.47 ✓ (ตรง source)
  - prepaid row 9: dateStart=2026-04-01 dateEnd=2026-04-03 days=2 intAmount=452.05 ✓ (ตรง source — exclusive end)

## 2026-06-27 — Leasit: PRE/POS/NON 3 categories + parser อ่าน "ประเภท" จากสรุปรวม + compact layout (build `leasit_engine 20260627j` / `page_debt_leasit 20260627f`)
- **อาการ (เตย):** หลัง deploy import 94 สัญญา system โชว์ 27 PRE / 67 POS — **ผิด**. ผู้ใช้ตรวจ Excel แล้วบอกมี **3 ประเภทไม่ใช่ 2**: PRE / POS / **NON (Term Loan ระยะยาว เช่น 20M)** + อยากเห็นภาพรวม+ตาราง 15 แถวใน 1 จอ (ไม่ต้องเลื่อน) ตามรูปแบบ Excel.
- **ROOT (classification ผิด):** ชีตเดี่ยวบางสัญญา cell "ประเภทตั๋ว" = **0** (ไม่ระบุ) → parser เดิมตี ticketType ตามชีตเดี่ยว → ทุกตัวที่ค่า=0 หรือว่าง = POS (default fallback) ทำให้ 67 ตัวที่จริงเป็น PRE หรือ NON ถูกเหมาเข้า POS. ของจริง: **ในชีต `สรุปรวม` คอลัมน์ "ประเภท" (col 4/E)** = authoritative (PRE/POS/NON ถูกหมด) — finance ทีมกรอกที่นี่ ไม่ใช่ที่ชีตเดี่ยว.
- **FIX 1 — parser อ่าน summary sheet:** `parseLeasitWorkbook` เพิ่ม `summaryTicket = {loanId: 'PRE'|'POS'|'NON'}` map (อ่าน R7+ ของชีต `สรุปรวม` col 0=ลำดับที่, col 4=ประเภท). ในลูปต่อสัญญาเพิ่ม `resolvedTicket = summaryTicket[loanId] || ticketType.toUpperCase()` (summary เป็นหลัก, ชีตเดี่ยวเป็น fallback) + sanitize ให้เป็น 1 ใน {PRE,POS,NON} (default POS ถ้า unknown). loan object + `litLoanToDebtRow` ใช้ `resolvedTicket` แทน `ticketType` เดิม. verify ไฟล์จริง 94 ชีต: **PRE 85 · POS 8 · NON 1** (เดิม 27/67/0). NON = "เงินกู้ยืมระยะยาว วงเงิน 20,000,000 บาท" (`lITSM/PBF/68-11193`, loanId 28) ✓ — ตรงที่เตยบอก. **⚠️ ผู้ใช้ต้อง re-upload ไฟล์ 1 ครั้งหลัง deploy** เพื่อ reclassify; ก่อน upload ยังโชว์ PRE 27/POS 67 ตามเดิม.
- **FIX 2 — 3 buckets ใน UI:** `principalSummary` เพิ่ม bucket `NON: { count, active, drawn, repaid, outstanding }` + classification `t === 'PRE' → PRE; t === 'NON' → NON; else POS`. drawer `ticketLabel/ticketColor/ticketDesc` รองรับ 3 ค่า (สี: PRE oklch 250 น้ำเงิน · POS 25 ส้ม · NON 305 ม่วง) + Desc ("Pre-financing"/"Post-financing"/"Non-PRE (Term Loan)"). ตารางหลักเพิ่ม **คอลัมน์ "ประเภท"** ระหว่าง # กับ เลขที่สัญญา (chip สีตาม label) → แยกชั้นได้ทันที.
- **FIX 3 — compact layout (15 rows/page):** เป้า — ไม่ต้องเลื่อนเห็น KPI+ตาราง ใน 1 จอ.
  - **Maturity banner ย่อเป็น 1 บรรทัด default** (`maturityOpen` state, padding 8/12 — เดิม 14) — โชว์ "▸ ครบกำหนดภายใน 30 วัน (N สัญญา · เงินต้น X)" + summary ขวา "M เกินกำหนด · K ใน 7 วัน". กดเปิด → grid auto-fill minmax 260px (เดิม 280) padding 7/font 11 (เดิม 10/12).
  - **Principal cards ย่อ:** padding 8 (เดิม 14), borderTop 2px (เดิม 3px), badge font 10/พริ้ม 7 (เดิม 11/10), grid label font 10/value 13 (เดิม 12/16).
  - **เปลี่ยน 2 cards → 3 cards + KPI dual-row (4 ตัวเลขในการ์ดเดียว):** grid `repeat(auto-fit, minmax(220px, 1fr))` — ทำให้ wrap ตามจอ (4 col บนจอกว้าง / 2 col บนจอแคบ). KPI card รวม "ดอกล่วงหน้า/เกิดจริง/ส่วนต่าง/ค้างคืน" ใน 2×2 grid + header "💰 ดอกเบี้ย · N สัญญา (🟢A·⚫C)". แทน KPI Row 4 cards เดิมที่ตั้งสูง ~88px → ตอนนี้รวมในแถวเดียวกับ Principal (~120-140px).
  - **ตาราง compact**: row padding 4/6 (เดิม default ~8/10), font 12 (เดิม 13), border-bottom 1px ink-100. **Pagination 15 rows/page** (state `page`+`PAGE_SIZE=15`): footer "หน้า X/Y · แสดง A–B จาก N" + ปุ่ม « ‹ › » (disabled ตามขอบเขต). filtered กรอง+sort เหมือนเดิม pageRows = slice(start, end).
- **verify (preview, isolated render `<LeasitPanel>` + 94 mock rows จากไฟล์จริง):** classification: PRE 85 / POS 8 / **NON 1 = 20M** ✓ · 4 cards render (3 PrincipalCard + 1 KPI inline) · table โชว์ **15 rows ต่อหน้า** + pagination footer · maturity banner ย่อ default ("▸ 7 สัญญา · เงินต้น 12,303,000") · chip ประเภทในตารางทุกแถว · ไม่มี console error.
- **Push checklist (อัปเดต):** หลัง deploy + hard-refresh → เตยกด **📥 นำเข้า Excel re-upload ไฟล์ลีซอิทอีกครั้ง** ให้ ticket type ของ 94 สัญญาเปลี่ยนเป็น PRE/POS/NON ที่ถูก (กระทบเฉพาะ `leasitTicketType` ใน debtMaster — ไม่กระทบ schedule/refund ที่กรอกมือ).

## 2026-06-27 — Leasit: เงินต้นก่อนดอกเบี้ย + defensive form + reload button + warning เมื่อ SQL ยังไม่รัน (build `page_debt_leasit 20260627g`)
- **อาการ (เตย):** หลัง deploy build ก่อน — (1) "ปุ่มเพิ่มสัญญากดแล้วไม่มีอะไรขึ้น" (2) "import เสร็จแล้วรีเฟรชข้อมูลหาย" (3) "อยากเห็นเงินต้นก่อน ดอกเบี้ยตามหลัง" (โครงสร้างเดิมเอา KPI ดอกแซมกับ Principal cards ในแถวเดียวกัน).
- **ROOT (form ไม่เปิด):** form `LeasitLoanForm` ใช้ `const E = window.LeasitEngine` ตรงๆ — ถ้า render run ก่อน engine โหลดเสร็จ (race condition จาก in-browser Babel transform 2 ไฟล์ขนาดใหญ่) → `E` = undefined → `E.litBlankLoanDraft()` throw → effect crash → modal ไม่ render (React swallow silently).
- **ROOT (data หาย หลัง import):** ผู้ใช้ยังไม่ได้รัน `supabase/leasit-loans.sql` → 3 child tables ไม่มีจริง → push ฝั่ง `interestSchedulePrepaid`/`interestScheduleActual`/`interestRefund` fail silent → schedule/refund ที่ผู้ใช้กรอก/import **ไม่ persist** (ตกหล่นถาวร). `debtMaster` (เก็บ master+leasit fields ใน JSONB) ยัง persist ปกติ — แต่ผู้ใช้ไม่ทราบสาเหตุ.
- **FIX 1 — Layout เงินต้นก่อน (โครงใหม่ตามที่ user ขอ):**
  - **แถว 1 (สำคัญสุด):** ป้าย "💰 เงินต้น — รับ / จ่ายคืน / คงเหลือ" + grid `repeat(3, 1fr)` ตายตัว — 3 PrincipalCard (PRE/POS/NON) เห็นเปรียบเทียบได้ทันที (เลิกใช้ auto-fit ที่ wrap แล้วลำดับเปลี่ยน).
  - **แถว 2:** ป้าย "📈 ดอกเบี้ย — ล่วงหน้า / เกิดจริง / ส่วนต่าง / ค้างรับคืน" + การ์ดเดียว grid `repeat(5, 1fr)` มี 5 ช่อง (สัญญา + 4 ตัวเลขดอก). font ใหญ่ขึ้น (value 16 จาก 13) เพราะมีพื้นที่กว่า — อ่านง่าย.
- **FIX 2 — Defensive form:** `LeasitLoanForm` เปลี่ยน `const E = window.LeasitEngine` → `const E = window.LeasitEngine || {}` (กัน method call crash) + ห่อ effect ใน try/catch + state `loadErr` + ถ้า error → render Modal เล็กๆ "⚠️ เปิดฟอร์มไม่ได้" + ปุ่ม "↻ refresh หน้า" (graceful fallback). เพิ่มเช็ค `if (!E || typeof E.litBlankLoanDraft !== 'function')` ก่อนเรียกใช้ engine.
- **FIX 3 — ปุ่ม "↻ ดึงใหม่" ใน toolbar:** เรียก `WTPData.refreshFromServer()` (ถ้ามี) → toast "ดึงข้อมูลจาก Supabase สำเร็จ" — ผู้ใช้ verify ได้ทันทีว่า data ใน Supabase หรือไม่ (ไม่ต้อง refresh ทั้งหน้า). fallback = full reload ถ้า method ไม่มี.
- **FIX 4 — Warning banner ถ้า SQL ยังไม่รัน:** เช็คเงื่อนไข `allRows.length > 0 && 3 child tables ทั้งหมดว่าง` → แสดง card สีเหลือง "⚠️ ตาราง schedule ใน Supabase ยังไม่มี — ดอกเบี้ยรายงวดไม่ถูก persist" + คำแนะนำให้ admin รัน `supabase/leasit-loans.sql` + ระบุชัดว่า debtMaster ยังอยู่ครบ (สัญญาไม่หาย). โผล่ขึ้นมาเฉพาะเมื่อจำเป็น (ไม่รก ถ้ารัน SQL แล้ว) — diagnostic ตรงจุด.
- **verify (preview):** layout มี header "💰 เงินต้น" + "📈 ดอกเบี้ย" แยกชัด ✓ · 3 PrincipalCard fixed grid ✓ · ปุ่ม "เพิ่มสัญญา" เปิด modal (14 input fields) ✓ · ปุ่ม "↻ ดึงใหม่" + warning banner render ตามเงื่อนไข ✓ · เคส engine ยังไม่โหลด → modal "เปิดฟอร์มไม่ได้" + ปุ่ม refresh (ไม่ crash).

## Repo rule: keep CLAUDE.md current
**Every time you `git push`, update this `CLAUDE.md`** to reflect anything that changed (architecture, conventions, new pages, gotchas). Treat it as part of the push, like the `?v=` bump.
