# Cashflow page — TODO (จดไว้ก่อนแก้)

หน้า: https://patima-fin.github.io/waterpog-fin/#cashflow
ไฟล์หลัก: `app/page_cashflow.jsx`
สถานะ: **ยังไม่แก้** — รออีกหน้า (index.html) ทำเสร็จก่อน กันแก้ชนกัน
เลขบรรทัดอ้างอิง ณ commit `b56b8f2` (อาจขยับถ้าไฟล์ถูกแก้)

---

## 1. เอาข้อความ "💧 โครงการรวมกลุ่มจ่ายตามสภาพคล่อง (คลิกดู)" ออกจากหัวตาราง
- **ที่:** `app/page_cashflow.jsx:1391` — หัวแถว section "2: กระแสเงินสดออก"
- ปัจจุบัน:
  `2: กระแสเงินสดออก (Outflow Details) · 4 หมวด · 💧 โครงการรวมกลุ่มจ่ายตามสภาพคล่อง (คลิกดู)`
- **แก้เป็น:** ตัด ` · 💧 โครงการรวมกลุ่มจ่ายตามสภาพคล่อง (คลิกดู)` ออก เหลือแค่
  `2: กระแสเงินสดออก (Outflow Details) · 4 หมวด`
- หมายเหตุ: ปุ่มเปิด pool สภาพคล่องยังอยู่ที่ label แถวหมวด 2 (`2. … 💧` line ~1403) — แค่เอาข้อความหัวออก ไม่กระทบปุ่ม

## 2. เอาคำว่า "สิ้น W2 → สิ้นเดือน" ออก
- **ที่:** `app/page_cashflow.jsx:1468` — แถว "💼 ยอดคงเหลือสุทธิปลายงวด (Final Net Position)"
- ปัจจุบัน (ใน `<td colSpan={2}>` ตรงกลาง):
  `สิ้น{weeks[nowWeek].label} → {isLastWeekOfMonth ? 'สิ้นเดือนถัดไป' : 'สิ้นเดือน'}`
- **แก้:** ลบข้อความในเซลล์นี้ (ปล่อยว่าง) หรือเอา caption cell ออก

## 3. รายการ "จ่ายแล้ว" จัดกลุ่มผิด — ต้องแก้หมวดได้ (เช่น ลีซ อิท ควรเป็นหมวด 3 ต้นทุนทางการเงิน)
- **อาการ:** `PV20260600002` บริษัท ลีซ อิท จำกัด (มหาชน) โผล่ในหมวด 1 (ดำเนินงาน) แต่ควรเป็น **หมวด 3 (ต้นทุนทางการเงินและดอกเบี้ย)**
- **รากเหง้า:** `openActualDrill()` ที่ `app/page_cashflow.jsx:1042-1043`
  - PV จัดหมวดจาก AP ที่ผูกกันเท่านั้น: `if (pv.AP_No) { ap = … ; c = categorizePayable(ap); }`
  - ถ้า PV **ไม่มี AP ผูก** → `c = 1` (ดำเนินงาน) เสมอ ไม่เช็คชื่อ Payee และไม่มีทาง override
- `categorizePayable()` ที่ `:109` จัดหมวดจาก keyword (`ดอกเบี้ย|interest|ค่าธรรมเนียม|wht|…`) → ชื่อ "ลีซ อิท" ไม่เข้า keyword เลย ตกหมวด 1
- **✅ ตัดสินใจแล้ว: ทำทั้ง (a) + (b)**
  - **(a) override หมวดราย PV** — เก็บ key ด้วย `PL_PV_No` (คล้าย `cf.paidAp.<vchno>` → ใช้ `cf.pvCat.<PL_PV_No>` ผ่าน WTPOverride, sync ข้าม user) + **ปุ่ม/ดรอปดาวน์แก้หมวดใน drill-down "จ่ายจริง"** (openActualDrill) แก้ได้ทีละรายการ
  - **(b) mapping ชื่อเจ้าหนี้ → หมวด** — ตาราง vendor→cat (ลีซ อิท + บริษัทลีสซิ่ง/การเงินอื่น → 3) ใช้ร่วมทั้ง AP และ PV; ให้ PV ที่ไม่มี AP วิ่งชื่อ Payee ผ่าน mapping + finance-keyword check ด้วย (เลิกตก default 1 ดื้อๆ)
  - **ลำดับความสำคัญตอน resolve หมวด:** override ราย PV (a) > vendor mapping (b) > `cf_category` ของ AP ที่ผูก > keyword > default 1
  - เก็บตาราง vendor→cat ที่ไหน? เสนอ `localStorage` (คล้าย `CF_FLEX_LS_KEY` ที่ `:162`) ให้แก้รายชื่อในแอปได้ — ตัดสินใจตอนลงมือ
- มีอยู่แล้ว: คอลัมน์ "หมวด CF" (`cf_category` dropdown) ที่หน้าจัดการเจ้าหนี้ `app/page_data_extras.jsx:2400` — ใช้กับ **AP เท่านั้น** ครอบไม่ถึง PV ที่ไม่มี AP (ข้อ a/b มาเติมส่วนนี้)
- 🔗 **ดูข้อ 6(ค):** ฝั่งประมาณการ (forecast drill) ก็ให้แก้ประเภทได้ด้วย (`fe.CATEGORY`) — ทำเป็นฟีเจอร์ "แก้หมวด" ชุดเดียวกันทั้ง 2 drill

## 4. "รับเงินโครงการ" — แยก"แผนล็อกเต็ม" ออกจาก "ยอดคาดที่เหลือ" (ตัด IV ที่รับแล้ว)
- **อาการ:** ตาราง section 01 ช่อง W2 = **10,989,798** ซึ่ง = ยอด "รับจริงไปแล้ว" เป๊ะ → ตารางคาดการณ์ดันโชว์เงินที่เข้าบัญชีไปแล้วว่า "กำลังจะรับใน W2"
  - ตัวเลขจริงจากจอ: W2=10,989,798 · สัปดาห์ที่เหลือ=15,337,662 · TOTAL=26,327,459 (= แผนล็อกเต็ม) · รับจริง=10,989,798
- **รากเหง้า:** เมื่อเดือนถูกล็อก `ivInflowByWeek.forecast` = `ivPlanBucketsFromLock` (`:560-562`) = baseline ที่ freeze **รวม IV ที่รับไปแล้ว** (ตอน lock ไม่กรอง paid — ตั้งใจ เพื่อ plan ไม่หด) → ตาราง (`planIv` `:742`) เลยโชว์รวมที่รับแล้ว
  - drill `'iv'` locked mode (`:833-859`) ก็นับ `it.net` ของ IV ที่ paid ด้วย (แค่ขึ้น "✅ รับแล้ว")

- **✅ ตัดสินใจแล้ว (resolve ข้อขัดแย้ง "ฟรีชต้องครบ" vs "คาดการณ์ไม่ควรมีของที่รับแล้ว"):** แยก 2 ก้อนคนละที่
  1. **แผนล็อกเต็ม (ครบทุกรายการ) — คงไว้** ที่การ์ด KPI `PlanVsActualCard` (`:1280` `plan={ivForecast}` = 26,327,459) + drill-down ที่ลิสต์ IV ทุกใบพร้อมสถานะ → **ไม่หายสักรายการ** ใช้เทียบ Plan vs Actual เหมือนเดิม
  2. **ตารางประมาณการ section 01 "รับเงินโครงการ" → เปลี่ยนเป็น "ยังคาดจะรับ (เหลือ)" = แผนล็อก − ที่รับแล้ว** (ตัด IV ที่ `ivIsPaid`/มี `ivActualReceiveDate`)
- **โบนัส = แก้บั๊กนับเงินซ้ำ:** เงินที่รับแล้วอยู่ใน "เงินสดคงเหลือยกมา" (`weekBF`/`liveBalance` `:770`) อยู่แล้ว → ถ้ายังนับเป็น inflow อีก = Final Net Position บวมเกินจริง การตัดออกทำให้ net position ถูกขึ้นด้วย
- **วิธีทำ:**
  - เพิ่ม bucket ใหม่ = `ivPlanBucketsFromLock` แต่ cross-ref invoices สด ตัดใบที่ paid ออก → ป้อนให้ `ivCombinedByWeek` (`:739`, ตารางเท่านั้น)
  - คง `ivForecast` (การ์ด KPI) = baseline เต็ม **ไม่แตะ**
  - drill `'iv'` locked: ตัด IV ที่รับแล้วออกจาก "ยอดที่นับ" ของตาราง (จะโชว์เป็นบล็อกแยก "รับแล้ว" ก็ได้ แต่ไม่รวมในยอด forward) ให้ผลรวม drill = เลขในช่องเสมอ
- **✅ confirm แล้ว (ผู้ใช้เคาะ):** TOTAL ในตารางเปลี่ยน **26,327,459 → ~15,337,662 (เหลือที่ยังไม่รับ)**; ยอดเต็ม 26.3M ยังอยู่ครบที่การ์ด KPI (แผนล็อก vs รับจริง 41.7%)
- เทียบ memory: cashflow-iv-forecast-paid, cashflow-iv-plan-lock-day1

## 5. ตัวเลข % (Grand Totals Actual) จางเกิน อ่านยาก
- **ที่:** `app/page_cashflow.jsx:1718` — `<span style={{ color: '#fff' }}>{grandPct.toFixed(2)}%</span>`
- **อาการ:** ป้าย `34.58%` สีขาวบนแถบ gradient ฟ้าอ่อน (`#5fe0ff → #b8f4ff` ที่ `:1714`) → contrast ต่ำ มองแทบไม่เห็น
- **แก้:** เปลี่ยนสีเลข % เป็นสีเข้ม (เช่น `var(--brand-800)` / ink เข้ม) หรือเพิ่ม text-shadow / พื้นหลังเข้มรองหลังตัวเลข ให้ตัดกับแถบฟ้าอ่อน
- หมายเหตุ: คำว่า "ของแผนทั้งเดือน" (`:1719`) อยู่บนแถบเดียวกัน ปรับพร้อมกันได้ · เช็กป้าย % ใน WEEK cards (`~:1627`) ด้วยว่าใช้สีขาวบนพื้นอ่อนเหมือนกันไหม

## 6. Drill-down popups — รวมกลุ่มรายการ + แก้ประเภทในประมาณการ
ทั้ง 2 โหมดของ modal (`page_cashflow.jsx:1765`): `mode==='actual'` (จ่ายจริง `:1783-1819`) และ plan/ประมาณการ (`:1828-1897`) — ตอนนี้เรนเดอร์ **flat** ด้วย `DrillRow` (`:2184`) ทีละแถว

**(ก) จ่ายจริง — รวมกลุ่ม + กดกางดูรายย่อย**
- ตอนนี้ลิสต์แบนทุก PV (เลข PV ซ้ำ/ผู้รับเงินเดียวกันโผล่หลายแถว เช่น PV…009 ×2, PV…012 ×2)
- รวมกลุ่ม **ชื่อ(ผู้รับเงิน)เดียวกัน + วันเดียวกัน** → 1 แถว header (ชื่อ + ยอดรวม + จำนวนใบ) กดกางเห็นรายย่อย

**(ข) ประมาณการ — รวมกลุ่มด้วย (ผู้ใช้ย้ำ: "ชื่อเดียวกัน + วันเดียวกัน")**
- key = normalize ชื่อ (ตัด " (เลขที่ AP)" ท้าย) + date · ยังแยก pending/paid (`:1847-1848`) ก่อน แล้วค่อยจัดกลุ่มในแต่ละบล็อก
- การแก้ยอด/จ่ายแล้ว/แก้ประเภท → อยู่ในรายย่อยที่กางออก (ต่อใบ)

**(ค) ประมาณการ — แก้ "ประเภท" (หมวด 1-4) ได้** ← ผู้ใช้ขอเพิ่ม
- ตอนนี้ `DrillRow` forecast แก้ได้แค่ AMOUNT (`commitForecastEdit` `:1169`) + ปุ่ม "✏️ แก้" (`:2260`)
- เพิ่ม: ดรอปดาวน์เลือกหมวด → `commitForecastCategory(feId, cat)` set `fe.CATEGORY` (ใช้ `CATEGORY_LABELS` `:139`)
- เปลี่ยนหมวดแล้ว item ย้าย bucket → ถ้าอยู่ drill หมวดเดิม (เช่น out1) ให้ลบออกจาก popup ทันที (เหมือน `toggleManualPaid` `:1143-1161` ตัด item ออก)
- AP ในประมาณการ: แก้หมวดผ่าน `cf_category` (มี dropdown ที่ `page_data_extras.jsx:2400` แล้ว — เพิ่ม inline ใน drill ได้)

**pattern ร่วม:** การจัดกลุ่ม (ก)(ข) เหมือน bank diary `BDDayItemGroup` (`page_bank_diary.jsx:676`) + FIX 4 → พิจารณาแยกเป็น component กลาง reuse ทั้ง 2 หน้า
**โยงข้อ 3:** ฝั่งจ่ายจริงใช้ override ราย PV (`cf.pvCat`) + vendor mapping; ฝั่งประมาณการแก้ `fe.CATEGORY`/`cf_category` — **ทำชุดเดียวกัน (ฟีเจอร์ "แก้หมวด" ครบทั้ง 2 drill)**
